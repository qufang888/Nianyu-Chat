# 念语 · 消息发送→模型回复 管线 与 情绪/朋友圈系统说明

> 本文说明两件事：(1) 本次新增的「心情平滑过渡系统」与「朋友圈敏感程度判定」；(2) 你问的——从你点发送、消息送到模型、再回到回复，这一路上有多少系统参与、哪些参数会被改动。

---

## 一、本次新增 / 改动

### 1. 心情平滑过渡系统（防「忽喜忽悲」）
- 角色内心维护一个**连续情绪值 `moodValue`（−100~100）**，不再只存离散心情词。
- 新增 `MOOD_VALENCE`（心情词→数值）映射与 `valenceToMood`（数值→最近心情词）分段反推。
- 核心函数 `applyMoodChange`：以「**心情过渡指数** `moodSmoothing`（0~1，默认 0.5）」为步进系数，把 `moodValue` 朝 AI 判定的目标心情**平滑移动**，`newVal = round(old + moodSmoothing × (target − old))`，再反推出显示的离散心情词。
- AI 每轮判定的 `requestMoodJudge` 与随机事件选定的心情 `handleChooseEvent`，**现在都走平滑写入**，不再瞬间跳变。
- **效果**：`moodSmoothing` 越小越平滑（变化幅度小、过渡慢，不会一下子开心一下子伤心）；越大越灵敏（更快贴近 AI 判定的目标心情）；设为 1 等同旧版每次直接跳变。

### 2. 朋友圈敏感程度判定（防「普通唠嗑也发圈」）
- `judgeAndPostMoments` 的 prompt 新增 `shareUrge`（0~100，AI 给出的「此刻想发朋友圈的强烈程度」），并把**角色当前心情**注入 prompt（聊到尽兴、情绪上扬时才更想分享）。
- 新增「**朋友圈敏感程度** `momentsSensitivity`（0~1，默认 0.5）」：阈值 `= (1 − momentsSensitivity) × 100`。
- 仅当 `postMoments === true` **且** `shareUrge ≥ 阈值` 时才真正发朋友圈。
  - 敏感程度高（如 80%）→ 阈值低（20）→ 稍聊到尽兴就发；
  - 敏感程度低（如 20%）→ 阈值高（80）→ 只有极度想发才发，**普通唠嗑几乎不发**。
- 仍受「单人物每日上限 `dailyMomentLimit` / `momentDailyLimit`」约束；旧模型未返回 `shareUrge` 时视为 100（向后兼容）。

---

## 二、发送消息 → 模型回复：一路上多少系统参与？

> 以**单聊**为例；群聊在「模型生成」阶段会为每个成员**串行**生成（其余阶段一致）。

### ① 前端采集（ChatWindow / MiniChat 发送逻辑）
- 采集：输入文本、附件图片、当前聊天 id/类型、自我身份（selfRole）、按聊天的世界书选择。
- 调用 `api.sendMessage(...)` → IPC `chats:send`。
- **改动参数**：无（仅采集与落库用户消息）。

### ② IPC `chats:send` → `handleSend`（主进程）
- 落库用户消息，进入上下文组装。

### ③ 上下文组装（`generateAIResponses` / `runOne`）
参与系统：
1. **历史消息** `dm.getMessages`（最近 16 条）。
2. **自我身份** `resolveActiveSelfRole`（全局 `currentSelfRoleId` / 按聊覆盖 `chatSelfRoles`）。
3. **世界书** `resolveWorldBook`（按聊 `chatWorldBooks` > 角色 `worldBookId` > 全局 `defaultWorldBookId`）。
4. **模型解析** `resolveRoleModel`（角色绑定模型；`supportsImages` 决定是否走多模态）。
5. **好感度/情绪倾向系统** `applyAffinityChange → analyzeSentiment`：扫描用户文本情绪词，可能改变 `role.affinity`（私密小窗且关闭「影响情绪好感」时跳过）。
6. **系统提示词构建** `buildSystemPrompt`：注入角色卡、`affinityTone(好感度)`、**当前心情** `buildEmotionContext(role.mood)`、角色规则+通用规则、记忆（观察者「记忆冻结」时跳过）、世界书。
7. **限流** `rateWaitMs` / `rateMark`：按模型 id 做 QPS 限速，必要时等待。
- **改动参数**：`role.affinity`。

### ④ 模型调用 `queryAI`（ai.ts，流式）
- 返回 content + 思维链(reasoning) + token 用量；`dm.addMessage` 落库（记录 `token_used`）。
- **改动参数**：消息库、token 计数。

### ⑤ 回复完成后的后台系统（每条回复后 `void` 触发，并行、不阻塞前端）
1. **心情判定** `requestMoodJudge → judgeMood`：AI 依据最近 N 条（`moodJudgeHistory`）判定心情；受冷却(`moodJudgeCooldownMs`)、`dialogueMoodImpact>0`、观察者跳过规则约束。→ **本次新增：经 `applyMoodChange` 平滑写入** `role.mood` + `role.moodValue`，广播 `role:mood`。
   - **改动参数**：`role.mood`、`role.moodValue`。
2. **关系值 & 朋友圈** `requestRelationshipAndMoments`：
   - `judgeRelationship`：更新 `role.bond`、`role.relation`，广播 `role:bond`。
   - `judgeAndPostMoments`：**本次新增：AI 额外返回 `shareUrge`，按 `momentsSensitivity` 阈值门控**，达标才 `dm.addMoment`（可选生图），广播 `moments:changed` / `moments:autoPosted`。
   - **改动参数**：`role.bond`、`role.relation`、`store.moments`。
3. **自动记忆** `extractMemories`（开启时）：AI 总结沉淀 `memories`。**改动参数**：`store.memories`。
4. **随机事件**：按好感度阈值(`eventNegAffinity` / `eventPosAffinity`)+冷却触发，弹 `RandomEventModal`；选定后 `handleChooseEvent` 调整好感度 + **本次新增：平滑写入心情**。**改动参数**：`role.affinity`、`role.mood` / `role.moodValue`。

### ⑥ 前端渲染
- 流式分块 `onChunk`（带 `belongs(streamId)` 守卫，防私密回复串入公屏），展示消息、心情徽标、观察者面板。
- `role:mood` / `role:bond` / `moments:*` 广播实时同步**主窗与小窗**。

---

## 三、一路上被改动的参数汇总

| 参数 | 由谁改动 | 受哪些设置门控 |
| --- | --- | --- |
| `role.affinity` | 情绪词分析 / 随机事件 | `privateAffectsEmotion`（私密窗）、`eventMoodImpact` |
| `role.mood`、`role.moodValue` | 心情判定 / 随机事件 | `dialogueMoodImpact`、`moodJudgeCooldownMs`、`moodJudgeHistory`、**`moodSmoothing`（新）**、观察者开关 |
| `role.bond`、`role.relation` | 关系值判定 | `autoRelationship`、`moodJudgeCooldownMs` |
| `store.moments` | 朋友圈判定 | `autoMoments`、`dailyMomentLimit`、`momentDailyLimit`、**`momentsSensitivity`（新）** |
| `store.memories` | 自动记忆 | `enableAutoMemory` |
| token 计数 | 每次模型调用 | — |

---

## 四、硬编码 / 默认值告知（便于你调参）
- 默认 `moodSmoothing = 0.5`、`momentsSensitivity = 0.5`：`src/types.ts` 的 `DEFAULT_SETTINGS`。
- `MOOD_VALENCE` 映射与 `valenceToMood` 分段阈值、`applyMoodChange` 公式：均在 `electron/main.ts`。
- 朋友圈阈值换算 `threshold = round((1 − momentsSensitivity) × 100)`：`electron/main.ts` 的 `judgeAndPostMoments`。
- 心情判定默认回顾 10 条、冷却 20 秒（既有，未改动）。
