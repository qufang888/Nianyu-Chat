# 硬编码清单（念语 nianyu-client）

> 范围：情绪系统 / 随机事件 / 自动触发，以及少量核心设计常量。
> 标记：✅ 已修复（已集中/改为 AI 判定） ｜ ⚠️ 保留（必要或后续可做成设置） ｜ 🔧 建议后续提取

## 一、情绪系统

| # | 位置 | 硬编码内容 | 状态 | 说明 |
|---|------|-----------|------|------|
| 1 | `electron/main.ts` `deriveMoodFromAffinity` | 好感度阈值→心情：≤20生气/≤45低落/<70平静/<90开心/否则撒娇 | ✅ 已删除 | 改为 AI 依据对话判定 `judgeMood` |
| 2 | `electron/main.ts` `MOODS` | 心情枚举 8 个（开心/撒娇/心动/害羞/平静/低落/难过/生气） | ✅ 已集中 | `MOOD_DESC`/`NEG_MOODS`/`POS_MOODS`/事件枚举/`validMoods` 全部引用它，不再重复列 |
| 3 | `electron/main.ts` `MOOD_DESC` | 8 种心情→系统提示语气文案 | ⚠️ 保留 | 必要映射：把 AI 判定的心情词翻译成上下文情绪 |
| 4 | `electron/main.ts` `affinityTone` | 好感度阈值 80/60/40/20 + 语气文案 | ⚠️ 保留 | 核心设计（好感度→语气），非本次范围 |
| 5 | `electron/main.ts` `judgeMood` | 历史窗口 `slice(-10)`→`moodJudgeHistory`、单条截断 `slice(0,140)`、maxTokens `200` | ✅ 已提取 | 历史窗口已做设置，其余保留 |
| 6 | `electron/main.ts` `requestMoodJudge` | 冷却 `20000`ms→`moodJudgeCooldownMs` | ✅ 已提取 | 避免每条都判定 |
| 7 | `electron/main.ts` `activeEvents` | 安全超时 `5*60*1000` | ⚠️ 保留 | 防事件占用泄漏 |

## 二、随机事件

| # | 位置 | 硬编码内容 | 状态 | 说明 |
|---|------|-----------|------|------|
| 8 | `electron/main.ts` `handleRandomEvent` bias 分支 | 剧情偏向阈值 `affinity < 30`（冲突）/`> 70`（甜蜜）→`eventNegAffinity`/`eventPosAffinity` | ✅ 已提取 | 已放入「情绪与事件（高级）」设置区 |
| 9 | `electron/main.ts` | 事件历史窗口 `slice(-12)`→`eventHistory`、单条截断 `slice(0,160)` | ✅ 部分提取 | 历史窗口已做设置，截断保留 |
| 10 | `electron/main.ts` | 事件生成 `queryAI(..., 700)`→`eventMaxTokens` | ✅ 已提取 | 调参项 |
| 11 | `electron/main.ts` | `options 必须恰好 3 个`、`.slice(0,3)`、`options.length < 2` 兜底 | ⚠️ 保留 | 用户要求固定 3 个，保留 |
| 12 | `electron/main.ts` | affinity 范围提示「-5 到 +5」 | ⚠️ 保留 | 仅提示文案 |
| 13 | `electron/main.ts` | `validMoods` 列表 | ✅ 已改为 `[...MOODS]` | 不再与枚举脱节 |

## 三、自动触发（前端，主窗/小窗）

| # | 位置 | 硬编码内容 | 状态 | 说明 |
|---|------|-----------|------|------|
| 14 | 原 `ChatWindow.tsx` / `MiniChat.tsx` | 冷却 `25000`、概率 `0.55`（两处相同数值） | ✅ 已抽取 | 提到 `src/eventThemes.ts` 的 `EVENT_COOLDOWN_MS` / `EVENT_TRIGGER_THRESHOLD`，消除漂移 |
| 15 | `ChatWindow.tsx` / `MiniChat.tsx` | `EVENT_THEMES` 主题列表（两处完全相同） | ✅ 已抽取 | 提到 `src/eventThemes.ts` 共享 |

## 四、本次新增的可调设置（已落地）
- `settings.eventMoodImpact`（事件影响心情程度，0~1，默认 1）
- `settings.dialogueMoodImpact`（对话影响心情程度，0~1，默认 1）
- `settings.moodJudgeCooldownMs`（心情判定冷却，默认 20000ms）
- `settings.moodJudgeHistory`（心情判定回顾轮数，默认 10）
- `settings.eventNegAffinity`（低好感冲突阈值，默认 30）
- `settings.eventPosAffinity`（高好感甜蜜阈值，默认 70）
- `settings.eventHistory`（事件参考上下文条数，默认 12）
- `settings.eventMaxTokens`（事件生成长度上限，默认 700）

## 五、后续仍可继续提取（非必须）
1. 自动触发概率 / 冷却（`EVENT_TRIGGER_THRESHOLD` / `EVENT_COOLDOWN_MS`）做成可调滑块。
2. `electron/ai.ts` 思维链 `partialTagHold` 暂扣字符数——属流式解析技术细节，非业务硬编码，建议保留（已加注释说明）。
3. 聊天主上下文窗口 `slice(-N)`（如 -16/-24/-14 等）属模型上下文技术参数，通常不需用户调，保留。

## 六、音效系统（v1beta-a20 新增）

| # | 位置 | 硬编码内容 | 状态 | 说明 |
|---|------|-----------|------|------|
| 16 | `src/utils/sound.ts` `BUILTIN` | 7 个 mp3 文件名与相对路径 `sounds/xxx.mp3`（error 1 个、click 1 个、notification 5 个） | ⚠️ 保留 | 每个场景对应的音频文件，集中在此常量维护；**相对路径**以同时兼容生产（`loadFile` file://）与开发（vite http://）环境 |
| 17 | `src/utils/sound.ts` `CACHE_TTL = 3000` | 设置缓存有效期 3000ms | ⚠️ 保留 | 播放前读取 `sound.enabled/volume/custom` 的缓存时长，避免每次播放都 IPC 读设置 |
| 18 | `src/types.ts` `DEFAULT_SETTINGS.sound` | `{ enabled: true, volume: 0.7, custom: {error:null,click:null,notification:null} }` | ⚠️ 保留 | 音效默认开启、默认音量 70%、无自定义；用户可在「设置 → 音效」修改 |
| 19 | `src/utils/sound.ts` 通知随机 | `notification` 5 个文件随机选 1 个 | ⚠️ 保留 | `Math.floor(Math.random()*files.length)` 避免提示音单调 |
| 20 | `electron/main.ts` `registerSchemesAsPrivileged` / `protocol.registerFileProtocol('nysound')` | 自定义音效协议 scheme 名 `nysound`、映射到 `app.getPath('userData')/custom-sounds` | ⚠️ 保留 | 用户自定义音效文件存放目录与协议名；渲染进程以 `nysound://<filename>` 播放 |
| 21 | `electron/main.ts` `sound:setCustom` | 目标文件名规则：`snd-<error|click|notification>.ext` 与 `snd-role-<roleId>.ext`（ext ∈ mp3/wav） | ⚠️ 保留 | 复制到 `custom-sounds/` 后的稳定文件名；key 非法字符被替换为 `_` |
| 22 | `electron/main.ts` `sound:pick` | 文件选择过滤器仅允许 `mp3` / `wav` | ⚠️ 保留 | 自定义音效仅支持这两种格式 |
| 23 | `electron/main.ts` 三个 `BrowserWindow` `webPreferences.autoplayPolicy` | `'no-user-gesture-required'` | ⚠️ 保留 | 允许后台通知卡片、报错弹窗等无手势触发的音效自动播放 |
| 24 | `src/types.ts` `Role.soundPath` | 角色自定义消息音效文件名（存 `nysound://` 对应的文件名；null/空=沿用全局） | ⚠️ 保留 | 仅影响该角色的消息提示音，不作用于报错/点击音效 |

## 七、v2.3.2 新增（错误气泡 / 输入框外观 / 对比评测）

| # | 位置 | 硬编码内容 | 状态 | 说明 |
|---|------|-----------|------|------|
| 25 | `src/components/ErrorBubble.tsx` | 错误气泡自动消失 `15000`ms（15 秒） | ⚠️ 保留 | 用户要求「15 秒自动消失」，与视频气泡 8s 分开维护 |
| 26 | `src/components/ErrorBubble.tsx` | 详情错误码截断 `slice(0, 240)` 字符 | ⚠️ 保留 | 仅展示错误详情摘要，完整内容在错误日志 |
| 27 | `electron/ai.ts` `httpStatusToCode` | 状态码→错误码映射（401/403→auth、404→notFound、429→rateLimit、400→badRequest、408→timeout、5xx→serverError） | ⚠️ 保留 | 软件识别错误码的语义映射，集中维护 |
| 28 | `src/utils/errorDiagnosis.ts` `DIAGNOSES` | 错误文本→原因/解决办法（双语）正则规则集 | ⚠️ 保留 | 复用既有诊断引擎，新增规则只需追加一条 |
| 29 | `electron/main.ts` `compare:start` | 评测输入单条截断 `slice(0, 1500)`；互评跳过自身输出 | ⚠️ 保留 | 控制评测 token 成本 |
| 30 | `src/styles/index.css` `.chat-input` | 输入框默认背景 `#f2f3f5` / 文字 `#1f2329` | ⚠️ 保留 | 浅灰底深字，可被设置中的「输入框外观」覆盖 |

