# 念语 (Nianyu) 版本更新记录

> 最新发版：**v2.3.11**（2026-09-05）

## v2.3.11 (2026-09-05)

### 新增：主动消息「随机时间」触发模式（可调范围 1 秒 ~ 24 小时）

- **背景**：此前主动消息只支持固定静默间隔（5 分钟 ~ 5 小时离散选项）。新增「随机时间」模式：每次触发前在 **[最短, 最长]** 秒内随机抽取本次静默时长，让主动开口的时刻更自然。
- **设置入口**：设置 → 主动消息 → 新增「触发时机」切换（**固定间隔 / 随机时间**）+ 随机范围两个输入框（最短/最长，单位秒）。**硬编码边界**：可调范围钳制 `1 ~ 86400` 秒（1 秒 ~ 24 小时），且自动保证最短 ≤ 最长；默认范围 60 ~ 1800 秒。
- **调度实现**：主进程调度器（`electron/main.ts`）按聊天维度维护「当前抽中间隔」，触发一条主动消息后**立即重抽**下一次间隔并随 `idle:activity` 广播下发（带 `intervalMs`），主窗/小窗倒计时据此显示，与实际触发时刻保持一致。固定模式行为完全不变。
- 涉及：`src/types.ts`（`idleTimingMode` / `idleRandomMinSec` / `idleRandomMaxSec`）、`electron/main.ts`、`src/components/Settings.tsx`、`src/components/ChatWindow.tsx` / `MiniChat.tsx`（倒计时取广播间隔）、`src/i18n/translations.ts`。

### 修复：未读消息状态不实时 / 已读残留

- **现象**：① 新消息来了不及时显示未读；② 已经看过消息，未读标记却一直在，像要重启软件才更新；③ 会话列表里完全看不出哪个聊天有未读。
- **根因与修复**（三层）：
  1. **水位线不同步**：主进程在「正盯着该聊天」时 AI 回复完成会直接推进已读水位线并广播 `chats:readUpdated`，但聊天窗口此前**没有订阅**该广播，本地水位线停留在旧值 → 已看到的消息一直挂着未读徽章，直到重开聊天。现已实时订阅同步（`src/ipc.ts` / `electron/preload.ts` 新增 `onChatsReadUpdated`）。
  2. **水位线永不建立/永不前移**：v2.3.9 起「打开聊天不再视为已读、仅用户手动滚动到底才标记」，导致内容不足一屏（没有滚动事件）的聊天水位线永远不建立（wm=0 时未读判定直接失效 → 新消息永远不显示未读），且程序自动滚动/键盘/拖动滚动条到底也不算已读。现改为：**首次打开聊天时建立水位线基线**（历史视为已读，此后新消息正常计未读）；**视口位于底部即视为已读**（不再限定滚动手势），打开有未读的聊天仍先定位到未读分隔线、保持未读直到滚到底——类 IM 语义不变。
  3. **侧栏无未读角标**：会话列表此前完全没有未读提示。新增主进程 `chats:unreadCounts` 按水位线统一统计（仅 AI/系统消息，用户自己发的与正打开的会话不计；从未打开过的会话不计，避免老数据升级后全量标红），侧栏每个会话行显示红底白字未读数角标（>99 显示 99+），随会话列表 1.5s 轮询刷新。
- 涉及：`electron/main.ts`、`electron/preload.ts`、`src/ipc.ts`、`src/components/ChatWindow.tsx`、`src/components/ChatList.tsx`、`src/styles/index.css`（`.chat-unread-badge`）。

### 新增：本地部署文字模型支持（Ollama / LM Studio / vLLM 等）

- **背景**：本地推理服务（Ollama、LM Studio、vLLM 等）均提供 OpenAI 兼容接口，但此前模型提供方列表没有本地预设，且非 `openai-compatible` 提供方强制要求 API Key。
- **改动**：
  - 模型提供方新增 **「本地模型」** 预设（`src/types.ts` `Provider='local'`）：默认 Base URL `http://localhost:11434/v1`（Ollama），默认上下文 8192；编辑器内给出 Ollama / LM Studio（`http://localhost:1234/v1`）/ vLLM（`http://localhost:8000/v1`）地址示例。
  - **API Key 可留空**：主进程 `noApiKey` 校验（`electron/ai.ts`）对 `local` 提供方豁免，本地服务无需鉴权即可直连；若本地服务自身设置了鉴权仍可填写 Key。
  - 走既有 OpenAI 兼容 `/chat/completions`（含流式）与 `/models` 拉取模型列表路径，联网搜索、思维链解析、QPS 限速等能力全部可用。**仅文字聊天模型**：TTS / ASR / 生图 / 生视频独立 API 不在本次范围。
- 涉及：`src/types.ts`、`electron/ai.ts`、`src/components/ModelEditor.tsx`、`src/i18n/translations.ts`。

### 验证

- `npm run build`（tsc 主进程 + vite 渲染）0 错误通过；渲染端 `tsc --noEmit` 仅存量类型告警（showToast 布尔误用等历史遗留，均不在本次改动行）。

### 修订：打开聊天即标记已读 + 自己的消息不计未读（v2.3.11 当日修订）

- **现象**：点进有未读的聊天时侧栏角标消失（这只是「当前会话不统计」的假象），切到别的聊天后角标又恢复；聊天气泡右上角的「● 未读」徽章与「以下 N 条新消息」分隔线同样残留——因为此前水位线只在「视口滚到底」时才前移，而打开有未读的聊天时视口停在未读分隔线处，水位线始终不动。
- **修复（语义变更）**：**打开/切换到某聊天（主窗与快捷小窗）即视为已读**，立即把该聊天水位线推进到当前最新消息（`ChatWindow` 切换 effect 与 `MiniChat` 的 `loadMessages` 各调一次 `markChatRead`）。侧栏角标、聊天气泡未读徽章、未读分隔线随之**真正清除**，切走不再复活。未打开的聊天收到新消息仍正常亮角标；「视口到底即已读」（`maybeMarkRead`）保留作为兜底。
- **自己发的话不再显示已读未读**：聊天内未读判定（`unreadMsgs` 过滤与 `MessageRow` 的 `unread`）排除 `sender_type === 'user'`；侧栏统计此前已排除。

### 修复：Token 消耗统计失真（流式回复恒为 0）

- **现象**：统计页的全局 Token 消耗、人物板块 Token、每条回复下方的「消耗 N tokens」对流式回复**恒为 0 或严重偏低**。
- **根因**（两处）：
  1. 主进程流式路径 `streamOne` 的 `finalizeRole` 落库 `token_used: 0`，拿到手的 `res.promptTokens + res.completionTokens` 被直接丢弃（非流式路径落库正确）。
  2. `streamAI` 返回值中 `promptTokens` 恒为 0——流式响应几乎不返回 usage，旧代码只对 completion 做字符估算、**漏了 prompt**；模型调用量排名（`recordModelUsage`）因此只统计到一半。
- **修复**：
  1. `finalizeRole` 增加 `tokens` 参数并如实落库 `token_used`（正常完成 / 中断保留部分内容两条分支均传入），全局统计、人物统计、单条消息显示随之恢复准确。
  2. 流式请求体新增 `stream_options: { include_usage: true }`，支持的服务端（OpenAI / DeepSeek / vLLM 等）会返回**真实 usage**；不支持的（如 Ollama）忽略该字段，走字符估算兜底（约 2 字符 ≈ 1 token，prompt 按请求体全文、completion 按回复正文）。
- 群聊自动接话 / 主动消息路径此前已正确统计，未改动。

### 修复：聊天界面崩溃 Cannot access 'be' before initialization（v2.3.11 当日热修）

- **现象**：打开任意聊天即白屏报 `Something went wrong / Cannot access 'be' before initialization`（生产压缩后变量名；开发态为 `Cannot access 'isUser' before initialization`）。
- **根因**：上一节「自己发的话不计未读」改动中，`MessageRow` 的 `const unread = !isUser && ...`（render 期同步求值）引用了**其后约 90 行**才用 `const` 声明的 `isUser`（位于 system 消息提前 return 之后），每次渲染任一消息行都命中 TDZ。
- **修复**：把 `const isUser = msg.sender_type === 'user';` 上移到 `unread` 之前，删除后方的重复声明（`ChatWindow.tsx` `MessageRow`）。
- 与 v2.3.10 修复的 RoleEditor TDZ 同类：**render 体内同步执行的代码引用其后声明的 const 即 TDZ**，压缩后变量名变单/双字母。已在声明处加注释警示。

### 热修：聊天气泡未读徽章「切走再回来又出现」（v2.3.11 二次修订）

- **现象**：点进聊天后气泡右上角「● 未读」徽章消失，切到别的聊天再回来徽章又出现——期间没有任何新消息。
- **根因**：水位线状态存在**两类「旧值后到覆盖新值」的写入路径**——① 切换聊天时先发出的 `getSettings` 旧快照，可能在 `markChatRead` 的最新水位线广播之后才返回，`load` 里直接 `set` 把已读进度打回去；② 组件里还残留一个对同一通道 `chats:readUpdated` 的旧监听器裸 `set`（与新监听器双重写入）。任一后到即回退，徽章复活。
- **修复**：ChatWindow 水位线改为**「只前进不后退」**——按聊天 key 记录已应用的最大水位线（`applyWm`），任何更小的值（旧快照 / 迟到回调）一律忽略，且只作用于当前正查看的聊天；移除重复的裸 `set` 旧监听器；切换聊天时重置该聊天的门槛与本地状态。侧栏角标与悬浮球未读读的是主进程存储，本就不受此竞态影响。
- 涉及：`src/components/ChatWindow.tsx`（`wmFloorRef`/`applyWm`、load、切换 effect、移除 `onChatReadUpdated` 旧监听）。

### 移除：聊天气泡未读徽章 / 未读分隔线 / 侧栏未读角标（v2.3.11，按用户要求）

- **移除范围**：① 聊天气泡右上角「● 未读」徽章与消息行未读高亮（`msg-unread`）；② 「以下 N 条新消息」未读分隔线；③ 侧栏会话列表红色未读角标（`chats:unreadCounts` IPC 一并移除）。
- **保留**：桌面悬浮球的红色未读角标与后台消息提醒卡片（独立于水位线的 `unreadMap` 体系）不受影响；`readWatermark` 水位线仍在主进程后台记录（`chats:markRead` 接口保留），仅界面不再渲染任何未读视觉标记。
- **背景**：未读徽章经历多轮修正（打开即已读 → 竞态防护）后，用户明确要求直接移除聊天气泡与侧栏的全部已读/未读视觉标记。
- 涉及：`src/components/ChatWindow.tsx`、`src/components/ChatList.tsx`、`src/components/MiniChat.tsx`、`electron/main.ts`、`electron/preload.ts`、`src/ipc.ts`、`src/styles/index.css`。

## v2.3.10 (2026-09-02)

### 模型分组与标签（手动归类 + 快速筛选）

- **新增全局分组实体**：`settings.modelGroups` 保存 `{id,name,color}`，模型通过 `ModelConfig.groupIds` 引用，一个模型可属于多个分组。设置 → 模型管理 →「模型分组」区可新建 / 重命名 / 改色 / 删除分组；删除分组仅把模型移出、不删模型，并自动清理所有模型的孤儿 `groupIds` 引用（`electron/db.ts` 启动时也会兜底清理脏引用）。
- **新增自由标签**：模型编辑器内「标签」栏输入后回车即成标签，可点 × 删除，支持从已有标签联想。上限：每模型 12 个、单标签 16 字符；分组上限 20 个、单名 12 字符。
- **筛选联动**：模型管理筛选工具条新增「按分组」「按标签」两行胶囊（组内 OR，与能力维度 AND），含「未分组」虚拟项；模型卡片直接显示其分组色块与标签。
- **硬性上限（硬编码，改动需告知）**：`MODEL_GROUP_COLORS`（8 色调色板）、`MODEL_GROUP_NAME_MAX=12`、`MODEL_GROUP_MAX=20`、`MODEL_TAG_LEN_MAX=16`、`MODEL_TAG_MAX=12`，均定义在 `src/types.ts`。

### 新增：NSFW 内容输出检测（模型能力探针新项）

- 模型「检测能力」与「一键检测所有模型」新增 **NSFW 输出探测**：发送一段成人向描写请求，按返回正文语义判定（非状态码）。命中拒绝词库判「不支持」、命中露骨词 / 足量正文判「支持」、过短 / 异常判「无法判定」。结果写回 `ModelConfig.supportsNsfw`，仅作标记展示，不写进任何请求体、不绕过模型安全策略。
- **风险与限制**：探针属文本启发式，准确率不保证 100%（角色扮演可能绕过拒绝词库）；会向模型真实发送成人内容请求，可能触发厂商风控或留下审核记录。因此**一键全量检测弹探测项选择框，NSFW 默认不勾**并附风险提示，用户需手动勾选才纳入。
- 探针常量（硬编码）：探针提示词、`NSFW_REFUSAL_PATTERNS`（中英拒绝词库）、`NSFW_EXPLICIT_PATTERNS`（露骨词库）、`NSFW_PROBE_MAX_TOKENS=200`、最小正文长度 `NSFW_MIN_CONTENT_LEN=25`，定义在 `electron/ai.ts`。

### QPS 支持小数（v2.3.10，修复）

- **修复**：此前 QPS 输入框用 `Number(x)||0` 回写，输入 `0.` 会被吃掉小数点导致小数打不进；限速逻辑用计数制，`0.5` 被误判为约 1 次/分。
- **现改为**：输入框用字符串中间态 + 失焦归一化并加 `step=0.1`；限速改用「间隔制 `60000/qps` 毫秒 + 60 秒滑动窗口」双约束（qps=0.5 → 约 120 秒 1 次，qps=2 → 约 30 秒 1 次），既平滑突发也限制总量。改动位于 `ModelEditor.tsx`、`electron/main.ts`（`rateWaitMs`）。

### 修复：模型对比评分因单模型报错而整体丢失（v2.3.10）

- **现象**：对比多模型时，若中间某个模型输出报错，会导致其余成功模型的质量评分也丢失（如仅第一个模型有评分）。
- **根因**：原实现把所有输出拼成一条给评测模型一次性打分，靠 `index` 顺序回写；任一输出为空 / 评测返回的 JSON 被截断或漏项时，解析失败即整体丢弃全部评分。
- **修复**：改为**逐模型独立评分**（`electron/main.ts` 的 `compare:start`）。每个成功输出的模型各发一次评判请求，单条结果独立落库；任一模型报错或单次评判失败仅跳过该条并记入错误日志，不影响其他模型。只要至少一条成功评分，就隐藏全局「评分失败」横幅。
- 评测提示词相应改为单条输出打分（`{"score":88,"comment":"评语"}`），`max_tokens` 由 1024 降到 512，降低截断风险。

### 验证

- `npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 修复：设置页进入即崩溃 Minified React error #301（v2.3.10）

- **现象**：进入「设置」页直接白屏，报错 `Something went wrong / Minified React error #301`（生产构建吞掉真实错误，实为 "Element type is invalid"）。
- **根因**：`Settings.tsx` 在大量 hook 声明序列中间插入了一个「加载中」提前 `return`（`if (!draft) return <div>加载中</div>`）。`draft` 初始为 `null`（设置异步载入），首次渲染只跑少量 hook；当 `ThemeProvider` 异步回填 `draft` 后二次渲染会跑全部 hook，hook 数量不一致，触发 Rules of Hooks 崩溃（生产环境表现为 #301）。
- **修复**：把所有 hook（含原先落在 guard 之后的 4 个模型列表 state `asrModelList/ttsModelList/imgModelList/modelLoading`）一律上移到提前 `return` 之前；派生常量（`voice/imageGen/cursor` 等假设 `draft` 非空）保留在 guard 之后。彻底消除 hook 顺序随 `draft` 空/非空变化而错乱的问题。

### 修复：设置页渲染期误触发 Too many re-renders（v2.3.10）

- **现象**：修复 #301 后进入「设置」页仍白屏，报 `Too many re-renders. React limits the number of renders to prevent an infinite loop.`。#301 原本在挂载即崩溃、组件从未完整渲染，因此该 bug 此前被掩盖，用户未单独报告。
- **根因**：`Settings.tsx` 中三处筛选胶囊按钮把 `toggleSet` 直接作为事件处理器值、未用箭头函数包裹：`onClick={toggleSet(setGroupFilter)(g.id)}`（1797 行）、`onClick={toggleSet(setGroupFilter)('__ungrouped__')}`（1815 行）、`onClick={toggleSet(setTagFilter)(tg)}`（1840 行）。`toggleSet(setX)` 返回函数后立刻以 `(key)` 调用，在**渲染期**即执行内部 `setter(prev => ...)`，触发渲染期 setState → React 渲染计数保护抛错。
- **验证方法**：用 `react-dom/server` 的 `renderToStaticMarkup` 对 Settings 完整体做纯渲染（无 effect、无并发、无 act），若报 Too many re-renders 即证明渲染体存在渲染期 setState；修复前 SSR 抛错，修复后 SSR 成功输出 44977 字符 HTML，排除 jsdom + act 并发环境的假阳性干扰。
- **修复**：三处统一改为箭头函数包裹 `onClick={() => toggleSet(setX)(key)}`，将执行推迟到点击事件。
- **验证**：`npm run build` 0 错通过。

### 新增：模型对比「正在评分」横幅（v2.3.10）

- **背景**：逐模型独立评分阶段（见上文「修复：模型对比评分因单模型报错而整体丢失」），评分在主进程顺序进行，前端此前无任何进行中提示，用户无法判断评分是否在运行。
- **改动**：
  - 主进程 `electron/main.ts` 的 `compare:start` 逐模型评分循环内，每个模型开始评判前新增 `broadcast('compare:judging', { compareId, modelId, modelName })`。
  - 渲染进程 `src/ipc.ts` 的 `NianyuAPI` 接口与实现新增 `onCompareJudging`；`electron/preload.ts` 注册 `compare:judging` 通道转发。
  - `src/components/ModelCompare.tsx` 订阅 `onCompareJudging`，新增 `judging` 状态，`compare:judged` / `compare:done` 时清零；对比面板在运行区下方渲染「正在评分：{模型名}」蓝色横幅（含打字指示器动画），评分结束自动收起。
  - 文案：中文 `compare.judging = '正在评分：{name}'`，英文 `compare.judging = 'Judging: {name}'`（`src/i18n/translations.ts`）。

### 修复：评测质量评判结果前端不显示（v2.3.10）

- **现象**：对比多模型时，底部蓝色「正在评分：{模型名}」横幅出现几秒后消失，但每个模型输出框下方**没有任何质量评分 / 评语**（之前已有逐模型评分逻辑，却只见横幅不见结果）。
- **根因**：评测模型常返回**非严格 JSON**（如外层带 Markdown 代码块围栏 ```json、前后有说明文字、或字段写成 `score = 88` 形式）。原解析仅靠 `parseFirstJson` 抽第一个 JSON 对象，遇到上述格式即返回 `null`，导致该模型 `judgments[modelId]` 未被写入；前端 `judge` 为 `undefined`，整块评分区域不渲染，仅横幅随 `compare:done` 收起。
- **修复（双保险）**：
  1. 主进程 `electron/main.ts` 新增 `extractJudge(text)`，先走 `parseFirstJson`（兼容 `{score,comment}` 或 `{scores:[...]}` 取首条）；失败再用正则兜底抽取 `"score":88` / `score = 88`（0~100 钳制）与就近 `comment` 字符串。任一路径命中即落库 `judgments[modelId] = { score, comment }`；全失败才记 `judgeError` 日志（不再整体丢弃）。
  2. 前端 `src/components/ModelCompare.tsx`：评判模型已选定、但某模型无有效评分时，渲染「未评分（评分模型未返回有效结果）」橙色提示（`compare.noScore`），替代此前整块留白，让用户明确看到「该模型未拿到分」而非「什么都没发生」。
- 验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 修复：悬浮球悬停改为快捷聊天面板（v2.3.10）

- **现象**：此前鼠标悬停悬浮球弹出的是**未读消息面板**；用户希望改为弹出**快捷选择聊天面板**，点击面板内聊天直接呼出对应小窗。
- **改动**：
  - 渲染端 `src/floating-ball.ts`：面板数据改为 `api.getChatList()` 返回的全部**单聊（💬）+ 群聊（👥）**（含最近一条消息预览），每次悬停展开先 `fetchChats()` 刷新；移开自动收起，列表超出一屏可滚轮下拉。移除原未读清单渲染逻辑，未读仅保留为悬浮球**角标**（后台新消息计数）。
  - 主进程 `electron/main.ts` 新增 `ipcMain.on('ball:open-chat', ...)`：收到点击的 `{chatType, chatId}` 后先 `clearUnreadForChat` 清该会话未读，再置 `pendingMiniChat` 并 `showMiniWindow()`，**直接呼出对应小窗聊天界面**（`MiniChat`）。原 `electron/floatingBall.ts` 中跳主窗的 `ballOpenChat` 处理被移除，统一由主进程走小窗路径。
  - 预加载 `electron/preload.ts` 与 `src/ipc.ts` 的 `NianyuAPI` 接口新增 `ballOpenChat(chat)` 通道，供渲染端点击调用。

### 新增：统计人物板块展示关系类别（v2.3.10）

- **背景**：统计界面「人物」板块此前只显示**关系值（数值）**，用户看不到该人物与自己的**具体关系是什么**（如「恋人 / 好友 / 同事」）。
- **改动**：`src/components/StatsView.tsx` 人物行在关系值后追加关系类别标签，取自 `src/types.ts` 的 `RELATION_LABELS[normalizeRelation(relation)]`（中文如「恋人」「朋友」「熟人」「陌生」等，缺失时回退原始值）。`normalizeRelation` 统一把旧枚举 / 自由文本归一为受控关系类别，避免脏数据导致标签空白。

### 新增：统计聊天模型调用量排名（v2.3.10）

- **背景**：此前统计界面无「哪个模型被调得最多」的视角，排查成本与高频模型无据可依。
- **改动**：
  - 主进程 `electron/db.ts` 的 `Store` 新增 `modelUsage: Record<modelId, { name, tokens, calls }>`；聊天回复四类路径（单聊 / 群聊成员 `streamOne`、群聊导演选人、空闲主动消息）在每次 `queryAI`/`streamAI` 返回后调用 `dm.recordModelUsage(id, name, promptTokens, completionTokens)`，`tokens` 累加 `prompt+completion`、`calls` +1。
  - 新增 `dm.getModelStats()`：仅统计**当前仍启用**的模型（`settings.models` 中 `enabled` 者），按 `tokens` 从高到低排序，返回 `{ modelId, name, tokens, calls }[]`；模型被更换或删除（不再启用）即自动退出排名。
  - IPC：`electron/main.ts` 注册 `stats:modelUsage` → `dm.getModelStats()`；`src/ipc.ts` / `electron/preload.ts` 暴露 `getModelStats()`；`src/components/StatsView.tsx` 统计界面新增「聊天模型调用量排名」区块，每项显示 token 总量与「调用 N 次」，并按 token 占比渲染蓝紫渐变进度条，无记录时显示空态。

### 修复：卸载程序「删除全部数据」勾选无效（v2.3.10）

- **现象**：Windows 卸载念语时，勾选「删除所有使用数据（不可恢复）」，卸载完成后 `%APPDATA%\nianyu-client`、`%LOCALAPPDATA%\nianyu-client`、`文档\念语数据` 及自定义数据目录**依旧保留**，勾选如同虚设（v2.3.0/v2.3.2 仅修了进程名与漏删默认目录，未触及此结构性失效）。
- **根因**：`build/installer.nsh` 经 `package.json` 的 `nsis.include` 在 electron-builder 内置卸载脚本**末尾**被 include。原删除逻辑写在独立段 `Section "-postuninstall"`，该段位于内置卸载段 `Section "un.${UNINSTALL_SECTION_NAME}"`（即 instfiles 页）**内部**，在自定义 `UninstPage`（数据清除确认页）**之前**执行；而自定义确认页排在脚本最末，段执行时复选框 `$deleteAppDataChecked` 状态尚未就绪（恒为 0），故 `IfFileExists`/删除分支永远不进。
- **修复**：将删除逻辑（taskkill 念语.exe、RMDir 上述三个数据目录、自定义数据路径逐行读删）整体移入 `Function .onUninstSuccess`——该回调在**全部卸载页（含自定义确认页）执行完毕后**才触发，此时复选框状态已确定；保留空 `Section "-postuninstall" SectionEnd` 维持脚本结构不被破坏。彻底修复勾选删除无效。
- 验证：重新构建安装包、勾选删除并卸载，数据目录被正确清除。

### 修复：新建数字人崩溃 Cannot access 'I' before initialization（v2.3.10）

- **现象**：在「数字人」界面点击「新建数字人」后，软件弹出 `Something went wrong / Cannot access 'I' before initialization`（生产构建压缩后变量名变为单字母 `I`；开发态原信息为 `Cannot access 'selectedModel' before initialization`）。
- **根因**：`src/components/RoleEditor.tsx` 在 render 体内求值顺序错误——第 83 行 `const currentModel = models.find((m) => m.id === selectedModel);` 在 **render 期同步执行 `find` 回调**，引用了第 95 行（其后）才用 `const` 声明的 `selectedModel`。首帧 `models` 为空数组，`find` 回调零次执行故不崩；当 `useEffect` 异步拉取模型列表后重渲染、`models` 非空时，`find` 同步执行回调即命中 `selectedModel` 的 TDZ（暂时性死区），抛出 `ReferenceError`。这是典型的变量声明顺序引发的 TDZ，压缩后变量名 `selectedModel` → `I`。
- **修复**：将 `enabledModels` 与 `selectedModel` 两行声明整体上移到 `currentModel` 之前，确保 `selectedModel` 在被 `find` 回调引用前已完成初始化（`RoleEditor.tsx` line 83-95 区）。
- **复现与验证**：用 vite+jsdom 客户端挂载（`createRoot` + `act`，非 SSR，因为 SSR 不跑 effect、无法复现 effect 拉取数据后的重渲染）对 `RoleEditor` 做无头复现，修复前稳定输出 `BOUNDARY_ERROR: Cannot access 'selectedModel' before initialization`、组件栈指向 `RoleEditor`；修复后同脚本输出 `RENDER_DONE no crash`。随后 `npm run build`（tsc 主进程 + vite 渲染）0 错误通过。
- **注**：该复现脚本为临时验证文件，验证通过后已删除。

### 修复：流式回复切换聊天后生成重复气泡 / 思考过程块异常展开（v2.3.10）

- **现象 1**：模型尚未开始输出时切换到别的聊天，再切回原聊天，会多出一个新的「正在回复」占位气泡；最终可能同时出现「占位气泡 + 已完成的真实回复」。
- **根因 1**：`src/components/ChatWindow.tsx` 的 `onStreamChunk` 处理存在「兜底新建占位」分支——当本地 `streamingMsgs` 中没有对应 `streamId` 时，会直接用当前到达的 chunk 创建一个新的占位气泡。切换聊天时 effect 会清空 `streamingMsgs`，后续切回若后端流仍在继续，下一个 `stream:chunk` 到达时因找不到已有占位，就会凭空创建新气泡。
- **修复 1**：移除 `onChunk` 的兜底新建占位逻辑，改为「无占位则忽略该 chunk」。流式占位统一由 `stream:start` 创建；错过 start 的中途 chunk 不再生成孤立/不完整的气泡，最终完整消息仍由 `stream:done` 落库后正常显示。`src/components/MiniChat.tsx` 此前已采用相同的「无占位则忽略」策略，保持一致。
- **现象 2**：AI 回复的思考过程（reasoning）块在流式输出期间被强制展开后，即便内容已经开始输出、且用户设置默认折叠思考过程，该块仍保持展开不收回。
- **根因 2**：`ReasoningBlock` 组件用 `useState(defaultOpen)` 记录展开状态，但 `defaultOpen` 在流式进行中会随 `streaming && !msg.content` 从 `true` 变为 `false`；组件未响应 prop 变化，导致展开状态被「锁死」在最初的 `true`。
- **修复 2**：`src/components/ReasoningBlock.tsx` 增加 `useEffect` 在未收到用户手动切换时跟随 `defaultOpen` 变化；同时记录用户是否手动点击过箭头，手动操作后不再被自动回弹覆盖。
- **相关加固**：`MessageRow` 中对 `msg.reasoning` 的判断改为 `msg.reasoning?.trim()`，避免仅含空白字符的 reasoning 仍渲染出空思维链块；`typing` 判定也同步使用 `trim()`，防止空白 reasoning 让加载动画不显示。
- **验证**：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 优化：毛玻璃主题悬浮球快捷聊天面板背景不透明度（v2.3.10）

- **现象**：毛玻璃主题下，悬浮球快捷聊天面板（及右键菜单）背景半透明，`backdrop-filter` 模糊仍会透出下层窗口文字，导致面板内文字与背景文字叠在一起、难以辨认。
- **修复**：`src/floating-ball.ts` 的 glass 主题覆写中，将 `.fb-panel` 背景由 `rgba(18,16,38,0.62)` 提高到 `rgba(18,16,38,0.88)`、`.fb-ctx` 右键菜单由 `rgba(18,16,38,0.72)` 提高到 `rgba(18,16,38,0.88)`，显著减少透出、文字不再与背景重叠。边框与主色描边保持不变。

### 移除：未读消息蓝色背景与头像侧蓝色竖条（v2.3.10）

- **现象**：AI / 用户未读消息的气泡周围有一圈淡蓝色背景，头像一侧（AI 消息在左、用户消息在右，因 `flex-direction:row-reverse`）有一条蓝色竖条（`box-shadow`），干扰阅读。
- **根因**：v2.3.9 在 `src/styles/index.css` 的 `.msg-row.msg-unread` 上加了三项视觉标记：`box-shadow:-3px 0 0 0 var(--color-primary)`（左侧竖条）、`border-radius:4px`、`background:color-mix(in srgb, var(--color-primary) 9%, transparent)`（气泡淡蓝背景）。群聊场景下多条未读同时存在时，整片消息区都是淡蓝色，干扰严重。
- **修复**：清空 `.msg-row.msg-unread` 的视觉样式（保留 `msg-unread` 类用于已读水位线逻辑判断）；`.unread-badge`（右上「● 未读」徽章）与 `.unread-divider`（「以下 N 条新消息」分隔线）按需仍正常显示。
- **验证**：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

## v2.3.9 (2026-09-01)

### QPS 限速覆盖聊天类 AI 功能（按各模型自身 qps；生图/生视频/ASR/TTS 不受限）

- **背景**：v2.3.7 的核查结论显示，主回复路径（`runOne` / `streamOne`）与 AI 补全提示词已纳入 QPS，但其余 11 类走独立 `queryAI`、不经 `rateWaitMs` / `rateMark`，不共享默认模型的 QPS 配额，高频后台调用可能触发服务端 429 限流。
- **改动**：为全部独立 AI 调用点补上 `rateWaitMs(cfg.id) → sleep → rateMark(cfg.id)` 守卫，与既有主回复路径完全一致：
  - 心情判定 `judgeMood`、关系/朋友圈判定 `judgeRelationship` / `judgeAndPostMoments`、场景生图判定 `triggerSceneImage`、翻译 `translateText`（此前已落地）
  - 群聊导演选人 `pickNextSpeaker`、群聊接话 `handleGroupContinue`（Anthropic 非流式分支 + 流式分支）、主动消息 `handleProactive`、随机事件 `handleRandomEvent`、记忆提炼、模型对比（每模型并发 + 评测模型）
  - 角色简介 AI 补全 `roles:aiComplete`（在 `aiCompleteRole` 调用前守卫）
- **限速按各模型自身 `qps`**：所有聊天类调用用 `rateWaitMs(cfg.id)`，即按该调用所用模型自己的 `qps` 字段限速（每个模型独立计数、互不影响），并非统一按默认模型限速。
- **生图 / 生视频 / ASR / TTS 不受 QPS 约束**：这四类走各自独立 API（独立 `baseUrl/apiKey`，非 `ModelConfig`，无 `qps` 字段），不经过 `queryAI`/`streamAI`，故完全不受 QPS 限速。`runVideoGenJob` / `runMomentVideoJob` 不挂任何 QPS 守卫。（注：v2.3.8 初版曾误将生视频计入默认模型 QPS 预算，已修正移除。）
- **AI 补全提示词维持原策略**：超限速直接返回 `rateLimited`（不消耗额度、前端提示「暂时用不了」），成功才 `rateMark`，与用户预期一致。
- **限速逻辑仍集中在主进程**：`rateWaitMs` / `rateMark` / `sleep` 为 `main.ts` 模块级函数，所有调用点统一复用，未下沉到 `ai.ts`（避免与同样调用 `queryAI` 的 `aiCompleteRole` 重复守卫）。
- 验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 新增：模型能力探针（真实请求探测，非启发式）

- **功能**：模型编辑器新增 **「检测能力」** 按钮，向该模型发送极小探针请求，依据 HTTP 状态码**真实判定**能力，而非按模型名关键字猜测：
  - **视觉（图片输入）**：发一条带 `image_url` 的图片消息，服务端接受（2xx）= 支持、明确拒绝（400/415/422）= 不支持。
  - **工具调用**：带一个空 `tools` + `tool_choice:auto`，接受 = 支持。
  - **JSON 模式**：带 `response_format={type:'json_object'}`，接受 = 支持。
  - **上下文窗口**：GET `/models` 读取该模型 `context_window`（best-effort，多数 OpenAI 兼容网关不返回此字段）。
  - 无法判定（401/429/5xx/网络异常）时返回 `null`，**保留用户手动标记原值、不覆盖**。
  - **Anthropic 系**：仅做连通性 + 上下文窗口探测，视觉/工具/JSON 三项标注「未探测」（格式差异大，避免误判），仍可由用户手动标记。
- **结果落库与展示**：探查结果写回 `ModelConfig.supportsImages/supportsTools/supportsJson/maxContext` 并记录 `lastDetectedAt`，模型编辑器以徽章（✓/✕/—）展示，可手动覆盖。设置页「记忆与模型」区新增 **「一键检测所有模型能力」** 按钮，逐个探测全部模型后统一落库并弹结果摘要。
- **实现**：`electron/ai.ts` 新增 `detectCapabilities`（+ `postChatRaw` / `capFromStatus` / `fetchModelContextWindow` 探针辅助）；`main.ts` 新增 IPC `models:detect(id)` / `models:detectAll()`，仅覆盖已确认项、不覆盖手动标记；`preload.ts` + `src/ipc.ts` 补 `detectModel` / `detectAllModels`；`ModelEditor.tsx` 加按钮与徽章、`Settings.tsx` 加一键检测；i18n 中英新增 `model.detect*` / `model.cap*` / `settings.detectAll*`。
- 验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 新增：长记忆（per-chat 独立开关）+ 手动让 AI 总结记忆

- **功能**：聊天「其他操作（⋯）」新增 **「长记忆」** 独立开关（key=`single:roleId` / `group:groupId`，每聊天独立）。开启后，同一菜单出现 **「✨ AI 总结记忆」** 按钮，点击后调用**默认模型**（`settings.defaultModel`，受默认模型 QPS 限速）总结该聊天最近对话、去重写入记忆。
- **与自动记忆提炼的关系**：`enableAutoMemory`（全局自动提炼，已有）与 `longMemory`（per-chat 长记忆手动总结）是两套独立开关。长记忆按钮仅在 `longMemory[chatKey]=true` 时可见/可用；总结逻辑复用 `doExtractMemories` 核心（观察者模式写入开关对两者都生效），但不受 `enableAutoMemory` / 主动消息开关门控。
- **实现**：`types.ts` 的 `AppSettings` 新增 `longMemory: Record<string,boolean>`；`main.ts` 新增 IPC `memories:summarize(chatType, chatId)`（门控 `longMemory`、受默认模型 QPS、写聊天级记忆），并把原有 `extractMemories` 的提炼核心抽取为 `doExtractMemories`（自动路径保留 `enableAutoMemory` + 主动消息门控）；`preload.ts` + `src/ipc.ts` 补 `summarizeMemories`；`ChatWindow.tsx` 的 more-dropdown 加开关与按钮；i18n 新增 `chat.longMemory*` / `chat.summarizeMemory*` / `chat.summarizing` / `chat.summarizeFail`。
- 验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 新增（v2.3.9 修订）：长记忆语义收敛 — 总开关 + 10 轮自动提取

将长记忆从「仅门控手动按钮」升级为「该聊天长期记忆总开关」：

- **关闭长记忆 = 从零开始**：`main.ts` 记忆注入处新增 `settings.longMemory[chatKey]` 门控，关闭的聊天不向模型回灌任何已存记忆、不做任何自动提炼、也不显示手动按钮；已存记忆原样保留，重开即恢复（非破坏性）。
- **自动提炼：双开关 + 每 10 轮**：`extractMemories` 自动路径在原有 `enableAutoMemory` 判断后追加 `longMemory[chatKey]` 门控（两者都开才自动），并累计 `autoMemRoundCount[chatKey]`（新增 `AppSettings` 字段，持久化于 settings JSON），满 10 条用户消息才提炼并归零；主动消息触发的轮次不计入计数器。
- **前端计数显示**：长记忆开关下方实时显示「已累计 N/10 轮」（订阅 `settings:changed` 同步 `autoMemRoundCount`），新增 i18n `chat.longMemoryAutoHint`（中英）。
- **记忆管理用户主导**：资源库 → 记忆 增/改/删 + 聊天「记住」按钮保持不变，用户主导记忆内容与生命周期。
- **实现**：`types.ts` 的 `AppSettings` 新增 `autoMemRoundCount: Record<string,number>`（默认 `{}`）；`main.ts` 的 `extractMemories` 改为双开关门控 + 10 轮计数（`AUTO_MEM_ROUNDS=10` 常量），记忆注入处加长记忆门控；`ChatWindow.tsx` 新增 `autoMemRound` state 并在 `load()` / `onSettingsChanged` 同步，more-dropdown 加计数提示；`translations.ts` 补 `chat.longMemoryAutoHint`。`MiniChat.tsx` 无长记忆 UI，仅复用 `api.extractMemories` 自动路径（逻辑已上移主进程）。
- 验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 新增：已读未读显示 + 类 IM 已读回执

- **功能（满足「未读消息加视觉标记」+「类 IM 已读回执」两项）**：
  1. **未读视觉标记**：每条消息 `id` 大于该聊天的已读水位线 `readWatermark[chatKey]` 时，气泡左侧加高亮竖条 + 右上角「● 未读」徽章。
  2. **类 IM 已读回执**：切换/打开某聊天（`api.setActiveChat` → 主进程 `app:active-chat`）即把该聊天水位线前移到最新消息 id（视为已读）；当前前台聊天收到新 AI 消息也实时前移水位线（避免「正看着却标未读」）；非前台聊天的未读保持，直到被打开。水位线变更经 `chats:readUpdated` 广播，前端 `ChatWindow` 实时同步。
- **实现**：`types.ts` 的 `AppSettings` 新增 `readWatermark: Record<string,number>`；`main.ts` 新增 `markChatRead(chatType, chatId, lastId?)` 模块级函数 + IPC `chats:markRead`（在 `app:active-chat` 与 `generateAIResponses` 末段调用）；`preload.ts` + `src/ipc.ts` 补 `markChatRead` / `onChatReadUpdated`；`ChatWindow.tsx` 的 `MessageRow` 加 `readWatermark` prop 与未读标记、订阅 `onChatReadUpdated`；`index.css` 加 `.msg-unread` / `.unread-badge`；i18n 新增 `chat.unread` / `chat.unreadTip`。
- 验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 新增：模型能力筛选（设置 · 模型管理）

- **功能**：模型管理卡片区上方新增 **「能力筛选」** 工具条，提供 视觉 / 工具 / JSON / 推理 四个能力胶囊按钮（多选，AND 逻辑：模型须同时具备所选能力才命中），右侧实时显示「匹配 N / 总数 个模型」；选中后模型卡片列表即时过滤，空结果提示「没有符合所选能力的模型」；「清除筛选」一键复位。
- **实现**：`Settings.tsx` 新增 `modelFilter: Set<string>` 状态 + `filteredModels` 派生（空集合=不过滤）；模型卡片 `map` 改用 `filteredModels`；i18n 中英新增 `settings.modelFilter` / `settings.filterClear` / `settings.filterMatch` / `settings.filterNoMatch`。

### 新增：模型选择器悬停能力提示（角色编辑器 · 绑定模型）

- **功能**：角色编辑器「绑定模型」下拉的**每个候选项悬停时弹出能力提示框**，列出该模型的 视觉 / 工具 / JSON / 推理 支持情况与上下文窗口；**选择器本体（关闭态）悬停**也通过原生 `title` 显示当前模型的能力摘要，便于快速判断模型能做什么。
- **实现**：`SelectMenu.tsx` 的 `SelectOption` 新增 `tooltip?: React.ReactNode`，面板项 `onMouseEnter` 测量定位后用 portal 渲染 `.select-menu-tip`（右侧空间不足自动翻转到左侧）；`RoleEditor.tsx` 的绑定模型 `SelectMenu` 为每个选项注入 `tooltip`（能力明细）并给触发器加 `title`（能力摘要）；`index.css` 加 `.select-menu-tip`。

### 修复：悬浮球未读消息面板不显示未读

- **根因**：`electron/floatingBall.ts` 的 `pushUnread` 对**手动回复**采用「主窗可见即不计未读」的过严语义，导致用户正常开着主窗对话时悬浮球永远收不到未读；渲染端 `src/floating-ball.ts` 的 `unreadItems/unreadCount/panelOpen/dragging` 声明位于 `onData` 闭包引用之后（TDZ 风格脆弱写法）。
- **修复**：`pushUnread` 改为**统一类 IM 语义**——仅当用户「正盯着该聊天本身」（主窗可见且为当前会话）时才视为已读，其余（主窗隐藏 / 在看别的聊天 / 手动回复 / 主动消息）均计入未读；移除已无用的 `fromProactive` 形参（同步更新 `main.ts` 两处调用点）。渲染端把上述四个状态声明上移到 `mount()` 顶部，消除声明顺序隐患。
- **实现**：`electron/floatingBall.ts` `pushUnread` 签名与语义；`main.ts` `sendStreamDone` / `pushMediaUnread` 调用点；`src/floating-ball.ts` 状态声明上移。

### 增强：聊天框类 IM 已读未读可见性

- **根因**：原实现在「打开聊天（`app:active-chat`）」与「前台聊天新消息」时立即把水位线前移到最新 id，导致**刚打开有未读的聊天就被瞬间清零**，用户根本看不到未读标记；且原未读仅一条 3px 竖条、徽章易被裁切，视觉过弱。
- **修复**：
  1. **解耦「打开」与「已读」**：`app:active-chat` 不再立即 `markChatRead`（悬浮球未读清除仍由 `setActiveChat` 负责），改由渲染端在**用户主动滚动到底部**（真正读完）后标记已读。
  2. **未读分隔线**：消息列表中「最后一条已读」与「第一条未读」之间插入 **「以下 N 条新消息」** 分隔线（类 IM「未读消息」），返回有未读的聊天时先定位到第一条未读消息。
  3. **视觉增强**：`.msg-unread` 加主色淡底 + 保留左侧高亮竖条，`.unread-badge` 加重加圆角防裁切；新增 `.unread-divider` 样式。
- **实现**：`main.ts` `app:active-chat` 移除 `markChatRead`；`ChatWindow.tsx` 新增 `readWatermarkRef/userScrolledRef/latestIdRef`，滚动到底部（用户主动）调 `api.markChatRead`，自动滚动定位到第一条未读，列表注入未读分隔线；`index.css` 增强未读样式；i18n 新增 `chat.unreadDivider`。
- 验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过（88 模块）。

## v2.3.7 (2026-08-30)

### 主动消息：改为主进程调度，托盘/后台不再停摆

- **问题**：主动消息由渲染进程 `setInterval` 每 3 秒自检触发。主界面关闭到托盘时窗口仅 `hide()`，渲染进程定时器被 Chromium 节流乃至冻结，计时停摆；只有重新打开窗口、渲染进程恢复后才补发，表现为「消息姗姗来迟」。单聊与群聊同样受影响。
- **修复**：触发逻辑下沉主进程。主进程已有全局权威计时基准 `idleState`，新增每 3 秒一次的调度器，依次校验「全局开关 → 该聊天单独开关 → 静默时长达标 → 无进行中的生成 → 该聊天有历史消息」，全部满足才调用 `handleProactive`。窗口隐藏、最小化、托盘常驻均不影响计时与触发。
- **渲染端收敛**：`ChatWindow.tsx` 与 `MiniChat.tsx` 移除各自的触发逻辑与 `proactiveBusyRef`，只保留倒计时显示与「进入聊天时初始化计时基准」，杜绝主进程与渲染端双触发。
- **当前聊天来源**：`app:active-chat` 回传的聊天同时作为调度目标；缺失时兜底取 `idleState` 中最近有活动记录的聊天。快捷小窗切换会话时也上报当前聊天。

### 群聊自动接话：新消息进入悬浮球未读清单

- **问题**：未读判定区分「手动回复」与「AI 自发消息」。自动接话落库的消息未打标记，被归入手动回复，于是「主窗可见 = 已读、不进未读」。开着自动接话切去别的聊天时，新回复完全不出现在未读面板。
- **修复**：`ChatMessage` 新增 `from_auto` 字段，`handleGroupContinue` 写入消息时置为 `true`；`sendStreamDone` 的未读判定改为 `from_proactive || from_auto`，与主动消息一样按「AI 自发」处理：用户正盯着该聊天才不计，切走或窗口隐藏都计入未读。手动回复与手动点「继续对话」的行为完全不变（后者因用户正盯着该聊天，仍不计未读）。
- **兼容性**：`from_auto` 不参与记忆控制，自动接话仍照常写入记忆（`from_proactive` 的记忆隔离逻辑未改动）。

### 后台消息卡片

主动消息与自动接话统一经 `sendStreamDone` 下发，主进程与渲染端各有一路卡片触发并带 1.5 秒去重，窗口隐藏时照常弹出 Steam 风格卡片。

### 未读判定修正：进入主界面不再清空全部未读

- **问题**：`showMainWindow` 与悬浮球 `ball:activate` 此前都会调用 `clearAllUnread()`，把全部未读一次性清空。用户只是呼出主窗口（双击托盘 / 点悬浮球 / 从最小化恢复），却未必打开的是那个有未读的聊天，于是托盘期间累积的主动消息、自动接话未读被悄悄抹掉，造成漏看。
- **修正**：移除上述两处的 `clearAllUnread()` 调用。未读只在「该聊天被真正打开查看」时清除（主窗 / 小窗 `app:active-chat` 回传 → 主进程 `setActiveChat` → `clearUnreadForChat`）；呼出主界面、从最小化／托盘恢复、点击悬浮球，都只显示窗口、不碰未读。其余会话未读继续留在角标与悬浮球面板，直到用户逐一打开对应聊天。
- 改动文件：`electron/main.ts`（`showMainWindow` 移除 `clearAllUnread` 调用与 import）、`electron/floatingBall.ts`（`ball:activate` 移除 `clearAllUnread` 调用）。`clearAllUnread` 函数定义保留、暂不删除（无调用方，不影响构建）。验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 新增：AI 自动补全提示词（生图 / 生视频弹窗）

- **功能**：生图弹窗与生视频弹窗（文本输入框）左下角新增 **「AI 补全提示词」** 按钮。先在输入框写一段粗略想法（如「一只猫」），点击后调用**默认模型**（`settings.defaultModel`，未设则取第一个启用模型）把想法扩展成一段完整可用的英文提示词，并**替换**回输入框，便于继续微调。生图补「图像提示词」（主体 / 环境 / 光影 / 风格），生视频补「视频提示词」（画面 / 运镜 / 氛围）。
- **受 QPS 限速约束（与用户预期一致）**：超默认模型的每分钟请求上限（模型管理里设置的 QPS）时，点击不消耗额度，返回 `rateLimited` 并提示「AI 补全暂时用不了（请求过于频繁，请稍后再试）」，稍后重试即可。
- **实现**：新增 IPC `prompts:autocomplete`（main.ts）→ preload `autocompletePrompt` → `src/ipc.ts` `NianyuAPI.autocompletePrompt`；主窗 `ChatWindow.tsx` 生图/生视频两处弹窗、小窗 `MiniChat.tsx` 生图弹窗均接入；i18n 新增 `chat.aiCompletePrompt` / `chat.aiCompleting` / `chat.aiCompleteRateLimited` / `chat.aiCompleteFailed`（中英文）。
- 验证：`npm run build`（tsc 主进程 + vite 渲染）0 错误通过。

### 修复：玻璃主题下「编辑模型配置」弹窗文字穿透重叠

- **问题**：模型管理里点「编辑」打开的弹窗用 `.modal`（非 `.modal-card`），玻璃主题下 `.modal` 背景为半透明白 `var(--color-panel)`（`rgba(255,255,255,0.22)`），背后是设置页 + `modal-mask`（`rgba(0,0,0,0.5)`）。半透导致设置页文字透过弹窗、与编辑页文字重叠，难以辨认。
- **修复**：在 `index.css` 的 `[data-theme='glass']` 不透明兜底选择器（原只含 `.modal-card` 等浮层）中补入 `.modal`，使玻璃主题下编辑模型弹窗统一换成高不透明深色底 `rgba(18,16,38,0.88)`，杜绝穿透。
- 验证：`npm run build` 0 错误通过。

### 核查：视频生成链路 + QPS 限速覆盖范围

- **视频生成链路正常**：完整路径 `video:generate`(main.ts:4703) → `runVideoGenJob`(main.ts:3868) → `ai.ts:generateVideo`(ai.ts:673，支持「即时返回」与「任务式轮询 `pollVideoTask`」两种模式) → 进度经 `video:progress`/`video:done` 广播到右上角悬浮气泡 → 下载本地 mp4 → 写 AI 视频消息并补未读。`npm run build` 0 错误，无链路断点。
- **QPS 限速仅包裹两处主回复路径**：`runOne`(main.ts:2468-2471) 与 `streamOne`(main.ts:2722-2725)，按 `cfg.qps`（每分钟最多 N 次）+ 窗口 `RATE_WINDOW_MS=60000` 限流。AI 自动补全提示词已**主动纳入**限速（超限速返回 `rateLimited`，不消耗额度）。
- **其余 AI 功能 QPS 覆盖状态（v2.3.7 当时）**：调用点 grep 全量确认为心情判定(998)、关系/朋友圈(1125/1181)、场景生图判定(1283)、翻译(2109)、AI 补全角色(4045)、群聊接话(2941/2953)、主动消息(3090)、群聊导演选人(2834)、随机事件(3227)、记忆提炼(3798)、模型对比(4129/4172)，以及独立视频生成 API。这些功能当时走各自独立 `queryAI`、不经 `rateWaitMs`/`rateMark`、不共享默认模型 QPS 配额；该现状已在 **v2.3.8 全部修正**（详见 v2.3.8 更新记录），本段仅作历史核查留档。

## v2.3.6 (2026-08-28)

### 开屏动画：仅冷启动播放，托盘/悬浮球唤出不再重播
- **问题**：`showMainWindow` 在窗口被销毁后会重建渲染进程（重新 `createWindow`），导致从托盘双击、或从悬浮球进入主界面时重新挂载 `App`，开屏动画被再次播放；用户期望只有双击 exe 冷启动才播开屏。
- **修复**：主进程新增冷启动标记，仅在进程首次 `createWindow` 时正常播放开屏；窗口销毁后被 `showMainWindow` 重建时，加载地址携带 `?nosplash=1`，`App.tsx` 据此初始化 `showSplash=false`，不再重播。隐藏（未销毁）窗口直接 `show()` 的情况因渲染进程未重建，开屏状态天然保持关闭。

### 后台消息提醒卡片：覆盖未挂载聊天的场景（修复"卡片消失"）
- **问题**：Steam 风格通知卡片原本仅由渲染端收到 `stream:done` 后调用 `notifyCard` 触发；当后台到达的主动/自动消息所属聊天当前未在渲染端挂载时，`onDone` 不触发，卡片不弹出，表现为"消息卡片弹窗没了"。
- **修复**：在 `sendStreamDone`（所有流式 AI 回复的统一收尾）与四处媒体生成（生图/生视频）站点统一调用主进程 `showNotifyCard`；`showNotifyCard` 内部仍按"主窗/小窗均隐藏"判定是否弹出，与渲染端触发互补且带 1.5 秒去重，避免重复。

## v2.3.5 (2026-08-28)

### 毛玻璃主题：浮层面板背景加深（防文字穿透重叠）
- **问题**：毛玻璃主题下 `--color-panel` 为 `rgba(255,255,255,0.22)` 半透明白底，聊天界面「其他操作」下拉菜单（`.more-dropdown`）等浮层菜单浮在聊天内容上方时，下方文字穿透导致菜单文字与聊天内容重叠、难以辨认。
- **修复**：在 `index.css` 新增 `[data-theme='glass']` 专用规则，对以下浮层统一换成高不透明深色底 `rgba(18,16,38,0.88)`（边框 `rgba(255,255,255,0.18)`）：`.more-dropdown`（其他操作）、`.mention-pop`（@提及/表情浮层）、`.mini-drawer`（小窗抽屉）、`.modal-card`（模态框）、`.error-bubble`（错误提示气泡）、`.reasoning-block`（思考过程块）、`.qps-queue`（排队提示）、`.mini-toast`（轻提示）。已逐项核查：`.affinity-pop`（好感度弹窗）为纯彩色文字无背景、`.img-preview-bar` 无背景、`.select-menu-panel` 用实色 `--color-select-bg`、Steam 风格通知卡片为独立透明窗口且深色态 95% 不透明，均无需改动。

### 主动消息：主界面最小化到托盘后仍正常触发
- **问题**：主动消息的空闲检测定时器（`setInterval`）跑在渲染进程（`ChatWindow.tsx` 与 `MiniChat.tsx`），判定逻辑无可见性门槛；但主窗口隐藏到托盘时 Chromium 默认对后台页面节流定时器，导致"到时间了消息还没发过来"。主窗口可见时一切正常。
- **修复**：在主窗口与小窗的 `BrowserWindow` 配置中 `webPreferences.backgroundThrottling` 设为 `false`，禁用后台定时器节流，保证隐藏 / 托盘驻留时主动消息与随机事件定时器照常运行。
- 验证：`tsc -p tsconfig.node.json` + `vite build` 均 0 错误通过。

## v2.3.4 (2026-08-28)

### 未读清单一致性核查与修复
- **核查结论**：未读数据仅有一份权威源（主进程 `electron/floatingBall.ts` 的 `unreadMap`），**只由悬浮球面板渲染**；主界面侧栏与快捷小窗均无独立未读红点/清单，「多处清单不同步」的前提不成立。悬浮球面板经 `ball:unread` 广播实时刷新，与 `unreadMap` 始终一致。
- **修复 A（已读残留）**：原 `setActiveChat` 仅记录当前聊天、不清未读。场景：主窗可见时你在看聊天 A，聊天 B 的主动消息计了未读，点开 B 后 `activeChatKey` 变 B 但 B 的未读没被清，仍留清单。现于 `setActiveChat` 内调用 `clearUnreadForChat(type, id)`，主界面 / 快捷小窗打开任一聊天即清除该会话未读。
- **修复 B（新消息漏显）**：生图 / 生视频完成写入 AI 消息走 `dm.addMessage` + `broadcast('stream:user')`，不经过 `sendStreamDone`，故窗口隐藏时不会计未读、不进清单。现在四处生成完成站点（场景生图 `triggerSceneImage`、生视频 `runVideoGenJob`、生图 `image:generate`、发图生图 `image:generateFromImage`）追加 `pushMediaUnread(...)`：主窗不可见或未正盯该聊天时补未读（占位文案 `[图片]` / `[视频]`，头像按聊天类型解析），确保后台生成结果也照常出现在未读清单。
- 验证：`tsc -p tsconfig.node.json` + `vite build` 均 0 错误通过。

### 开屏动画展示逻辑（说明）
- `App.tsx` 的 `showSplash` 状态恒为 `true`，每次启动都进入开屏；时长默认 1.8s（减少动效时 0.6s），淡出后 +0.5s 调 `onDone()`；点击可跳过。展示与否不依赖 localStorage / 已展示标记，仅由「刚启动」决定。`hasShownSplash` 仅作历史记录。

## v2.3.3 (2026-08-23)

### 安全核查：朋友圈 / 模型对比 / AI 补全 / AI 翻译 不泄漏思维链
- **核查结论**：经全量排查 `reasoning`（思维链）字段，仅在聊天回复主路径（`runOne` / `streamOne` / 主动消息）被读取并存为可折叠的 `msg.reasoning`。以下四个场景只读 `res.content`（模型最终回复文本），**不读取也不展示思维链**：
  - 朋友圈自动发（`judgeAndPostMoments` → `parseFirstJson(res.content)`）
  - 模型对比评分（`compare:start` → `res.content`）
  - AI 补全（`aiCompleteRole` → `res.content`）
  - AI 翻译（`translateText` → `res.content`）
- 用户预期「只会输出模型回复本身、思维链不展示」成立，无需改代码，已记录在档备查。

### 设置界面重构：分类整合到左侧导航区块
- **诉求**：把设置里平铺的连续 section 整合进不同区块，便于浏览。
- **实现**：
  - 新增 `SETTING_CATS`（6 分类，顺序即展示顺序）：通用与 AI 行为 / 模型与群聊 / 主题与外观 / 生成与扩展 / 翻译与音效 / 窗口与数据。
  - 设置页改为「左侧 sticky 分类导航 + 右侧分组滚动」经典布局：点导航平滑滚动到对应分区并高亮；右侧滚动时按距顶位置自动高亮当前所在分类。
  - 复用既有 `.panel` 滚动容器，分类间加分隔线；六种语言（中/英）新增 `settings.catGeneral` 等 6 个导航键与 `settings.*` 说明键。

### 删除左侧菜单栏无响应的刷新按钮
- **根因**：该按钮在 `App.tsx` 派发 `refresh` 自定义事件（`dispatchEvent(new CustomEvent('refresh'))`），但全仓无任何 `addEventListener('refresh')` 监听者，按下无任何效果。
- **实现**：移除 `App.tsx` 的 `refreshKey` state 与 `refreshUI` useCallback、`Sidebar` 的刷新按钮；`Sidebar` 现只保留导航 logo + 视图切换项。
  - 注：聊天界面「⋯」菜单里的「🔄 刷新界面」是**另一个**独立可用的按钮（仅刷新当前聊天视图、带 toast 提示），不在此次删除范围，予以保留。

### 「关于念语」移入设置内部
- **实现**：原左侧菜单栏底部 ℹ️「关于念语」按钮删除；在设置「窗口与数据」分类末尾新增「关于念语 · 念语」按钮，点击打开原有关于弹窗（`App.tsx` 经 `onAbout` prop 传入 `Settings`，`setAboutOpen(true)`）。

### 毛玻璃主题专属背景（自定义颜色 / 导入图片）
- **诉求**：毛玻璃（glass）/ 磨砂（frost）主题更换时可自选背景颜色，或导入图片作为软件背景，毛玻璃磨砂效果仍保留；该设置只在开启毛玻璃类主题时出现，否则隐藏。
- **实现**：
  - `AppSettings.glassBgColor`（CSS 颜色）/ `glassBgImage`（data URL）两个字段，默认均为空（空=跟随主题默认渐变）。
  - `ThemeContext` 在设置变更时：当 `theme==='glass'||'frost'` 且用户设了颜色/图片，把 `--app-bg` 内联为对应颜色或 `url(...)`；否则移除内联值回退主题默认。磨砂仍由主题 CSS 的 `backdrop-filter` 提供，背景替换不影响磨砂。
  - 设置「主题与外观」分类末尾新增「毛玻璃背景」区块（条件渲染：`theme==='glass'||'frost'` 才显示，未开毛玻璃主题时完全隐藏）：背景颜色选择器 + 导入图片按钮（FileReader 转 data URL 即时生效）+ 清除背景按钮 + 实时预览块；颜色与图片互斥（选其一即清空另一）。
  - i18n 新增 `settings.glassBg*` 六个双语键（标题/说明/颜色/图片/导入/清除/预览）。

### 玻璃主题聊天界面颜色覆盖（解决换背景后字体/边框看不清）
- **诉求**：毛玻璃主题更换背景色/导入图片后，顶部 Token 消耗数字字体、椭圆边框、气泡文字/边框有时与背景颜色融合，导致看不清；希望可单独调节这些元素的颜色，且气泡文字、边框也要一并纳入。范围限定为「玻璃主题（含 frost）」。
- **实现**：
  - `AppSettings` 新增 5 个字段（空=跟随主题默认）：`glassTokenText`（Token 栏字体色）、`glassTokenBorder`（Token 栏边框色/椭圆）、`glassBubbleUserText`（用户气泡文字色）、`glassBubbleAiText`（AI 气泡文字色）、`glassBubbleBorder`（气泡边框色，空=透明）。`DEFAULT_SETTINGS` 均默认空串。
  - `ThemeContext` 新增 `setGlassVar(name, val)` 辅助：仅当 `isGlass`（glass/frost）且值非空时 `setProperty`，否则 `removeProperty` 回退主题默认；注入 `--glass-token-fg` / `--glass-token-border` / `--glass-bubble-user-fg` / `--glass-bubble-ai-fg` / `--glass-bubble-border`。
  - `index.css` 对应选择器改为带回退变量的写法：`.token-bar` 用 `var(--glass-token-fg, var(--color-primary))` 与 `var(--glass-token-border, var(--color-primary))`；`.msg-row.user .bubble` / `.msg-row.ai .bubble` 文字色分别回退 `--color-bubble-user-text` / `--color-bubble-ai-text`；`[data-theme='glass'],[data-theme='frost'] .bubble` 新增 `border: 1px solid var(--glass-bubble-border, transparent)`。仅毛玻璃类主题注入，零回归风险，不影响其它主题布局。
  - 设置「毛玻璃背景」区块（`sec-glassbg`，已 gate `theme==='glass'||'frost'`）内、背景预览块之后，新增 5 个 `<input type="color">` + 一个「重置颜色」按钮（一键清空 5 个字段）。
  - i18n 新增 `settings.glassTokenText` / `glassTokenBorder` / `glassBubbleUserText` / `glassBubbleAiText` / `glassBubbleBorder` / `glassColorReset` 六个双语键。
  - `npm run build`（tsc + vite）0 错误通过；已本地 commit（未推送 GitHub）。

### 构建与发布
- `npm run build`（tsc + vite）0 错误通过；`package.json` 版本 `2.3.2 → 2.3.3`；文档同步（使用说明.md / 版本更新记录.md / 硬编码清单.md）。

## v2.4.0（开发中，待发版）

> 累积本次新增功能；版本号待发版时确认（当前 `package.json` 仍为 2.3.3）。

### 桌面悬浮球（独立透明置顶窗口，最高优先级）
- **实现**：新增 `electron/floatingBall.ts`（独立 `BrowserWindow`：无边框 / 透明 / 置顶 / 跳过任务栏 / 不可缩放 / 禁用系统移动），`floating-ball.html` + `src/floating-ball.ts` 渲染端，`vite.config.ts` 多入口新增 `floating-ball`。
- **拖拽无跳动/无粘滞**：渲染端 `mousedown` 仅发出 `ball:drag-start`（携带指针在窗口内抓取偏移），主进程每 16ms 轮询 `screen.getCursorScreenPoint()` 直接 `setPosition`，窗口始终贴合光标；`ball:drag-end` 返回是否发生有效位移以区分点击/拖拽。**未使用** `-webkit-app-region: drag`。透明窗配合 `setIgnoreMouseEvents`（拖拽期间整窗可交互，其余透明区 `forward` 穿透），仅悬浮球/面板本体可交互，无死区。
- **交互**：左键呼出主界面（`ball:activate`，并清未读）/ 右键退出（`ball:quit` → `app.quit`）/ **悬停即展开未读面板（含空态「暂无未读消息」）** / 拖拽自由移动（位置持久化、不回弹）。
- **未读**：主进程统一维护 `Map`（`pushUnread`/`clearAllUnread`/`clearUnreadForChat`），主窗口隐藏或最小化时才累计；角标 ≤99 显示数字、>99 显示「99+」、无未读隐藏。
- **接线**：`main.ts` 注入 `setBallMainShow`/`setBallMainWindow`、`app.whenReady` 创建、`registerIPC` 注册；设置页新增「桌面悬浮球」开关（`ball:set-enabled` 创建/销毁）。
- **streamId 格式**：`sendStreamDone` 按首个 `:` 切分 `chatId:roleId`（ids 内含冒号也安全），据此推送未读。

### ASR 语音输入 400 修复
- **根因**：`transcribeAudio` 硬编码 `audio/webm` + `audio.webm`，多数 OpenAI 兼容 ASR 服务端拒收 webm/Opus 返回 400。
- **实现**：`VoiceSettings` 新增 `asrFormat`（默认 wav）/ `asrLanguage`；前端 `useVoiceInput` 在 wav 时经 `blobToWav`（Web Audio 解码 → 重采样 16kHz 单声道 → 16-bit PCM WAV）重封装；主进程按格式映射扩展名/MIME，追加 `response_format=json` 与可选 `language`。

### TTS 按角色音色选择
- **实现**：`audio:tts` 新增可选 `roleId`，按 `ttsVoices[roleId]` 解析音色（缺省回退 `ttsVoice`）；新增 `audio:listVoices`（优先服务端 `/audio/voices`，失败回退内置清单 alloy/echo/fable/onyx/nova/shimmer）。
- **设置 UI**：语音设置内「按角色配置音色」列表（含群成员），下拉来自 `listVoices` + 手动输入；`ttsVoices` 存于 `voice` 子对象。
- `ChatWindow`/`MiniChat` 的 `speak` 调用 `textToSpeech(content, msg.role_id)` 传入角色。

### 设置搜索框（百度建议式候选）
- **实现**：设置页 `list-header` 右侧搜索框，索引 ~35 个设置项（标题 + 中英文关键词）；最多 5 条按关联度排序；毛玻璃下拉（可滚动、适配多主题）；命中高亮；点击/回车滚动到锚点并 `setting-flash` 闪动 5 秒。
- 各 `section-title` 增加稳定 `id`（sec-*），CSS 新增 `.settings-header-search` / `.settings-search-input` / `.settings-suggest*` / `.search-hl` / `.setting-flash` 等。
- **位置修正**：搜索框从 `.panel` 正文内部 sticky 区域移出，改为位于 `.list-header` 标题右侧，不侵入正文；去掉白底背景，消除不同主题下背景突兀的问题。

### 内置使用指南
- **实现**：新增 `src/components/GuideView.tsx` 弹窗，覆盖快速开始 / AI 配置 / 聊天群聊 / ASR / TTS / 生图 / 生视频 / 朋友圈 / 悬浮球 / 设置搜索 / 备份 / 窗口小窗等 12 节；指南内搜索逻辑与设置页一致（同款候选 + 闪动）。设置页「窗口与数据」新增「打开使用指南」入口，并在 **设置页顶部搜索框右侧常驻「打开使用指南」按钮**，入口更显眼。

### 本轮修复（2026-08-28）
- **设置搜索框遮挡正文**：`.settings-search` sticky 容器补全不透明背景（`var(--color-surface)`）+ 底部分隔线/阴影，滚动时正文不再从搜索框下方透出；内联 `marginBottom` 改为容器 `padding-bottom`（含于背景）。
- **设置搜索不跳转/无反应**：匹配算法由整串 `startsWith/includes` 改为**拆词多关键字**（如「语音 输入」逐词命中标题或关键词），任一 token 命中即候选；回车跳转到关联度最高的 `searchResults[0]`；候选高亮支持多词同时高亮。指南内搜索同步修正。
- **设置搜索框位置与白底**：搜索框从 `.panel` 正文内部移出，改为 `.list-header` 标题右侧，不侵入正文；去掉白底背景，不同主题下不再突兀；`.main-pane` 改为 `overflow: visible` 使候选框可向下展开不被裁剪。
- **错误日志黑底黑字**：错误日志弹窗背景与正文统一使用固定浅色文字（`#f0f0f0` / `#aaa` / `#999`），不再依赖 `var(--color-text)`，避免某些主题下文字为黑色导致不可见。
- **悬浮球拖拽卡顿/粘滞**：拖拽改为主进程轮询系统光标直接定位（见上），消除增量累加滞后；快速拖动时指针始终在窗口内，不再因移出窗口边界丢失 `mousemove` 而「粘住」。
- **未读消息面板不显示**：悬停即 `showPanel()`（原先仅在 `unreadCount>0` 时显示），无未读也展示空态面板；修正 `hidePanel` 误将整窗置为可交互的副作用。
- **使用指南入口不明显**：搜索框右侧新增常驻「打开使用指南」按钮。

### 本轮修复（2026-08-28 主题与悬浮球）
- **左侧栏移除「念」字**：`src/components/Sidebar.tsx` 删除 `.nav-logo` 元素，左侧导航仅保留功能图标。
- **悬浮球与未读面板去阴影**：`src/floating-ball.ts` 移除球体、角标、未读面板全部 `box-shadow`。
- **悬浮球与未读面板跟随主题**：`src/floating-ball.ts` 导入 `variables.css` 并监听 `onSettingsChanged`，根据当前主题设置 `data-theme`；球体、未读面板、角标边框、文字全部改用主题变量（`--color-primary`、`--color-primary-text`、`--color-panel`、`--color-text`、`--color-text-secondary`、`--color-border`、`--color-hover` 等），不再使用固定深/浅双色硬编码。
- **全局字体颜色适配**：新增 `--color-text-muted`、`--color-danger` 等语义变量并写入全部 14 套主题；`.narration`、`.msg-time-above` 改用 `var(--color-text-muted)`；`.error-bubble`、`globalErrorHandler.ts` 报错弹窗、`select-menu-item.active`、`.tab.active`、`.badge.primary`、`.srb-item-idx`、`.qps-queue .qps-cancel:hover`、`.sidebar .nav-lang button.active` 等处的文字/背景/边框统一改用主题变量与 `var(--color-primary-text)`，避免任意主题下出现黑底黑字或白底白字。
- **毛玻璃主题悬浮球未读面板对比不足**：毛玻璃主题下未读面板沿用 `--color-panel`（浅白低不透明 `rgba(255,255,255,0.22)`）+ 浅色文字，在亮桌面下对比不足、字被吃掉。`src/floating-ball.ts` 为 `[data-theme='glass'] .fb-panel` 新增深暗半透明磨砂底 `rgba(18,16,38,0.62)`（相应把计数徽标、行 hover 同步调为深底浅字）。这把"亮色覆盖"降下来、提高暗色对比，使浅色文字稳定可读，而非单纯调低 alpha（那只会透出更亮的桌面，仍看不清）。

- **悬浮球右键菜单 + 置顶 + 全屏自动关闭**：
  - 右键菜单：`src/floating-ball.ts` 右键不再直接退出，改为弹出自定义菜单（退出念语 / 本次关闭悬浮球 / 悬浮球置顶勾选），菜单跟随主题、点击空白或失焦自动收起。
  - 悬浮球置顶：新增设置 `floatingBall.alwaysOnTop`（默认 true）。菜单勾选或设置页开关即时调用 `ball:set-always-on-top` 切换窗口 `setAlwaysOnTop` 并持久化；取消后置顶则悬浮球会被其它窗口覆盖。
  - 本次关闭悬浮球：新增 `ball:close-session` IPC，仅销毁窗口并置会话级 `sessionClosed` 标记（不写设置），重启后按设置恢复显示。
  - 全屏自动关闭：新增设置 `floatingBall.autoHideInFullscreen`（默认 true）。`electron/main.ts` 为主窗口注册 `enter-full-screen`/`leave-full-screen`，按设置调用 `hideFloatingBall()`/`showFloatingBall()`；启动时若已全屏同样隐藏。
  - 类型与多语言：`src/types.ts`、`electron/preload.ts`、`src/ipc.ts` 同步 `ballSetAlwaysOnTop`/`ballCloseSession`；`Settings.tsx` 桌面悬浮球段新增两开关；`translations.ts` 增中英文案。

### 本轮修复（2026-08-28 设置搜索全量 + 开屏每次启动）
- **设置搜索全量覆盖**：原来搜索索引仅含分区/分类标题，勾选框、滑块、下拉等具体子项（如「随机事件」「主动消息记忆」「心情过渡指数」「心情判定冷却」）搜不到。`Settings.tsx` 改为在设置页挂载后**运行时扫描 `panelRef`**，自动给所有 `input[type=checkbox]` 的 label、`input[type=range]` 包裹块、`.field` 下拉、以及各子分区标题分配锚点并构建 `searchIndex`；基础静态索引（含中英文关键词）与其合并，搜索与跳转覆盖**全部设置项**。索引随语言切换与草稿加载重建，新增设置项自动纳入，无需手工维护。`goToSetting` 仍按 `id` 滚动并闪动 5 秒。
- **开屏动画每次启动都展示**：`App.tsx` 开屏初始状态由「`!localStorage('nianyu_splash_done')` 仅首次一次」改为恒为 `true`，移除一次性本地标记门槛；`hasShownSplash` 仅作历史记录、不再作为展示判定。退出软件后再次启动依旧展示开屏。

### 本轮修复（2026-08-28 主动消息未读 + 设置搜索加固）
- **主动消息回复计入未读**：`electron/floatingBall.ts` 的 `pushUnread` 增加 `fromProactive` 参数；`electron/main.ts` 的 `sendStreamDone` 透传 `message.from_proactive`。主动消息回复在你未正盯着该聊天时（主窗隐藏、或在看其他聊天）均计入未读并亮角标，仅正查看该聊天本身时不计；手动回复维持原行为（仅主窗隐藏时计）。判定依赖主进程记录的「当前聊天」：`App.tsx` 的 `openChat` 通过新增 IPC `app:active-chat`（`preload.ts`/`src/ipc.ts` 的 `setActiveChat`）回传主进程，`electron/main.ts` 注册处理并写入 `floatingBall.setActiveChat`。
- **设置搜索覆盖加固**：`Settings.tsx` 运行时扫描由「仅 `label:has(checkbox)` + `.field`」升级为覆盖全部控件，新增对 `div` 包裹的勾选框、全部滑块、`select`/文本输入/文本域、以及分段选项按钮组（`btn-primary`/`btn-ghost`，取其上方设置名）的索引；去除了对 `:has()` 选择器的依赖，按「设置名」统一锚定并去重，确保「随机事件」「全局主动消息」「心情过渡指数」等所有子项均可搜可跳转。

### 单实例锁 + 开屏动画仅一次
- 单实例：`app.requestSingleInstanceLock` + `second-instance` 聚焦已有窗口。
- 开屏：`App.tsx` 用 `localStorage` + `hasShownSplash` 惰性初始化，仅首次展示。

### 视频生成进度提示
- 确认既有 `VideoBubble`（订阅 `video:progress`/`video:done`，可拖拽、完成自动消失）已在位；主进程每 10 秒轮询进度的逻辑已存在，本版无需改动。

### 构建与发布
- `npm run build`（tsc 主进程 + vite 渲染，87 模块）0 错误通过；文档同步（使用说明.md / 版本更新记录.md）。

## v2.3.2 (2026-08-22)

### 模型对比：评测模型可手动指定 + 评分失败可见提示
- **诉求**：把「评测员」从固定的默认模型改为可在对比界面手动指定的模型（或选被测模型之一实现「互评」）；评分失败时给前端可见提示而非静默吞掉；分数区加「质量评分」标题更显眼。
- **实现**：
  - `ModelCompare` 顶部新增「评测模型」下拉（默认「默认模型」，可任选已启用模型；选中被测模型之一即互评）；`compareStart` IPC 新增 `judgeModelId`，主进程按其解析评测模型，回退默认模型。
  - 互评时跳过评判评测模型自身的输出（避免自夸）；评分失败（接口报错 / 未返回合法 JSON）时 `compare:judged` 携带 `judgeError`，前端底部显示红色提示并记入错误日志，不再 `console.error` 静默吞掉。
  - 各列评分区增加「质量评分」标题行。

### 深度思考：改为每模型手动标记（替代关键字猜测）
- **诉求**：从「按模型名关键字猜是否支持推理」改为在模型管理中手动勾选。
- **实现**：`ModelConfig.supportsReasoning`（默认缺省）；`ModelEditor` 新增「支持深度思考 / 推理」开关；设置页深度思考下拉不再因默认模型关键字猜测而禁用；`ai.ts applyDeepThink` 仅当 `deepThinkLevel ≠ off` **且** `cfg.supportsReasoning` 时写入 `reasoning_effort`（流式/非流式均生效）。

### 模型报错提示气泡（非模态，15s 自动消失）
- **诉求**：模型回复出错时弹出气泡，15 秒自动消失或点 × 关闭，点击展开详情；错误码被软件识别、记录到日志；错误信息不进聊天框、不写记忆；用软件设置的语言说明原因与解决办法。
- **实现**：
  - `ai.ts` 错误结构化：新增 `ModelErrorInfo` / `ModelApiError` / `httpStatusToCode`（401/403→auth、404→notFound、429→rateLimit、400→badRequest、408→timeout、5xx→serverError…）；`queryAI` 不再把错误文本当 content 返回，改返回 `error` 字段。
  - 主进程新增 `notifyModelError`：记日志（`dm.logError('model', …)`）→ `diagnoseError` 自动诊断原因/解决方案（双语）→ 广播 `app:modelError`。
  - 各回复路径（单聊非流式 / 群聊流式 / 主动消息 / Anthropic 回退）在 `res.error` 时不再 `addMessage` 写聊天框，改为气泡通知 + 前端「发送失败」重发面板（复用 stream:chunk error 机制）。
  - 新建 `ErrorBubble` 组件（portal 到 body，右上角浮层；外层 pointer-events:none、仅卡片可交互，绝不阻断输入框；15s 自动消失 / × 关闭 / 点击展开原因与解决）；挂载于 App 与 MiniChat。错误码文案使用软件当前语言。

### 输入框外观自定义（文字色 / 内部背景色）
- **诉求**：优化输入框内部背景色（默认浅灰/白），字体颜色与背景色可在设置中自定义，防止文字与背景相近看不清。
- **实现**：`AppSettings.inputBgColor` / `inputTextColor`（空=跟随主题）；`ThemeContext` 注入 `--input-bg` / `--input-fg` CSS 变量；聊天输入框（`chat-input` 类，主窗+小窗）默认浅灰底 + 深字，可被设置覆盖；设置页「输入框外观」新增双颜色选择器 + 实时对比度预览 + 恢复默认。

### 修复：卸载程序勾选删除数据但数据仍保留
- **根因**：v2.3.0 起默认数据目录迁移到 `文档\念语数据`，但卸载脚本只删 `$APPDATA/$LOCALAPPDATA\nianyu-client` 与自定义路径，漏删默认目录。
- **实现**：`build/installer.nsh` 卸载区段增加 `RMDir /r "$DOCUMENTS\念语数据"`（`$DOCUMENTS` 自动适配系统语言，两轮重试均删除）；卸载确认页文案同步更新数据位置说明。

### 构建与发布
- `npm run build`（tsc + vite）0 错误通过；`package.json` 版本 `2.3.1 → 2.3.2`；文档同步（使用说明.md / 版本更新记录.md）。

## v2.3.1 (2026-08-20)

### 记忆隔离（按聊天隔离，默认开启）
- **诉求**：同一人物在不同聊天中的记忆互相独立，互不串扰；可在人物界面设置，默认开启。
- **实现**：
  - `Role.memoryIsolation`（默认 `true`）+ `MemoryEntry.chatId` 维度；`db.listMemories(roleId, chatId?)` 同时返回本聊天隔离记忆 + 角色级共享记忆（无 chatId）。
  - `buildSystemPrompt` / `extractMemories` 按隔离开关取值；群聊以 `groupId` 作为 chatId。
  - 角色编辑新增「记忆隔离」开关；记忆面板以「本聊天专属 / 共享」徽标区分；复制角色/聊天时隔离记忆随新聊重映射。

### 模型设置增强（滑条 + 输入框双改）
- **诉求**：记忆读取限制（0~100 条）、上下文长度（0~百万 + 64K/128K/256K/512K/768K 快捷档）、温度（0~2）、Top-P（0~1）、Top-K（0~50）全部支持无级滑动 + 输入框直接输入；增加单次最大输出 max_tokens。
- **实现**：
  - `ModelConfig` 新增 `topP/topK/maxTokens/memReadLimit`；`ModelEditor` 新增 `RangeField`（滑条+数字框双向同步+快捷档位）。
  - 参数真实下发：`queryOpenAILike`/`streamAI`/`queryAnthropic` 增加 `max_tokens/top_p/top_k`（Anthropic 温度映射 0~1）。
  - `maxContext` 死字段复活：`trimToContext`（token 估算裁剪）在流式/非流式生成前生效；历史窗口由硬编码 `.slice(-16/-24)` 改为 `effectiveHistoryCap`（尊重 `memReadLimit`）。

### 视频生成完成检测（异步任务 + 进度气泡）
- **诉求**：生视频时自动检测是否生成完；生成期间可正常聊天，显示可拖动悬浮气泡进度。
- **实现**：
  - `ai.ts generateVideo` 重构：自动识别「任务式」接口（提交返回任务 ID → 轮询 `/videos/generations/tasks/{id}`，3s 间隔，9 分钟上限，progress 字段 0~1/0~100 自适应）与「即时返回」接口；新增 `onProgress` 回调与深层视频地址兜底查找。
  - 后端新增 `runVideoGenJob`（非阻塞）：`video:generate` 与「图生视频」分支立即返回 `{ok, started}`，后台生成全程广播 `video:progress` / `video:done`；完成自动写入 AI 视频消息（`image_path` = 本地 mp4）。
  - 前端新增 `VideoBubble`（可拖动悬浮气泡，订阅进度/完成广播，完成 8s 自动收起），挂载于 App；`image:get` 的 mime 表扩展 `.mp4/.webm/.mov`；`ImageGrid` 按扩展名渲染 `<video controls>`。
  - 聊天工具栏新增 **🎬 生视频** 按钮 + 输入弹窗（文本→视频）。

### 朋友圈视频生成（独立开关）
- **诉求**：朋友圈支持 AI 自动写提示词生成视频，独立开关，未配置视频模型时默认关闭。
- **实现**：`AppSettings.momentsVideoEnabled`（默认 false）+ 设置开关（未配置「生视频」API 时黄色提示）；`judgeAndPostMoments` 提示词新增 `needVideo/videoPrompt`，满足开关+配置时后台 `runMomentVideoJob` 生成并挂入 `moment.videos`（MomentsView 合并渲染）。

### 复制聊天与角色增强
- **诉求**：复制聊天/角色时，人物状态、记忆参数、聊天记录一并复制。
- **实现**：`db.copyRole(id, includeChats)` 完整克隆角色（状态/记忆含隔离 chatId 重映射/全部单聊消息与卡片 + 每聊参数 `remapChatSettings`）；新增 IPC `roles:copy`；角色列表「复制」按钮由浅拷贝改为调用 `copyRole(id, true)`。

### 模型对比（⚔️）
- **诉求**：同一问题同时发给最多 3 个模型，三列对照；回复与角色记忆无关；右上角显示完成时间；可 JSON 导出；默认模型自动评判输出质量。
- **实现**：
  - 后端 `compare:start`：并发 `queryAI`（中性 system，无角色记忆），返回每模型耗时/token/内容；随后默认模型从准确度/完整性/逻辑/可读性四方面打分（0~100 + 评语，JSON）。
  - 前端 `ModelCompare`：3 列模型选择 + 共享问题输入 + 开始对比 + 每列完成耗时徽标 + 客观指标 + 评分徽章（🟢/🟡/🔴）+ 导出 JSON（`model-compare.json`）；侧边栏新增 ⚔️ 入口。

### 模型自定义参数（JSON）
- **诉求**：模型编辑可输入 JSON 格式自定义参数，灵活传入厂商特有、UI 未单独暴露的请求字段。
- **实现**：
  - `ModelConfig` 新增 `customParams?: string`（JSON 文本）。
  - `electron/ai.ts` 新增 `applyCustomParams(body, cfg)`：每次请求（OpenAI 兼容 / Anthropic）构建完内置请求体后，解析 `customParams` 并**合并覆盖**同名字段；`messages` / `model` / `stream` 受保护不被覆盖（防止破坏对话体与流式引擎）。JSON 非法或非对象时抛**明确错误**，经 `queryAI` 捕获展示给用户。
  - `ModelEditor` 高级参数区新增多行输入框（等宽字体，4 行），保存时校验 JSON 合法性，非法则阻止保存并提示具体原因；占位符给出 `stop` / `frequency_penalty` 示例。

## v2.3.0 (2026-08-18)

### 群聊选人回复（F1）
- **诉求**：群聊里想精细控制对话走向——每次主动发消息、以及每条 AI 回复之后，都让用户自由选择「下一位由谁接话」。
- **实现**：
  - 设置 → 群聊新增 `groupSelectReply` 开关（与「AI 主动续聊 `groupAutoChain`」互斥：开启任一自动关闭另一）。
  - 群聊发送栏新增「全群可见」「进入记忆」两个开关：可见消息才允许勾选进入自动总结记忆；一旦取消可见，记忆开关自动禁用并清零（不可见消息不允许进记忆）。
  - 开启选人回复后，主进程在用户主动发送消息与每条 AI 回复消息完成后广播 `group:needSpeaker`（`payload { chatId, members:[{id,name,avatar}] }`），前端弹出选人浮层（列出群成员头像+名称），点击某成员即驱动该成员接话（`chats:groupContinue({chatId, forceRoleId, visibleToGroup, toMemory})`）。
  - 可见性 / 记忆标志随消息双向携带：用户消息经 `startStream`/`sendUserMessage` 的 `visibleToGroup`/`toMemory`；AI 回复经 `handleGroupContinue`/`handleStream` 写入 `msg_kind`（public/private）、`visibleToGroup`、`toMemory`。不可见消息自动 `toMemory=false`。
  - 主窗 `ChatWindow` 与快捷小窗 `MiniChat` 均接入（含选人浮层、可见性/记忆开关、F4 发图生图按钮）。

### AI 生视频（F2）
- **诉求**：除生图外，支持调用独立的视频生成模型异步产出视频。
- **实现**：设置 → 生图段之上新增「生视频」独立配置段：`enabled` + `baseUrl`/`apiKey`/`model`（复用生图模型列表拉取 `refreshModelList('img', …)`）+ `size`/`duration`，即时落盘。后端新增 IPC `video:generate`（异步生视频，调用方式与生图一致，结果经广播同步到聊天）。

### 自动读取人物头像生图（F3）
- **诉求**：异步生图时，自动把 AI 人物头像当作参考图喂给生图模型，提升人物一致性；与场景无关时不影响生成。
- **实现**：场景生图段新增 `asyncImageUseAvatar` 开关；开启后异步生图自动读取当前 AI 人物头像（非用户头像）作参考图。

### 发图片生图 / 生视频（F4）
- **诉求**：用户直接发一张图 + 一段文字提示词，AI 据此生图或生视频。
- **实现**：发送栏检测到「有附图且已输入提示词」时显示「以此图生图」「以此图生视频」按钮，调用后端 `image:generateFromImage`（异步生图/生视频）；仅发图片无文字提示词时不生效（普通图片消息照常发送）。

### 修复：卸载选「删除所有数据」后重装数据仍在（F5）
- **现象**：`uninstall.exe` 勾选「删除所有数据」，卸载看似成功，但重装后旧数据仍在。
- **根因**：`build/installer.nsh` 的 `taskkill` 用了 `nianyu-client.exe`，而真实 exe 文件名取自 `productName`（念语），即 `念语.exe`——进程从未被杀，数据目录（`$APPDATA\nianyu-client` / `$LOCALAPPDATA\nianyu-client`）被锁，`RMDir /r` 静默失败。
- **修复**：`taskkill /f /im "念语.exe"` 并在删除前后各加一次重试兜底（`Sleep` 等待句柄释放后再 `RMDir`）。

### 修复：深度思考模型回复气泡无加载动画（F6）
- **现象**：部分 AI 回复气泡内没有加载动画，只有顶部横幅显示「AI 正在回复」。
- **根因**：深度思考模型先流式输出 `reasoning`（思维链）再输出 `content`，此时渲染判定 `typing(=!content && !reasoning)` 与 `hasText` 同时为 false → 气泡渲染成空 div，仅顶部横幅有动画。
- **修复**：气泡渲染条件改为「有 `reasoning` 或 `hasText` 才渲染正文气泡，否则渲染 `.bubble-loading` 兜底滑块」；新增内联 `.typing-inline` 动画（流式中将滑块置于气泡尾随），主窗 / 小窗一致修复。

### AI 深度思考等级（F7）
- **诉求**：自动判定模型是否支持深度思考、以及可切几档；UI 可切换。
- **实现**：设置 → 模型管理新增深度思考等级下拉（off / low / medium / high）。`modelSupportsDeepThink(modelId)` 自动探测能力（正则匹配推理模型关键字），不支持时下拉禁用并显示「当前默认模型不支持深度思考」提示。生效路径：`setDeepThinkLevel` 写入请求体 `reasoning_effort`（仅 OpenAI 兼容接口生效）。

## v2.2.12 (2026-08-18)

### 修复：内联引用 [n] 点击不跳转 + 旁白内引用不可点
- **现象**：v2.2.11 上线后，AI 回复正文里的 `[1][2]…` 部分序号可点、部分不可点，且点击后均不跳转网页（下方搜索结果气泡的编号点击同样失效）。
- **根因（点击不跳转）**：`electron/main.ts` 用 `ipcMain.handle('app:openExternal')` 注册，而 `preload.ts` 用 `ipcRenderer.send('app:openExternal')` 发送。`send` 只配 `ipcMain.on` 才会触发，`handle` 仅响应 `ipcRenderer.invoke` —— 两者错配，主进程从未收到事件，故点击毫无反应（v2.2.10 也一并受影响，只是当时未被真实验证）。
- **根因（部分不可点）**：`src/utils/markdown.tsx` 的 `inline()` 先把 `（）`/`「」`/`""` 内的内容当作「旁白」直接包裹，**绕过**引用解析，导致写在旁白里的 `[n]` 只是纯文本、不可点击。
- **修复**：
  - `electron/main.ts`：`ipcMain.handle('app:openExternal', …)` → `ipcMain.on('app:openExternal', …)`（与 preload 的 `send` 配对）。
  - `src/utils/markdown.tsx`：旁白分支同样经 `withCitations()` 解析，使旁白内的 `[n]` 也可点击。
- **验证**：构建 0 错误。重启后正文与旁白里的 `[n]` 均可点击，点击即用系统浏览器打开对应网页。

## v2.2.11 (2026-08-18)

### 联网搜索「AI 回复正文内联引用 [n] 可点击跳转」
- **诉求**：AI 回复气泡内、被模型引用的联网搜索网页序号（如 `[1][2]`）要直接嵌入正文，且点击该序号即跳转打开对应网页（而非只在下方搜索结果气泡点编号）。
- **实现**：
  - `src/utils/markdown.tsx` 新增 `RenderOptions{citations,onCite}`：`renderMarkdown` 将正文中的 `[n]` 扫描为可点击徽标 `.cite-badge`（`onClick`/`onKeyDown(Enter/空格)` 调用 `onCite(url)`，并 `stopPropagation` 避免误触文本选择菜单）；`n` 超出搜索结果范围时保留原样文本，不影响阅读。
  - `ChatWindow.tsx` / `MiniChat.tsx` 的 `MessageRow` 新增 `searchResults` prop（外层 `.map()` 按 `sid` 取 `searchResultsByStream[sid]` 传入）；仅 **AI 消息**且存在本轮搜索结果时，把「序号 → URL」映射与 `api.openExternal` 回调交给 `renderMarkdown`。
  - `src/styles/index.css` 新增 `.cite-badge`（主题色、加粗、`cursor:pointer`、hover 淡背景、`:focus-visible` 描边），与 `.srb-item-idx` 视觉一致。
- **使用**：开启联网搜索后，AI 回复正文里出现的 `[1][2]…` 即对应本轮检索网页，点击序号用系统浏览器打开该网页（主窗 / 小窗均支持；若模型未显式标序号则不会生成可点徽标）。

## v2.2.10 (2026-08-18)

### 联网搜索结果「点击编号打开对应网页」
- **诉求**：点击联网搜索结果气泡内的数字编号，应直接打开所引用的网页。
- **根因**：Electron 中 `<a target="_blank">` 锚点不会跳转系统默认浏览器（无原生处理），导致点击编号 / 链接毫无反应。
- **实现**：
  - 主进程新增 IPC `app:openExternal`：`ipcMain.on('app:openExternal', …)`（`preload` 用 `ipcRenderer.send` 发送，须用 `on` 配对而非 `handle`）校验 `http(s)://` 后调用 `shell.openExternal(url)` 拉起系统浏览器（仅放行 http/https，避免任意协议被拉起）。
  - `electron/preload.ts` 与 `src/ipc.ts` 的 `NianyuAPI` 接口新增 `openExternal(url)`；preload 实现为 `ipcRenderer.send('app:openExternal', url)`（`fire-and-forget`）。
  - 前端 `ChatWindow.tsx` / `MiniChat.tsx`：搜索结果项由死链 `<a>` 改为可点击容器，整条结果（含编号徽标）点击即 `api.openExternal(r.url)` 打开网页；编号徽标 `cursor:pointer`、整条 hover 下划线提示可点击。
  - i18n：`chat.webSearchResultToggle` 文案补充「点击编号可打开网页」提示（中 / 英）。
- **使用**：开启联网搜索后，每条 AI 回复下方若展开「🌐 联网搜索 · N 条结果」气泡，点击其中的编号或任意结果条目，即用系统浏览器打开对应网页（主窗 / 小窗均支持）。

## v2.2.9 (2026-08-18)

### 联网搜索结果「按数字标序 + AI 回复内嵌引用编号」
- **诉求**：把联网搜索结果里的网页按数字标序，并在 AI 回复中把他引用的网页序号嵌入回复正文。
- **实现（electron/main.ts 核心）**：
  - `fetchPageContext` / `fetchSearchContext` 改造为返回 `{ context, pages }`：`context` 是注入模型的**带 `[1]、[2]…` 编号**的资料（抓取正文或摘要兜底，均带编号）；`pages` 是前端气泡展示用（仅标题/链接/短摘要）。
  - **关键一致性**：气泡展示的 `pages` 与注入模型的编号 `[1..N]` **顺序完全一致**（同一次返回、同源排序），保证模型回复里写的 `[2]` 一定能对应到气泡里的第 2 条。
  - 注入指令新增「**引用要求**」：使用了某条资料时，在对应句末用其编号标注（如「相关结论……[1]」，多来源可并列「[1][3]」），只标真正用到的编号、不得编造。
  - 广播 `search:results` 改为发送 `payload.pages`（原发送全部原始 results），使气泡只展示被编号、可被引用的网页。
- **实现（ChatWindow.tsx / MiniChat.tsx）**：气泡每条结果前加圆形编号徽标 `[n]`（`srb-item-idx`，新增配套 CSS），与模型引用编号对齐。
- **注意**：气泡是会话内展示、不写入存档，故重启软件后历史消息下的编号气泡不会复现（检索数据未入库）。若需重启后保留，需把搜索结果写进消息存档（数据层改动）。

## v2.2.8 (2026-08-17)

### 联网搜索结果气泡改为「每条回复独立拥有」
- **现象/诉求**：此前联网搜索结果在对话底部以**一个共享气泡**呈现，所有回复的检索结果堆在一起；用户要求「每个回复下面独立存在，不会所有回复都在一个气泡里」。
- **实现（ChatWindow.tsx / MiniChat.tsx）**：
  - 后端检索在流式开始前完成，前端先把结果暂存为 `pendingSearchRef`；首个 `stream:start` 事件将其挂到该消息的 `streamId`（`${chatId}:${roleId}`），存入 `searchResultsByStream[streamId]`。占位消息打上 `streamId`，完成后用 `streamMsgIdRef[消息id]=streamId` 保留映射，历史消息仍可显示自己的气泡。
  - 渲染时气泡**内联在消息 map 中、紧跟对应 AI 回复之后**（不再有全局独立气泡），每条回复独立折叠/展开（`expandedStreams[streamId]`）。
  - 群聊一次检索被多成员共享，仅首个发言成员的消息挂气泡（避免重复）；单聊每轮一条回复即一个气泡，符合预期。
  - 切换对话 / 新发一轮均重置 `pendingSearchRef` 与分桶状态，互不串扰。

## v2.2.7 (2026-08-17)

### 修复：百度源「抓取网页正文」几乎必失败 → 改用 Bing 直连 URL 抓取
- **现象**：用户使用 `provider=baidu` 时，终端打印 `page fetch empty, fallback to snippets`——6 条结果全部抓取失败，模型只能拿到搜索摘要（薄内容），仍不基于检索作答。
- **根因**：百度搜索结果链接是**加密跳转**（`https://www.baidu.com/link?url=...`），其真实目标页绝大多数是 JS 渲染 / 反爬站点（百度百科、知道、知乎等），服务端 `fetch` 拿到的正文 <120 字符（或 403），全部被丢弃；而默认只抓**前 3 条**（恰好是最易失败的百科/知道/知乎），结果 4~6（常是 SSR 的新闻/政府站）从未尝试。
- **修复（electron/main.ts）**：
  - `fetchPageContext` 在 `searchProvider==='baidu'` 时，**额外用 Bing 搜索同一 query 取得真实直连 URL**（百科/新闻等可服务端渲染的页面），与原结果合并去重后一起抓取，最大化拿到可读正文的概率（日志 `[websearch] baidu→补充 N 条 Bing 直连 URL 用于抓取`）。Bing 不可达则静默回退到百度结果。
  - 默认抓取条数 `webSearchFetchCount` 3 → **5**（范围仍 1~6），更多机会命中可抓页面。
  - `fetchOnePage` 增加 **`Referer` 头**（取目标 URL 自身 origin）降低 403 概率。
  - `extractArticleText` 增加 **`<div>` 兜底提取**（很多中文资讯/官网页用 `<div>` 排版而非 `<p>`），`<p>` 段落阈值 `>15` → `>10`，命中率提升。
- **硬编码值**：`PAGE_TEXT_MAX=2600` 字符、`PAGE_BYTES_MAX=1.5MB`、并发抓取上限 6、单页超时 2000~20000ms、仅抓 `text/html`、正文 <120 字符丢弃。

## v2.2.6 (2026-08-17)

### 联网搜索升级为 DeepSeek 式「检索 + 抓取网页正文」
- **背景**：原实现只把搜索引擎返回的「标题/摘要/链接」注入模型。但摘要常常为空或过短，模型即便被要求「必须依据」也缺乏可引用的实质内容，仍偏向凭记忆作答。按用户要求改为 DeepSeek 式：先检索，再**并发抓取前 N 篇结果网页的正文纯文本**，过滤广告/CSS/JS 后作为 grounding 注入。
- **核心新增（electron/main.ts）**：
  - `extractArticleText(html)`：轻量正文提取。依次尝试 `<article>/<main>` → 聚合 `<p>` 段落 → 全文去标签兜底；折叠空白，单页截断 `PAGE_TEXT_MAX=2600` 字符。复用既有 `decodeEntities`/`stripTags`。
  - `fetchOnePage(url, timeout)`：单页抓取。仅处理 `text/html`（跳过 pdf/json/图片）；带 `AbortController` 超时、字节上限 `PAGE_BYTES_MAX=1.5MB` 防大文件；正文 <120 字符（JS 渲染页/反爬页）丢弃；浏览器 UA + 中英文 Accept 头。
  - `fetchPageContext(results, settings)`：对前 `webSearchFetchCount` 条结果 `Promise.allSettled` **并发**抓取，单页失败不影响其余；拼接为「【网页 i】来源：url\n正文」块；全部失败返回空串。
  - `fetchSearchContext` 改造：拿到搜索结果后，若 `webSearchFetchPages` 开启则调用 `fetchPageContext` 取正文；为空则**回退**到原摘要格式（日志 `[websearch] page fetch empty, fallback to snippets`）；注入文案标注「已抓取网页正文」。
- **新增设置（设置 → 联网搜索）**：`抓取网页正文(DeepSeek 式)` 开关（默认开）、`抓取条数(1~6)`（默认 3）、`单页超时(ms)`（默认 8000，范围 2000~20000）。`types.ts` 的 `AppSettings`+`DEFAULT_SETTINGS`、Settings.tsx UI、translations.ts 中英文同步。
- **硬编码告知**：`PAGE_TEXT_MAX=2600`、`PAGE_BYTES_MAX=1_500_000`、并发抓取数上限 6、单页超时 2000~20000ms；仅抓取 `text/html`，正文 <120 字符丢弃。抓取并发进行，总耗时≈单页最慢者，不显著拖慢首字。
- 改动文件：electron/main.ts、src/types.ts、src/components/Settings.tsx、src/i18n/translations.ts。构建 tsc(backend)+vite 0 错通过。

## v2.2.5 (2026-08-17)

### 修复：模型「收到检索结果却不引用」（注入已生效但指令过弱）
- **背景**：v2.2.4 已确认检索结果合并进了首条 system 提示词（Anthropic / OpenAI 兼容端点均能读到），链路本身没问题。但用户实测模型仍「裸答」、不看搜索结果——根因是注入的指令措辞偏软（「仅供回答参考」且藏在很长的角色设定系统提示词末尾），模型被角色人设主导、倾向于凭自身记忆作答，检索资料权重被压低。
- **修复**：
  1. **指令改为硬性约束**：注入文案改为「【联网搜索结果 · 必须依据】……你必须优先依据这些资料回答，不得凭空编造，也不得只依赖自身记忆；若资料中包含答案，请直接引用其中的关键信息并自然融入角色口吻的回复……只有当资料确实与问题无关时才可不使用」，显著提升模型引用率。
  2. **检索结果格式更易读**：`fetchSearchContext` 返回文本由 `[1] 标题\n链接\n摘要` 改为明确分段「`[1] 标题：…\n链接：…\n摘要：…`」，摘要为空时标注「(无摘要)」，方便模型定位。
  3. **注入确认日志**：两处注入点（非流式 `runOne` / 流式 `streamOne`）在合并后输出 `[websearch] injected search ctx len=… role=…`，可在 `npm start` 终端确认「检索结果确实进了模型上下文」（与既有的 `[websearch] provider=… results=N` 呼应）。
- 硬编码告知：检索结果合并进入首条系统提示词；搜索结果作为硬性参考、优先于模型自身记忆被引用。
- 改动文件：electron/main.ts（`fetchSearchContext` 格式 + `appendToFirstSystem` 两处注入文案 + 注入日志）。构建 tsc(backend)+vite 0 错通过。

## v2.2.4 (2026-08-17)

### 修复：检索结果未注入模型（模型「裸答」、不引用搜索结果）
- **根因**：`fetchSearchContext` 已正确拿到结果（终端日志 `baidu results: 6`），但原先把结果作为 `system` 消息 **`messages.push` 追加到对话历史末尾**。而 `ai.ts` 的 Anthropic 路径只读取**第一条** system 消息（`messages.find(role==='system')`）并过滤掉其余 system 消息；多数 OpenAI 兼容端点也只认首条 system。末尾的搜索结果 system 消息被丢弃 / 严重降权，模型看不到检索内容，于是「裸答」。
- **修复**：新增 `appendToFirstSystem(messages, content)`，将「联网搜索结果」与「插件指令」**合并进第一条 system 消息**（无 system 消息时 unshift 到头部），同时强化指令措辞为「请基于以下信息回答用户的问题」。非流式 `runOne` 与流式 `streamOne` 两处注入均改用该辅助函数，确保 Anthropic / 各类 OpenAI 兼容端点都能读到 grounding。
- 硬编码告知：检索结果合并进入首条系统提示词；搜索结果仅作为回答参考，不要求模型注明来源。
- 改动文件：electron/main.ts（`appendToFirstSystem` + 两处注入点）。构建 tsc(backend)+vite 0 错通过。

## v2.2.3 (2026-08-17)

### 修复：百度源解析失败导致「开了开关却完全不搜索」
- **根因**：用户将检索源手动设为「百度」后，百度现网结果标题为 `<h3 class="c-title t tttitle">`，而解析器正则写死旧版 `<h3 class="t">` 一条都匹配不到，`searchWeb('baidu')` 返回空数组 → 不注入上下文、不广播 `search:results`，表现即「直接模型输出、无搜索」。
- **修复**：`parseBaidu` 正则改为兼容新旧结构（`\b(?:t|c-title)\b`），摘要抓取覆盖 `c-abstract` / `content-right` / `c-span`；百度请求补全 `Accept`/`Referer` 头并加 `rn=10`。同时：Bing 改用国内可达的 `cn.bing.com` 并补请求头；`auto` 回退链顺序调整为 `['bing','baidu','duckduckgo']`（DDG 国内常被墙，放最后避免空等 8s 超时）；`searchWeb` 全程输出 `[websearch]` 终端日志（provider / query / 命中条数 / 引擎失败原因），便于在 `npm start` 终端确认检索是否真的执行。
- 硬编码告知：单引擎超时仍 8000ms；`auto` 顺序 `['bing','baidu','duckduckgo']`。
- 改动文件：electron/main.ts（parseBaidu、searchOne、searchWeb）。构建 tsc(backend)+vite 0 错通过。

## v2.2.2 (2026-08-17)

### 修复：联网搜索无结果（默认检索源改为国内可达的多引擎免费回退）
- **根因**：默认检索源为 DuckDuckGo，而该源在中国大陆被屏蔽 / 强反爬，`searchWeb` 返回 0 条，于是既不注入上下文也不广播 `search:results`，聊天界面看不到任何搜索结果气泡。
- **修复**：默认检索源改为 **`auto`（多引擎免费回退）**，依次尝试 **Bing 直爬 → DuckDuckGo（Lite 优先、HTML 兜底）→ 百度直爬**，任一返回结果即用，无需任何 Key。另保留 Tavily / SerpAPI（需 Key）。每个引擎独立 8s 超时，结果按 url 去重。
- 各引擎独立解析器 `parseBing` / `parseBaidu` / `parseDdgLite` / `parseDdgHtml`；调度器 `searchWeb` 新增 `searchOne(engine)` 与 `auto` 回退链。
- 设置 → 联网搜索：默认源改为「自动」，下拉新增「Bing 直爬 / 百度直爬」；Key 提示改为「仅 Tavily / SerpAPI 需 Key」。
- 硬编码告知：默认 `searchProvider = 'auto'`（`src/types.ts` 的 `DEFAULT_SETTINGS`）；`auto` 链顺序 `['bing','duckduckgo','baidu']`，单引擎超时 8000ms。
- 改动文件：electron/main.ts（searchWeb 重写）、src/types.ts、src/components/Settings.tsx、src/i18n/translations.ts。构建 tsc(backend)+vite 0 错通过。

## v2.2.1 (2026-08-17)

### 联网搜索结果可折叠气泡 + 关闭确认框遮挡修复
- **联网搜索结果折叠展示**：开启联网搜索后，每次检索到的原始结果会在聊天消息区末尾以一枚低调的折叠气泡呈现——默认收起为一行「🌐 联网搜索 · N 条结果」药丸，点击展开可见全部结果（标题 / 摘要 / 链接，点击在新标签打开）。结果仅在本对话会话内展示、不持久化，多轮搜索自动去重累加；主界面与小窗各自独立显示，二者均经 `search:results` 广播同步。
  - 后端 `fetchSearchContext` 在检索完成后新增 `broadcast('search:results', { chatType, chatId, query, results })`；preload/ipc 新增 `onSearchResults`；ChatWindow / MiniChat 各增 `searchResults` 状态与折叠渲染（`src/styles/index.css` 新增 `.search-result-bubble` 等样式）。
  - i18n 新增 `chat.webSearchResultTitle` / `chat.webSearchResultToggle`（中英文）。
- **修复：关闭主界面确认框上半部分被遮挡**：确认框原渲染在自定义标题栏（`.custom-titlebar`）内部，而该元素带 `backdrop-filter` 会建立 `position: fixed` 的 containing block，导致全屏遮罩被限制在标题栏 44px 盒内、居中后上半部分溢出窗口不可见。改为用 React `createPortal` 渲染到 `document.body`，脱离标题栏的 containing block，确认框完整居中显示。
- 改动文件：electron/main.ts、electron/preload.ts、src/ipc.ts、src/components/ChatWindow.tsx、src/components/MiniChat.tsx、src/components/CustomTitleBar.tsx、src/i18n/translations.ts、src/styles/index.css。构建 tsc(backend)+vite 0 错通过。

## v2.2.0 (2026-08-16)

### 异步场景生图 + 联网搜索 + 插件系统（三大新功能）
- **异步场景生图（每个对话独立控制、主窗/小窗同步）**：AI 在后台依据对话场景自动生成场景配图，生图指令不出现在聊天界面（消息 `content` 为空，仅保留 `genPrompt` 供右键「查看提示词」）。主界面与小窗共用同一份每对话开关（`autoSceneImageChats[chatKey]`），经 `settings:changed` 双向同步，**不会出现「主界面关、小窗还开」**；同一聊天同时只生一次图，主窗/小窗通过 `broadcast('stream:user')` 实时同步同一张图。
  - 设置项（设置 → 异步场景生图）：「两次生图最小间隔（秒）」滑块（10~600，默认 120）、「场景判定方式」下拉（AI 判定 / 关键词启发式）。
  - 聊天界面工具栏新增 🖼️ 开关：每对话单独开启/关闭。
  - 硬编码告知：默认 `sceneImageIntervalSec = 120`、`sceneImageJudge = 'llm'`（`src/types.ts` 的 `DEFAULT_SETTINGS`）；节流公式 `interval = clamp(5..3600, settings.sceneImageIntervalSec) × 1000 ms`，键 `lastSceneImageAt[chatKey]`。
- **联网搜索（类 DeepSeek，每个对话独立控制）**：回答前先检索资料作为参考上下文注入 AI prompt（声明式注入，无需工具调用）。默认搜索源 **DuckDuckGo HTML（免费、无需 Key）**；另支持 Tavily / Bing / SerpAPI（需 Key）。聊天界面工具栏新增 🌐 开关（搜索中显示 ⏳ +「联网搜索中…」），每对话单独开启/关闭。
  - 设置项（设置 → 联网搜索）：搜索源下拉、搜索 API Key。
  - 硬编码告知：默认 `searchProvider = 'duckduckgo'`、`searchApiKey = ''`（`DEFAULT_SETTINGS`）；仅 DuckDuckGo 免费无需 Key，其余三家需填写 Key。
- **插件系统（声明式 / 受控 HTTP，兼容外部常见格式）**：导入外部插件，自动识别为 念语原生清单 / OpenAI `ai-plugin.json` / SillyTavern / NovelAI 世界书 / 角色卡 / 提示词规则，统一落为声明式 `Plugin` 记录。启用后其提示词片段注入对话。提供「已导入插件」列表，可启停 / 删除。
  - **安全边界**：默认**不执行任何 JS**；仅当全局「允许插件执行 JS」（`pluginAllowJs`，默认关）开启且插件带 `jsEntry` 时才在受限上下文加载——该开关带高危红色警示，等同于授予任意代码执行权限，仅信任来源时开启。
  - 受控 HTTP 工具（`plugin:callTool`）只发送预设请求，绝不执行任意代码。
  - 改动文件：electron/main.ts（judgeSceneImage / triggerSceneImage / searchWeb / fetchSearchContext / importPluginLogic 扩展 / 新 IPC）、electron/db.ts（Plugin 存储）、electron/preload.ts、src/ipc.ts、src/types.ts、src/components/Settings.tsx（三处新设置区 + 插件管理）、src/components/ChatWindow.tsx / MiniChat.tsx（每对话开关 + 搜索状态）、src/i18n/translations.ts。后端 tsc 0 错通过。

## v2.1.0 (2026-08-15)

### 心情平滑过渡系统 + 朋友圈敏感程度判定
- **心情平滑过渡（防忽喜忽悲）**：新增底层连续情绪值 `moodValue`（−100~100）与 `MOOD_VALENCE` 心情→数值映射、`valenceToMood` 分段反推。`applyMoodChange` 以「心情过渡指数」`moodSmoothing`（0~1，默认 0.5）为步进系数，把 `moodValue` 朝 AI 判定的目标心情平滑移动，再反推出显示的离散心情词。AI 每轮判定的 `requestMoodJudge` 与随机事件选定的心情 `handleChooseEvent` 现在都走平滑写入，不再瞬跳。
  - 硬编码告知：默认 `moodSmoothing = 0.5`（`src/types.ts` 的 `DEFAULT_SETTINGS`）；`MOOD_VALENCE` 映射与 `valenceToMood` 分段阈值在 `electron/main.ts`；公式 `newVal = round(old + moodSmoothing × (target − old))`。
- **朋友圈敏感程度判定（防普通唠嗑发圈）**：`judgeAndPostMoments` 的 prompt 新增 `shareUrge`（0~100，AI 给出的「此刻想发朋友圈的强烈程度」），并把角色当前心情注入 prompt（聊到尽兴才更想分享）。新增「朋友圈敏感程度」`momentsSensitivity`（0~1，默认 0.5）：阈值 `= (1 − momentsSensitivity) × 100`，仅当 `postMoments===true` 且 `shareUrge ≥ 阈值` 时才真正发朋友圈。敏感程度高→阈值低→稍聊到尽兴就发；敏感程度低→阈值高→只有极度想发才发，普通唠嗑几乎不发。旧模型未返回 `shareUrge` 时视为 100（向后兼容）。
  - 硬编码告知：默认 `momentsSensitivity = 0.5`（`src/types.ts` 的 `DEFAULT_SETTINGS`）；阈值换算 `threshold = round((1 − momentsSensitivity) × 100)` 在 `electron/main.ts`。
- 新增设置项（设置 → 情绪与事件（高级）/ 朋友圈）：「心情过渡指数」「朋友圈敏感程度」滑块（中英文 i18n 同步）。
- 改动文件：electron/main.ts、src/types.ts、src/components/Settings.tsx、src/i18n/translations.ts。构建 tsc(backend)+vite 0 错通过。

## v1beta-a41 (2026-08-09)

### 全局单人物每日上限选择器：增加「20 条」与「无限」
- 朋友圈界面「单人物每日上限（全局默认）」下拉在原 1/3/5/10 基础上新增 `20` 与 `无限` 两项。
- 「无限」以哨兵值 `0` 写入全局 `dailyMomentLimit`；`main.ts` 的 `resolvedLimit` 增加分支：`settings.dailyMomentLimit === 0 → Infinity`。与角色层 `0 = 用全局` 语义一致（角色未单独设置时继承全局，全局无限则角色也无限）。`DAILY_MOMENT_LIMIT = 5` 仍仅作为字段缺失（undefined）时的兜底。
- 新增 i18n 键 `moments.unlimited`（中「无限」/英「Unlimited」）；`Settings.tsx` 的每日上限滑块标签在值为 0 时显示「无限」，避免误导（滑块本身 1~20 不变，无限以 MomentsView 下拉为权威入口）。
- 改动文件：electron/main.ts（resolvedLimit）、src/components/MomentsView.tsx（下拉选项）、src/components/Settings.tsx（标签）、src/i18n/translations.ts。构建 tsc+vite 0 错通过。

## v1beta-a40 (2026-08-09)

### 朋友圈 AI 判定与关系值判定解耦 + 亲密度直采 + 全局单人物每日上限选择器
- 朋友圈 AI 判定与关系值 AI 判定彻底解耦：`requestRelationshipAndMoments` 拆分为两个独立 prompt/AI 调用——`judgeRelationship`（仅关系值/关系类别）与 `judgeAndPostMoments`（仅朋友圈、由 AI 的 `postMoments` 布尔动态决定）。两者互不影响；关系值界面的「重新判定关系」不再连带发朋友圈，朋友圈板块的「让 AI 生成」也不再改动关系值。
- 亲密度直接采用 AI 给出的关系值：去掉原 `±20` 增量阻尼，关系值 `bond = round(亲密程度 × 10)`（维持 0~1000 展示语义）直接落地；重新判定即反映 AI 当前评估，不再逐次 +20 爬升。
- 朋友圈界面新增「单人物每日上限（全局默认）」下拉（1/3/5/10 条）：写入全局 `dailyMomentLimit`，即时保存。优先级为「角色编辑里填了单人物上限 → 以角色为准；否则用全局」。手动触发发朋友圈不受此上限约束。
- `triggerRelationship` 接口新增第 5 参 `doRelationship`（与 `withMoments` 正交）；`BondPanel` 只判定关系、`MomentsView` 只发朋友圈。
- 改动文件：electron/main.ts、electron/preload.ts、src/ipc.ts、src/components/MomentsView.tsx、src/components/BondPanel.tsx（调用）、src/components/Settings.tsx（滑块 min=1，去 0=不限制）、src/i18n/translations.ts。构建 tsc+vite 0 错通过。

## v1beta-a39 (2026-08-09)

### 朋友圈：AI 动态判定场景 + 单人物每日上限
- AI 动态判定发朋友圈（替代「每条回复都触发」的硬编码节奏）：`requestRelationshipAndMoments` 的 prompt 新增 `postMoments` 布尔决策字段，要求模型只在对话出现值得分享的内容（真实情绪起伏/重要事件/关系进展/有趣的梗等）时为 true，日常寒暄必须为 false；代码侧仅在 `postMoments===true` 时生成朋友圈（旧模型未返回该字段则按 moments 是否有内容兼容处理）。发朋友圈频率完全由 AI 按场景决定，不再硬编码。
- 单人物每日上限：`Role` 新增 `momentDailyLimit` 字段（角色编辑可填，留空用全局 `dailyMomentLimit` 兜底，0=不限制）。自动发朋友圈按「发布者角色」统计当天已发数，达上限即暂停；手动触发（`MomentsView`「让 AI 生成」，即 `force=true`）不受此上限约束。
- 角色编辑 `RoleEditor.tsx` 好感度段下方新增「朋友圈每日上限（单人物）」数字输入 + 提示；`Field` 组件新增可选 `hint` 渲染；`translations.ts` 加 `role.momentDailyLimit` 中英文标签/提示/占位符。
- 改动文件：electron/main.ts、src/types.ts、src/components/RoleEditor.tsx、src/i18n/translations.ts、src/styles/index.css。构建 tsc+vite 0 错通过。

## v1beta-a38 (2026-08-09)

### 修复（关系值界面与朋友圈解耦）
- 关系值界面「让 AI 重新判定关系」按钮，此前会连带自动生成朋友圈（两件事被打包在同一函数 `requestRelationshipAndMoments` 中）。现拆分为独立行为：
  - 关系值界面手动判定：只更新关系值/关系类别，不再发朋友圈（`relationship:trigger` 传 `withMoments=false`）。
  - 朋友圈板块「让 AI 生成」按钮：仍按设置自动发朋友圈（`withMoments=true`）。
  - 聊天过程中 AI 自动判定关系并自动发朋友圈的 4 处自动触发行为不变（默认 `includeMoments=true`）。
- `electron/main.ts` 的 `requestRelationshipAndMoments` 新增 `includeMoments` 参数；`preload.ts`/`src/ipc.ts` 的 `triggerRelationship` 新增可选 `withMoments` 参数；按钮文案由「让 AI 分析并生成」改为「让 AI 重新判定关系」。

## v1beta-a37 (2026-08-09)

### 新增（生图 / TTS / ASR 设置「刷新模型列表」）
- 生图专用 API、TTS 专用 API、ASR 专用 API 三处设置各新增「刷新模型列表」按钮。
- 点击后向对应 Base URL 请求 OpenAI 兼容的 `/models` 接口，按类型关键字过滤（生图含 image/dall-e、TTS 含 tts/speech、ASR 含 whisper/transcrib/audio）后填入模型名下拉；过滤结果为空时回退全部模型，避免第三方平台命名不标准时漏掉可用模型。
- 模型名仍支持手填：输入框关联 datalist，既能从下拉选也能手动输入平台文档中的准确模型名。
- 复用聊天模型配置的 `listModels` 逻辑（`electron/ai.ts`），与「模型配置中心」完全独立，互不干扰。

## v1beta-a36 (2026-07-28)

### 修复（多窗口光标不显示 / 双光标 / 找不到光标）
- **现象**：小窗与主界面重叠且小窗在上时，鼠标移出小窗后主界面不显示光标（必须点击主界面才显示）；鼠标移出任一窗口后两处都不显示光标，导致任何位置都找不到光标。
- **根因**：上一版用 `window.blur/focus` 事件驱动光标显隐。在 Electron 中主界面与小窗是独立 OS 窗口，鼠标从一个窗口移到另一个（不点击）不改变焦点，被移开的窗口仍停留在 blur 隐藏态；且离开窗口到桌面时缺少 `mouseleave` 监听，显隐逻辑完全依赖焦点，造成「无焦点即无光标」。
- **修复**：
  - **改用「鼠标是否真正位于本窗口内」驱动显隐**：监听 `document` 的 `mousemove` / `mouseleave` / `mouseout(relatedTarget 为空)` 判定鼠标进出本窗口；进入即淡入显示，离开即淡出隐藏。焦点事件（`blur/focus`）降级为兜底，仅当鼠标确实在窗内时才恢复显示。
  - **单光标保证**：任意时刻只有鼠标真正所在的窗口渲染自定义光标，另一个窗口淡出并恢复原生光标；移到桌面时两窗均隐藏、由系统原生光标接管，绝不会出现「哪里都找不到光标」或双光标并存。
  - **丝滑切换保留**：进入窗口 200ms ease-out 淡入、离开 150ms ease-in 淡出，主界面与小窗之间、从桌面拖入 UI 均平滑过渡，不会突然弹出/消失。

### 修复（小窗底部双层边框）
- **根因**：`.mini-shell` 缺少主窗 `.app-root` 同款的 `transform: translateZ(0)` 合成层强制，导致 `border-radius + overflow:hidden` 未能正确裁剪底部子元素 `.mini-composer` 的圆角，在左下/右下角形成双层边框伪影。
- **修复**：为 `.mini-shell` 增加 `transform: translateZ(0)`（与 `.app-root` 一致）；并为 `.mini-titlebar` 增加顶部圆角、`.mini-composer` 增加底部圆角，使子元素边缘与圆角窗口对齐。

## v1beta-a35 (2026-07-28)

### 修复（小窗双层边框）
- **根因**：Electron `BrowserWindow` 已配置 `roundedCorners: true` + `hasShadow: true`（原生圆角边框+阴影），而 CSS `.mini-shell` 又声明了 `border: 1px solid var(--color-border)`，两层边框在窗口四角重叠。
- **修复**：移除 `.mini-shell` 的 `border` 声明，仅保留 OS 原生边框。

### 修复（双窗口双光标 + 丝滑切换动画）
- **现象**：同时打开主界面和快捷聊天小窗时，两个窗口各自渲染 Canvas 光标，出现两个光标并存。切换窗口时光标突然弹出/消失，体验割裂。
- **根因**：每个 Electron 窗口有独立的 `document` 和 `CustomCursor` 实例，blur/focus 时立即显示/隐藏光标，无过渡动画；非聚焦窗口的光标未正确隐藏。
- **修复**：
  - **聚焦窗口独占光标**：失焦时启动淡出动画（150ms ease-in，透明度 1→0），完成后彻底隐藏并恢复原生光标；聚焦时启动淡入动画（200ms ease-out，透明度 0→1），光标从无到有丝滑浮现。
  - **淡入淡出期间保持 rAF 运行**：空闲停帧逻辑检测到正在淡入/淡出时跳过停帧，确保动画完整播放。
  - **globalAlpha 透明度控制**：每帧通过 `ctx.globalAlpha = opacityRef.current` 控制整帧透明度，光标/拖尾/粒子同步淡入淡出。
  - **效果**：从桌面拖入 UI、主界面与小窗之间切换，光标均先微微浮现再完全显示，不会突然生成；切走时平滑消散而非瞬间消失。

## v1beta-a34 (2026-07-28)

### 新增（快捷聊天小窗支持动态光标）
- **小窗动态光标**：快捷聊天小窗（MiniChat）现已挂载与主界面完全一致的动态 Canvas 光标系统，规则统一。
- **同一套设置与渲染引擎**：小窗直接复用 `settings.customCursor` 配置与 `CustomCursor` 组件，总开关、拖尾、粒子、缓动、悬浮放大、空闲保留时长等全部参数与小窗均无差异。
- **原生光标同步隐藏**：开启动态光标后，小窗内同样通过 `body.cursor-hidden` 全局类彻底隐藏系统原生光标，避免动态光标与原生光标并存的割裂感。

## v1beta-a33 (2026-07-28)

### 新增（高性能自定义 Canvas 光标系统）
- **架构**：顶层全屏透明单层 Canvas 渲染光标与全部特效，不生成 DOM 节点、不使用 CSS `cursor: url()`，彻底解决删除对话卡片交互卡死问题。
- **设置面板**：设置 → 主题与外观区域新增「动态 Canvas 光标」总开关，支持切换动态光标/系统原生光标。
- **lerp 缓动跟随**：光标使用线性插值平滑跟随鼠标，灵敏度可调（0.1~0.5），距离远时自动加速跟紧。
- **交互状态检测**：自动检测鼠标下方元素类型，悬浮可交互控件时光标放大（比例可调 1.0~1.5）；拖拽操作时自动切换抓取状态。
- **液态渐变拖尾**（独立开关）：移动时产生渐变透明消散轨迹，点位硬性上限 10 个，禁止模糊运算，采用透明度+缩放实现液态消散；移动越快拖尾越长；静止 200ms 后停止新增点位并逐步清空。
- **流光粒子点缀**（独立开关）：移动时散发柔光粒子，同时存在粒子硬性上限 8 个；静止立刻停止生成，现有粒子自然消亡；简单渐变圆点绘制，无复杂图形或多层发光。
- **失焦/右键菜单保护**：窗口失焦或唤起右键菜单时立刻暂停渲染循环、清空画布、隐藏光标；恢复后自动继续。
- **Canvas 层级控制**：z-index: 9998，低于模态遮罩(10000)，不遮挡弹窗/悬浮小窗/右键菜单。
- **空闲保留时长**（新增设置项）：光标静止后保持可见的时长可调（1~300 秒，最高 5 分钟），超时后自动隐藏动态光标并恢复系统原生光标。

### 修复（v1beta-a33 首发后热修）
- **光标被画成巨幅图像**：原实现按 PNG 原始尺寸（512×512）绘制，导致光标覆盖约 1/4 窗口。改为固定显示尺寸（长边 28px，保持宽高比），与素材原始分辨率解耦。
- **拖尾/粒子不显示**：根因为巨幅光标遮挡了特效，现已随尺寸修复一并恢复；并下调粒子发射速度阈值（0.8）、提高发射概率，使特效在常规移动下更易出现。
- **悬停即光标消失**：原空闲逻辑在停止 rAF 时清空画布且不重绘，导致静止即光标消失。改为停止循环时保留静态光标；仅在超过「空闲保留时长」后才隐藏并恢复原生光标，该时长可在设置中调节（最高 5 分钟）。
- **FPS 降级算法错误**：原帧率计算公式有误，会误判低帧率。改为基于统计窗口的正确测量，避免特效被错误关闭。
- **清晰度与首帧吸附**：画布按 devicePixelRatio 缩放以保证光标清晰；首次移动直接吸附到鼠标位置，避免从离屏长距离缓动。

### 性能保障（硬性规则）
| 规则 | 说明 |
| --- | --- |
| 空闲停帧 | 静止 >300ms 停止 requestAnimationFrame 循环 |
| FPS 自动降级 | 实时监测帧率，<52fps 关闭粒子 → 减少拖尾 |
| 对象池复用 | 粒子(8)和轨迹点(10)预分配，运行时不新建/销毁 |
| 脏区域重绘 | 仅清除变化的区域，避免全屏 clearRect |
| 零高开销滤镜 | 禁止多层叠加滤镜/径向模糊等操作 |

## v1beta-a32 (2026-07-28)

### 修复（右键菜单 / 文字选中弹窗定位偏移）
- **根因**：最外层 `.app-root` 带有 `transform: translateZ(0)`（外加 `backdrop-filter`）以创建独立合成层、配合窗口圆角裁剪滚动条。按 CSS 规范，该元素成为 `position: fixed` 后代的包含块，导致气泡右键菜单（`.ctx-menu`）与文字选中记忆弹窗（`.sel-popup`）虽用 `fixed` + 视口坐标 `e.clientX/clientY`，实际却相对 `.app-root` 定位，从而出现在远离光标的错误位置。
- **修复**：`ChatWindow` 与 `MiniChat` 的消息气泡右键菜单、文字选中弹窗改用 React Portal 渲染到 `document.body`，彻底脱离 `.app-root` 的 transform/filter 包含块；`fixed` + `clientX/clientY` 现在精确对应真实光标位置。
- 顺带增加视口边界 clamp（菜单宽高预估），靠近视口右/下边缘时自动内收，避免菜单溢出被裁切。

## v1beta-a31 (2026-07-28)

### 修复（删除角色/模型配置后输入框锁死）
- **根因**：删除操作使用原生 `window.confirm` / `dialog.showMessageBox`，原生确认框关闭后会破坏 Chromium 渲染器输入框的焦点路由，键击无法进入任何输入框，直到窗口失去并重新获得 OS 焦点才恢复（即用户手动切到小窗再切回的现象）。故删除人物或模型配置后主界面输入框锁死，多次点击无效，切小窗即恢复。
- **核心修复**：`electron/main.ts` 的 `app:confirm` 处理在原生对话框关闭后主动 `mainWindow.focus()` + `mainWindow.webContents.focus()`，等价于一次真实 OS 窗口焦点切换，从根源重置焦点路由。
- **统一通道**：`RoleList` / `Settings` / `SelfRoleSettings` / `ChatList` / `ChatWindow` / `MiniChat` / `OnboardingWizard` 中所有 `window.confirm` 删除/确认逻辑改为 `await api.showConfirm(...)`，统一走已修复的 IPC 通道，彻底消除该 bug 类。
- 角色/模型/自我身份删除完成后额外派发 `nianyu:restore-focus`，主窗与小窗监听到后立刻把焦点还给输入框，无需任何额外点击或切窗。

## v1beta-a30 (2026-07-28)

### 修复 / 优化（随机事件弹窗遮罩）
- **遮罩误触**：`.event-overlay` 由 `pointer-events: none` 改回 `auto`，弹窗外点击不再穿透到下层聊天（杜绝误选文字/误点消息/误发）。遮罩为普通 div 无 autoFocus，不会偷走焦点；点击至多 blur 到 body，由关闭时焦点归还 + a29 键盘安全网自动恢复。
- **关闭时焦点归还**：随机事件弹窗卸载时派发 `nianyu:restore-focus` 事件，主窗口与快捷小窗监听后在无其他模态/遮罩打开的前提下立即把焦点还给输入框，无需手动点击或敲键触发。

## v1beta-a29 (2026-07-28)

### 修复
- **小窗崩溃（failed is not defined）**：`MiniMessageRow` 组件在主组件外部定义，闭包中无法访问 `failed` 状态变量，导致渲染时抛 ReferenceError 小窗完全无法打开。已将 `failed` 作为 prop 传入 `MiniMessageRow`。
- **输入冻结（键盘安全网）**：新增全局 keyboard capture 阶段监听，当用户敲击可打印键时若焦点不在任何输入框/编辑区，自动将焦点归还聊天输入框并重新派发按键事件（首字不丢失）。覆盖复制角色后切聊、通知弹窗偷焦、随机事件遮罩残留等所有偶发焦点丢失场景。主窗口与快捷小窗均已加装。

## v1beta-a28 (2026-07-28)

### 优化（微信风格图片气泡 + 多图合并）
- **图片去绿底**：发送/接收的图片消息不再带主题色气泡背景，仅显示图片本身（圆角 8px），与微信一致；含文字的图片消息，文字保留气泡、图片在气泡下方以图集呈现。
- **多图合并为一条消息**：一次选多张发送时，所有图片合并为同一条消息的图集（1/2/3/4/≥5 张自适应网格），不再拆成多条独立消息。
- 数据层新增 `images` 多图数组字段（`image_path` 保留兼容旧单图数据）；发送、落库、聊天列表反推、上下文历史拼接均同步支持多图。

> 最后更新: 2026-07-28

## v1beta-a27 (2026-07-28)

### 新功能
- **图片发送预览窗**：点击图片按钮选择本地图片后，不再立即发送，而是在输入框正上方展示缩略图预览条（仿微信/豆包），每张图右上角带「✕」可单独移除；可一次选多张，发送时第一张（或文字）触发 AI 回复，其余图片作为独立消息依次发出。主窗口与快捷小窗均支持。

### 修复（输入框偶发无法输入）
- **根因一**：随机事件全屏遮罩 `event-overlay` 默认拦截整窗指针事件，导致聊天框与角色编辑页等全局输入框被冻结；已改为遮罩层不拦截、仅弹窗本体接收交互。
- **根因二**：后台消息通知卡片窗口（透明 + 置顶）会窃取主窗口操作系统键盘焦点，造成「突然打不了字、发张图片就恢复」；已将通知窗口设为不可聚焦（`focusable:false`），从根本上杜绝偷焦点。
- 额外加固：消息更新后的自动聚焦不再抢走其他输入框/编辑页的焦点；主窗口与小窗在重新获得系统焦点时，若当前无编辑元素获焦且无事件遮罩，自动把焦点还给输入框，无需再靠发图片或点刷新恢复。

> 最后更新: 2026-07-28

## v1beta-a26 (2026-07-28)

### 修复（严重数据一致性 bug）

- **删除正在生成的聊天会产生幽灵会话 / 回复串台**：
  - 根因 1：删除聊天时，该聊天仍在后台流式生成（主进程 `handleStream` / 群聊接话 `handleGroupContinue` / 空闲主动回复 `handleProactive`），这些孤儿流在删除后继续把 AI 回复写回已删除的聊天 id。由于会话列表由消息反推（`getChatList` 扫描 `messages`），被删除群组的 id 重新出现为「0 人、群名显示一串数字、发消息无反应」的幽灵群。
  - 根因 2：前端按 `streamId.startsWith(\`${chatId}:\`)` 判断 chunk 归属，当某个 chatId 是另一个的字符串前缀时（如单聊角色 id 为 `1` 与 `12`），删除 `12` 时其回复会串到 `1` 的聊天。
  - 修复：主进程新增流式生成控制器注册表，删除聊天（`chats:delete`）时立即 `abort` 该聊天的全部进行中生成，孤儿流不再写库；前端 `belongs` 改为按最后一个 `:` 精确解析 chatId 比较；`getChatList` 跳过群组/角色已删除但残留消息的条目，从根上杜绝幽灵会话。
- **删除聊天后输入框卡死**：上述孤儿流/串台会让某聊天的流式消息永远不结束（或卡在「生成中」），导致输入框被 `replying` 状态禁用。修复后删除即中止生成，不再残留卡死状态。

## v1beta-a25 (2026-07-28)

### 修复

- **通知卡片点击 × 误开主界面**：原实现仅靠 `stopPropagation` 阻止叉号点击冒泡到卡片，但叉号仅 18px 且位于卡片右上角外缘，极易点偏命中卡片主体而触发「打开主界面」；`stopPropagation` 防御也不可靠。已加固：卡片主体点击增加硬防护（落在关闭按钮区域内一律不打开主界面），并放大叉号命中区（22px + `::after` 外扩 10px 透明热区），close 点击增加 `preventDefault` + `mousedown` 拦截。点击 × 现在稳定仅关闭卡片、不进入主界面。

## v1beta-a24 (2026-07-28)

### 改进

- **通知卡片主题自适应**：卡片不再固定深色样式，改为自动跟随系统主题配色。浅色主题（经典微信、活力多彩、青蓝、松绿、霜雪、玫瑰）显示浅色卡片（白底 + 柔和阴影 + 深色文字），深色主题（极简暗色、毛玻璃、星河、赛博、烬燃）显示深色卡片。同时修复了原卡片过重的黑色阴影（`rgba(0,0,0,.55)` → 柔和分层阴影），视觉更精致。切换主题后下次弹出即时适配。
- **最小化也触发通知卡片**：原先仅当主窗/小窗完全隐藏时才弹出卡片，现在窗口最小化到任务栏也会触发（只要用户不在操作任一窗口即视为后台，弹出提醒）。

### 修改文件

- `src/notify.ts`：重写卡片样式为双主题 CSS 生成器（`cardCSS`），新增 `applyTheme` 动态切换逻辑。
- `electron/main.ts`：`showNotifyCard` 可见性判断增加 `isMinimized()`；`processNotifyQueue` 向渲染进程传递当前 `theme`。

## v1beta-a23 (2026-07-28)

### 修复

- **关闭「随机事件」后仍然自动弹出**：原实现里，流式对话完成后触发随机事件的回调在窗口挂载时注册，闭包捕获了开关初始值（开启），设置变更不会让监听器重新注册，导致在设置中关闭随机事件后开关形同虚设、对话结束仍按概率弹出。已通过 ref 持有最新开关值，关闭后对所有已打开窗口即时生效（主窗口与快捷小窗均修复）。手动点击 🎲 按钮触发不受影响。

## v1beta-a22 (2026-07-28)

### 新增功能

- **点击音效覆盖全部交互场景**：原先仅按钮 / 带 `role="button"` 的元素点击发声。现扩展为更通用的可交互选择器，左侧栏导航（通讯录、统计、资源库、设置等 `nav-item`）、列表项、卡片、标签（Tab）、开关（Switch/Toggle）、链接、复选框 / 单选框等任意可点击元素均会播放 UI 点击音；点击纯空白容器（div/section/span 等无交互元素）仍不发声，即「空白界面的无效点击」不触发。
- **静默模式（托盘右键菜单）**：系统托盘右键菜单新增「静默模式」勾选项。
  - 开启后：**暂停后台消息卡片通知**（软件后台收到 AI 新消息不再弹出 Steam 风格卡片）且**关闭消息提示音**（含角色专属消息音效）。
  - 开启后：**UI 点击音、报错弹窗音等其余音效仍正常播放**（静默仅作用于「消息提示音」一类）。
  - 该模式为持久化设置（写入 `settings.json` 的 `silent` 字段），重启后保持；勾选状态实时显示在托盘菜单中。
  - 与「设置 → 音效 → 启用音效」总开关相互独立：总开关关掉则所有音效（含点击 / 报错）都不响；静默模式仅屏蔽消息提示音与卡片，不影响点击 / 报错声。

### 修改文件

- `src/utils/sound.ts`：`SoundSettings` 增加 `silent`；`playSound` 在 `notification` 且静默时（非试听）提前返回；`installGlobalSoundListeners` 选择器扩展到 `.nav-item` / 列表 / 卡片 / 标签 / 开关等。
- `src/components/Sidebar.tsx`：导航项 `nav-item` 增加 `role="button"`（可访问性兜底）。
- `electron/main.ts`：`TRAY_LABELS` 增加 `silent` 双语；`buildTrayMenu` 增加 checkbox 菜单项，点击经 `toggleSilentMode` 保存并重建菜单、广播 `settings:changed`；`showNotifyCard` 在静默时直接返回。
- `src/notify.ts`：监听 `settings:changed` 调用 `invalidateSoundCache`，使静默切换后提示音即时生效。
- `src/types.ts`：`AppSettings` 与 `DEFAULT_SETTINGS` 增加 `silent: boolean`（默认 false）。
- `使用说明.md` 已同步「§27 音效 · 静默模式」与点击音效覆盖范围说明。
- 构建通过

---

## v1beta-a21 (2026-07-27)

### 问题修复
- **音效完全不响**：修复两处根因——① 内置音效路径在生产构建（`loadFile` 加载 `dist/index.html`）下被解析为 `file:///sounds/...` 导致文件不存在；改为相对路径 `sounds/...`，同时兼容开发与生产环境。② 后台通知卡片、报错弹窗等无用户手势触发的音效被 Chromium 自动播放策略拦截；三个窗口均加入 `autoplayPolicy: 'no-user-gesture-required'`。

### 新增功能
- **音效自定义（全局）**：「设置 → 音效」中为「报错弹窗音效 / UI 点击音效 / 消息提示音效」三类分别支持选择本地 **MP3 / WAV** 文件替换，并提供「试听」「恢复默认」。自定义文件保存在用户数据目录 `userData/custom-sounds/`。
- **角色消息音效（按人物）**：角色编辑页新增「消息音效」字段，可为单个角色指定专属消息提示音（MP3 / WAV）。该角色设置后仅对其自身生效，全局消息提示音的自定义不作用于它；留空则沿用全局。后台收到该角色消息时优先使用其专属音效。

### 文档
- **使用说明.md**：「§27 音效」补充「自定义音效（全局）」与「角色自定义消息音效」说明。

> 说明：用户要求「版本备份中的使用指南」也加 `/v1` 红字提醒。依据项目规则，归档版本（`Chiyo 念语 v1beta-a1` ~ `a6` 等 `workbuddy` 目录之外的副本）为只读归档，严禁改动，故仅本活副本已更新。如需对某个具体备份版本单独处理，请明确指定并确认放开只读约束。

## v1beta-a20 (2026-07-27)

### 功能与体验调整
- **回到底部按钮位置整改**：按钮从消息区内部移至输入框容器上方，固定在输入框中轴线上方，聊天不在最底部时以线性动画弹出；主窗口与快捷聊天小窗行为一致。
- **音效系统集成**：新增 UI 点击音、报错弹窗音、新消息提示音三类音效（音频位于 `public/sounds/`）。设置页新增「音效」分区，可开关音效并调节音量。后台消息通知卡片滑入时播放提示音。
- **软件作者信息**：统一改为「前方非Chiyo」（package.json author 字段与关于弹窗）。
- **软件图标**：窗口 / 任务栏 / 托盘图标统一改用项目根目录 ICO 文件（`k0z5e-rk3ah-001.ico`）。

### 问题修复
- **删除会话后刷新按钮无响应**：删除单聊 / 群聊后，「刷新界面」按钮（光标为手指状）点击无反应。已修复——刷新时重置界面状态、重建并可靠聚焦输入框、弹出「界面已刷新」提示。

### 文档
- **使用说明.md**：
  - 所有使用指南（含模型配置章节）新增红字提醒：OpenAI 兼容 / 自定义 Base URL 必须以 `/v1` 结尾。
  - 新增「§27 音效」章节说明三类音效与设置入口；「回到底部」「刷新界面」章节同步更新。
- 新增本版本更新记录。

> 说明：用户要求「版本备份中的使用指南」也加 `/v1` 红字提醒。依据项目规则，归档版本（`Chiyo 念语 v1beta-a1` ~ `a6` 等 `workbuddy` 目录之外的副本）为只读归档，严禁改动，故仅本活副本已更新。如需对某个具体备份版本单独处理，请明确指定并确认放开只读约束。
