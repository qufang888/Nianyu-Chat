# 念语 (Nianyu) 使用说明

> 版本 1.0.0 | 作者: 前方 | 一个 AI 数字人聊天桌面客户端

---

## 目录

1. [简介](#1-简介)
2. [系统要求与安装](#2-系统要求与安装)
3. [启动与首次设置](#3-启动与首次设置)
4. [界面概览](#4-界面概览)
5. [模型配置](#5-模型配置)
6. [角色管理](#6-角色管理)
7. [聊天系统](#7-聊天系统)
8. [群聊与智能体互聊](#8-群聊与智能体互聊)
9. [好感度与情绪系统](#9-好感度与情绪系统)
10. [语音功能 (ASR/TTS)](#10-语音功能-asrtts)
11. [快捷聊天小窗 (MiniChat)](#11-快捷聊天小窗-minichat)
12. [世界书](#12-世界书)
13. [规则库](#13-规则库)
14. [记忆系统](#14-记忆系统)
15. [插件导入](#15-插件导入)
16. [随机事件](#16-随机事件)
17. [观察者模式（对局）](#17-观察者模式对局)
18. [空闲主动回复](#18-空闲主动回复)
19. [自我身份（用户角色卡）](#19-自我身份用户角色卡)
20. [主题与外观](#20-主题与外观)
21. [备份与还原](#21-备份与还原)
22. [快捷键与窗口控制](#22-快捷键与窗口控制)
23. [数据存储位置](#23-数据存储位置)
24. [错误自诊](#24-错误自诊)
25. [常见问题](#25-常见问题)
26. [一键恢复初始设置](#27-一键恢复初始设置)
27. [异步场景生图](#异步场景生图)
28. [联网搜索](#联网搜索)
29. [桌面悬浮球](#桌面悬浮球)
30. [设置搜索框](#设置搜索框)
31. [内置使用指南](#内置使用指南)
32. [TTS 按角色音色](#tts-按角色音色)
33. [ASR 语音输入修复](#asr-语音输入修复)
34. [单实例与开屏动画](#单实例与开屏动画)
35. [错误日志](#错误日志)
36. [主动消息与未读清单修复](#主动消息与未读清单修复v237)
37. [统计界面](#统计界面)

---

## 1. 简介

**念语** 是一个桌面 AI 数字人聊天客户端。你可以创建多个 AI 角色（数字人），与它们进行一对一或群组聊天。每个角色可以有独立的性格、背景、记忆、情绪和好感度，带来沉浸式的交互体验。

核心特性：

- 支持多种 AI 模型（OpenAI、DeepSeek、Anthropic、兼容 OpenAI 格式的任意 API）
- 单聊与群聊，群聊支持导演调度和轮询调度
- 流式输出与并行对话
- 语音识别输入（ASR）与语音合成播报（TTS）
- 快捷迷你聊天小窗
- 世界书、规则库、记忆系统、插件导入四大扩展能力
- 好感度、情绪、随机事件系统
- 观察者模式（对局）
- 空闲主动回复
- 多主题（14 套）、自定义字体/圆角
- 数据本地存储，支持一键备份与还原

---

## 2. 系统要求与安装

### 系统要求

- 操作系统: Windows 10/11
- 运行环境: Node.js 18+
- 磁盘空间: 至少 200MB（不含下载的模型数据）
- 网络要求: 需要互联网连接以调用 AI API

### 安装步骤

1. 确保已安装 Node.js（推荐 v18 或更高版本）
2. 将整个程序目录解压到本地（如 `D:\念语`）
3. 双击 `启动念语.vbs`（无 CMD 黑窗，推荐日常使用）
4. 首次启动会自动执行 `npm run build && npm start`

> **注意**: 如果电脑上未安装 Node.js，请先访问 <https://nodejs.org> 下载安装。  
> **启动方式区别**：
>
> - `启动念语.vbs`：后台静默启动，不弹出 CMD 命令行窗口，体验更接近正式软件。
> - `启动念语.bat`：显示构建/运行日志，适合排错时使用。

### 卸载与数据清除

通过 Windows「设置 → 应用」或控制面板卸载念语时，卸载程序会先弹出一个**数据清除确认页**：

- **默认不勾选**：仅移除程序本体，你的聊天记录、记忆、设置、自定义音效等使用数据会保留（`%APPDATA%\nianyu-client`、`%LOCALAPPDATA%\nianyu-client`，以及默认数据目录 `文档\念语数据`），日后重装或手动清理即可。
- **勾选「删除所有使用数据（不可恢复）」**：卸载的同时**彻底删除**上述全部数据目录（含自定义数据路径），操作不可恢复，请谨慎选择。

> ⚠️ **v2.3.0 修复**：早期版本卸载程序的「删除所有数据」因进程名错配（`taskkill` 误用 `nianyu-client.exe`，真实 exe 为 `念语.exe`）未能杀掉进程、数据目录被锁导致删除静默失败；v2.3.0 已修正进程名。>   
> ⚠️ **v2.3.2 修复**：v2.3.1 及更早版本默认数据目录已迁移到 `文档\念语数据`（不再存于 AppData），但卸载脚本只删 AppData 与自定义路径、**漏删默认目录**，导致「勾选删除数据但数据仍保留」。v2.3.2 起卸载脚本会一并删除 `文档\念语数据`（`$DOCUMENTS` 自动适配系统语言）。
> ⚠️ **v2.3.10 修复**：v2.3.2 后「删除所有数据」的删除逻辑写在独立段 `Section "-postuninstall"`，该段位于 electron-builder 内置卸载段（`Section "un.${UNINSTALL_SECTION_NAME}"`，即 instfiles 页）**内部**、早于自定义数据清除确认页执行；自定义确认页排在脚本最末，段执行时复选框 `$deleteAppDataChecked` 状态尚未就绪（恒为 0），导致勾选彻底无效。v2.3.10 将删除逻辑移入 `Function .onUninstSuccess`（全部卸载页含自定义确认页执行完毕后触发，此时复选框状态已确定），彻底修复；保留空 `Section "-postuninstall" SectionEnd` 维持脚本结构。

> 程序数据目录名取自 `package.json` 的 `name` 字段（`nianyu-client`），与显示名「念语」不同，手动清理时请注意。

---

## 3. 启动与首次设置

### 启动方式

- 日常启动：双击项目根目录的 `启动念语.vbs`（无 CMD 黑窗）
- 查看日志/排错：双击 `启动念语.bat`
- 该批处理会自动执行 `npm run build` 构建前端 → `npm start` 启动 Electron 窗口

### 首次启动向导

第一次运行时会弹出设置向导（Onboarding）：

1. **添加模型**: 至少添加一个 AI 模型配置（详见[模型配置](#5-模型配置)）
2. **创建角色**: 创建你的第一个 AI 数字人（详见[角色管理](#6-角色管理)）
3. **选择主题**: 选择你喜欢的界面主题

完成这三步后，即可开始使用。

### 关于窗口关闭行为

关闭窗口（点击 X）**默认是最小化到系统托盘**，不会退出程序。

- 如需真正退出，请在系统托盘中右键点击图标 → 选择「退出」
- 修改代码重新构建后，必须从托盘退出 → 重新启动，否则加载的是旧代码

---

## 4. 界面概览

主界面分为以下区域：

### 左侧导航栏（Sidebar）

| 图标   | 名称  | 功能              |
| ---- | --- | --------------- |
| 💬   | 聊天  | 聊天列表和对话面板       |
| 👥   | 通讯录 | 管理角色和群组         |
| ⚙️   | 设置  | 所有应用设置          |
| 📊   | 统计  | Token 消耗和角色统计   |
| 📚   | 资源库 | 世界书、规则库、记忆、插件导入 |
| 中/EN | 语言  | 切换界面语言          |

### 聊天视图

- **左栏**: 聊天列表（显示所有单聊和群聊会话）
- **右栏**: 对话面板（显示聊天消息和输入区域）

### 设置视图

设置页左侧为分类导航（sticky 固定），右侧为具体设置内容并随滚动高亮当前分类：点左侧分类名可平滑滚动到对应分区；右侧滚动时导航自动高亮所在分类。共 6 个分类：

- **通用与 AI 行为**：API 密钥、模型、聊天设置（流式输出、推理链等）、AI 行为、随机事件、情绪系统、空闲回复等
- **模型与群聊**：模型管理、群聊互聊（调度 / 自动接话 / 并发）等
- **主题与外观**：主题、字体、圆角、动效、聊天背景、输入框外观、毛玻璃背景（仅毛玻璃 / 磨砂主题显示）、动态光标等
- **生成与扩展**：生图、生视频、朋友圈、世界书与记忆、自我身份等
- **翻译与音效**：翻译、语音（ASR / TTS）、音效等
- **窗口与数据**：快捷小窗、窗口缩放、数据路径、备份与还原、错误日志，以及「关于念语」入口（原左侧菜单栏的「关于念语」已移入此处）

> 说明：原左侧导航栏底部的「关于念语」按钮已移入「设置 → 窗口与数据」分类末尾；原左侧导航栏的「刷新界面」按钮经核查无任何监听、按下无响应，已移除（聊天界面「⋯」菜单内的「🔄 刷新界面」为独立可用按钮，保留，见 §5 聊天系统）。

---

## 5. 模型配置

模型配置是念语的核心依赖。每个角色在回复时需要绑定一个模型配置。

### 添加模型

进入设置 → 模型管理 → 点击「添加模型」：

| 字段               | 说明             | 示例                                              |
| ---------------- | -------------- | ----------------------------------------------- |
| 显示名称             | 便于识别的名称        | GPT-4o, DeepSeek V3                             |
| 提供商              | 选择 API 提供商     | OpenAI / DeepSeek / Anthropic / 本地模型 / 自定义 / OpenAI 兼容 |
| Base URL         | API 请求地址       | `https://api.openai.com/v1`                     |
| API Key          | 认证密钥           | `sk-xxxxxxxx`                                   |
| 模型 ID            | 实际的模型标识符       | `gpt-4o`, `deepseek-chat`                       |
| 最大上下文长度          | 模型的最大 token 上限 | 4096 ~ 128000                                   |
| 温度 (Temperature) | 回复随机性 (0-2)    | 0.7                                             |
| 启用/禁用            | 控制模型是否可用       | 勾选                                              |

> <span style="color:rgb(222, 60, 54)">**重要提醒**：若选择「OpenAI 兼容」或自定义 Base URL，地址必须以 `/v1` 结尾（例如 `https://api.example.com/v1`）。缺少 `/v1` 会导致连接测试失败或请求报错。</span>

### 本地部署模型（v2.3.11 新增）

支持接入本地部署的**文字聊天模型**（Ollama / LM Studio / vLLM 等任何 OpenAI 兼容本地服务）：

- **提供商选「本地模型」**：默认 Base URL 为 Ollama 的 `http://localhost:11434/v1`，可按你的服务修改：
  - Ollama：`http://localhost:11434/v1`（需 Ollama 0.1.24+，自带 OpenAI 兼容层）
  - LM Studio：`http://localhost:1234/v1`（在 LM Studio 中开启 Local Server）
  - vLLM：`http://localhost:8000/v1`
- **API Key 留空即可**：本地服务通常无需鉴权；若你的服务设置了鉴权，再正常填写。
- **模型 ID**：填写本地模型名（如 `qwen2.5:7b`、`llama3.1:8b`），也可用编辑器内的「获取模型列表」从 `/models` 拉取。
- 流式输出、思维链显示、联网搜索、QPS 限速等能力与云端模型一致。TTS / ASR / 生图 / 生视频仍走各自的独立 API，与本地文字模型无关。

### 默认模型

- 可以在设置中指定一个「默认模型」
- 未单独绑定模型的角色将使用默认模型
- 点击模型右侧的星标可设为默认

### 深度思考等级（F7）

念语可让支持「深度思考 / 推理」的模型以更高强度进行推理，提升复杂问题的回答质量。

- **每模型手动标记（v2.3.2 起，替代自动探测）**：设置 → 模型管理 → 编辑模型，勾选「支持深度思考 / 推理」即表示该模型支持推理模式（如 `DeepSeek-R1`、`o 系列`）。不再按模型名关键字猜测，可精确控制每个模型是否开启。
- **等级切换**：在「深度思考」下拉中选择 `关闭 / 低 / 中 / 高` 四档（默认 `关闭`）。
- **生效范围**：等级通过请求体 `reasoning_effort` 下发，**仅对 OpenAI 兼容接口生效**，且**仅对模型管理中勾选了「支持深度思考 / 推理」的模型生效**；未勾选或 Anthropic 等非兼容接口上该设置不生效。

### 高级参数与滑条调节（v2.3.1）

模型编辑弹窗内的参数均支持**滑条无级调节 + 输入框直接输入**两种方式，修改后实时生效：

| 参数                  | 范围            | 说明                                                                                                                                           |
| ------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 最大上下文长度             | 0 ~ 1,000,000 | 0 = 不限制；提供 `64K / 128K / 256K / 512K / 768K` 快捷档位。达到上限时按 token 估算自动裁剪历史，只发送最近部分                                                              |
| 温度 (Temperature)    | 0 ~ 2.00      | 回复随机性，0 = 几乎确定，2 = 最具发散性                                                                                                                     |
| 核采样 (Top-P)         | 0 ~ 1         | 只从概率累计达 P 的候选词中采样，值越小越保守                                                                                                                     |
| Top-K               | 0 ~ 50        | 只从概率最高的 K 个候选词中采样，0 = 不限制                                                                                                                    |
| 记忆读取限制              | 0 ~ 100       | 仅把**最近 N 条**历史消息作为角色短期记忆发给模型；0 = 不限制。降低可节省 token 并减少串扰                                                                                       |
| 单次最大输出 (max_tokens) | 留空 = 不限制      | 限制单次回复的最大 token 数，由请求体 `max_tokens` 下发                                                                                                       |
| 自定义参数 (JSON)        | 留空 = 不使用      | 可填写**合法 JSON 对象**，每次请求时合并进请求体，覆盖同名内置参数（如 `stop`、`frequency_penalty`、厂商特有字段等）。`messages` / `model` / `stream` 受保护不会被覆盖；保存时校验 JSON 合法性，非法则阻止保存 |

> 以上 `top_p / top_k / max_tokens` 仅在 OpenAI 兼容接口生效；Anthropic 接口会转换为 `top_p`（温度仍按 0~1 映射）。自定义参数在所有接口（OpenAI 兼容 / Anthropic）的请求体中均会被合并。

### 测试连接

点击模型右侧的「测试连接」按钮验证配置是否可用。

### 检测能力（真实探针，v2.3.9）

念语不靠模型名猜测能力，而是对模型发起**真实的最小请求**来判定它支持哪些功能（避免启发式误判）。

- **单模型检测**：模型管理 → 编辑模型，点「检测能力」按钮（位于「测试连接」旁），会依次探测：
  - **视觉（图片输入）**：发送 1×1 透明 PNG 的极小请求，按 HTTP 状态码判定；2xx=支持，400/415/422=不支持，其余（401/429/5xx/网络异常）=无法判定。
  - **工具调用**：发送带 `tools` 的极小请求，同上判定。
  - **JSON 模式**：发送带 `response_format: {type:"json_object"}` 的极小请求，同上判定。
  - **NSFW 内容输出**：发送一段成人向描写请求，按**返回正文语义**判定（状态码永远是 200，无法靠状态码判断）。命中拒绝词库（如 I can't / 抱歉 / 无法）判为「不支持」；命中露骨词或回以足量正文判为「支持」；正文过短或网络异常判为「无法判定」。**该探针是文本启发式，准确率不保证 100%，且会向模型真实发送成人内容请求、可能触发厂商风控或留下审核记录**，因此仅用于标记展示，不改变任何请求、不绕过模型自身安全策略。Anthropic 系标「未探测」。
  - **上下文窗口**：优先读取 `/models` 返回的 `context_window`，读不到则用你手动填写的「最大上下文长度」。
- **能力徽章**：探测完成后在编辑弹窗内显示 ✓（支持）/ ✕（不支持）/ —（未探测/无法判定）三枚徽章，并标注最近探测时间。
- **Anthropic 特殊处理**：Anthropic 系接口只测连通性 + 上下文窗口，视觉/工具/JSON/NSFW 四项标「未探测」（该格式探测不可靠），可手动勾选下方「支持工具调用 / 支持 JSON 模式 / 可输出 NSFW 内容」。
- **手动标记保底**：无论探测结果如何，「支持工具调用」「支持 JSON 模式」「可输出 NSFW 内容」都可手动勾选；探测**无法判定（null）时不覆盖**你已有的手动标记，避免误伤。
- **设置页一键全量检测**：设置 → 模型管理 →「模型能力检测」区有「一键检测所有模型能力」按钮，**点击会先弹出探测项选择框**（视觉/工具/JSON/NSFW 复选，**NSFW 默认不勾**并附带风险提示），确认后才遍历所有模型批量探测并统一落库，完成后用 toast 汇总每个模型的能力。

### 模型能力筛选（v2.3.9）

- **入口**：设置 → 模型管理，模型卡片列表上方有筛选工具条。
- **能力维度（AND 逻辑）**：点击 视觉 / 工具 / JSON / 推理 / NSFW 任一胶囊即按该能力筛出模型，**可多选**——模型须同时具备所选全部能力才命中。
- **分类维度（分组 / 标签，组内 OR）**：另有「按分组」「按标签」两行胶囊，可多选分组与标签，模型命中任一所选分组 / 任一所选标签即出（同维度内 OR）；分类维度与能力维度之间是 **AND**（须同时满足）。「未分组」为虚拟项，筛出所有未归入任何分组的模型。
- **实时计数**：右侧显示「匹配 N / 总数 个模型」；无匹配提示「没有符合所选条件的模型」；点「清除筛选」一次性复位全部筛选。
- **用途**：配合「模型分组 / 标签」可极快地从一堆模型里挑出「能工具调用的生图专用组」「便宜且支持视觉」等自定义集合。

### 模型分组与标签（v2.3.10）

- **分组（大归类，全局实体）**：设置 → 模型管理 →「模型分组」区，点「＋ 新建分组」添加分组；每个分组可**改名字、改颜色（点色块或颜色选择器）**；「删除」会弹出确认，仅把模型移出该分组、不删除模型本身。删除分组后，原先归入该分组的模型自动变为「未分组」，无孤儿引用残留。分组数量上限 20 个、单名最长 12 字符。
- **标签（细标记，自由文本）**：在模型编辑器内「标签」栏输入文字后**按回车**即添加为标签（回车前的草稿也可从已有标签联想），点标签上的 × 删除。每个模型最多 12 个标签、单标签最长 16 字符。
- **给模型归类**：编辑某模型时，在「分组」栏勾选要归入的分组（**一个模型可属于多个分组**）；保存后该模型即带上分组与标签。
- **筛选联动**：分组 / 标签会即时出现在模型管理筛选工具条，配合能力维度做快速筛选（见上）。模型卡片上也直接显示其分组色块与标签。

### 模型选择器悬停能力提示（v2.3.9）

- **入口**：角色编辑器 → 绑定模型下拉。
- **用法**：下拉中**悬停任一模型选项**，右侧弹出能力提示框，列出该模型的 视觉 / 工具 / JSON / 推理 支持情况与上下文窗口；**选择器本体（未展开时）悬停**也会通过原生提示显示当前模型的能力摘要，便于快速判断某模型能做什么、适不适合绑定给该角色。

### 获取模型列表

点击「获取模型列表」从服务器拉取当前 API Key 可用的模型列表（部分 API 支持）。

---

## 6. 角色管理

### 创建角色

进入通讯录 → 点击「创建角色」：

| 字段    | 说明         | 是否必填 |
| ----- | ---------- | ---- |
| 姓名    | 角色的名字      | 必填   |
| 性别    | 角色的性别      | 可选   |
| 年龄    | 角色的年龄      | 可选   |
| 职业    | 角色的身份/职业   | 可选   |
| 一句话简介 | 简短的角色介绍    | 可选   |
| 性格    | 角色性格描述     | 可选   |
| 背景故事  | 角色的背景设定    | 可选   |
| 外貌    | 角色的外貌描述    | 可选   |
| 世界观   | 角色的世界设定    | 可选   |
| 关键记忆  | 角色重要的记忆    | 可选   |
| 行为规则  | 角色的行为准则    | 可选   |
| 示例对话  | 角色说话风格的示例  | 可选   |
| 开场白   | 初次对话时的第一句话 | 可选   |

### AI 自动补全

填写角色的基本信息后，可以点击「AI 补全」让 AI 自动生成剩余设定。

### 角色设置

- **绑定模型**: 为角色指定使用的模型（留空则使用默认模型）
- **关联世界书**: 为角色指定世界书
- **关联规则**: 从规则库中选取角色专属规则
- **好感度**: 初始好感度和好感度影响系数
- **记忆隔离**: 默认**开启**。开启后，该人物在不同聊天中的记忆互相独立（只在本聊天内读写）；关闭则所有聊天共享同一份记忆。记忆面板中会以「本聊天专属 / 共享」徽标区分两类记忆。

### 复制角色（复制状态 + 记忆 + 聊天记录）

通讯录角色卡片上的「复制」按钮会调用后端 `roles:copy` 完整克隆：

- 复制角色的**全部配置与状态**（性格、世界书/规则关联、好感度、情绪等），新角色名自动加「副本」后缀；
- **记忆参数一并复制**：按聊隔离的记忆随新聊重映射，共享记忆保持共享；
- **聊天记录一并复制**：该角色所有单聊的完整消息记录会 1:1 复制为新的聊天卡片（卡片名加「副本」），并与新角色绑定。

### 导入角色卡

点击角色编辑页面的「导入角色卡（支持 PNG / JSON）」按钮，选择角色卡文件即可自动解析并填充表单。支持以下格式：

- **SillyTavern 角色卡（PNG）**：PNG 文件内嵌 `chara` 元数据块，解析后**自动以该 PNG 图片本身作为角色头像**（兼容直接 JSON 或 base64 包裹的元数据）。
- **SillyTavern 角色卡（JSON）**：V1/V2/V3（`name`/`char_name`、`description`/`char_persona`、`personality`、`scenario`、`first_mes`、`mes_example`、`avatar` 等字段），若 JSON 内含 base64 头像也会自动提取保存。
- **本软件自有格式**与**通用 JSON / 文本**（支持从 ` ```json ` 代码块或首个 `{...}` 中提取）。
- **NovelAI 角色卡**等常见第三方格式（字段按上述规则映射）。

> 提示：导入 PNG 角色卡时无需再单独选择头像，图片即头像；若想换头像可在导入后用「选择头像」按钮覆盖。

---

## 7. 聊天系统

### 开始对话

在通讯录中点击角色 → 「开始聊天」，即可进入单聊模式。

### 发送消息

- 在底部输入框中输入文字，按回车或点击发送按钮
- 支持 Shift+Enter 换行
- 消息将以用户身份发送，AI 角色会自动回复

### 消息操作

鼠标悬停在消息上会显示操作按钮：

| 操作 | 说明                                                                      |
| -- | ----------------------------------------------------------------------- |
| 撤回 | 撤回该条消息，并联动删除由该消息（含其触发的 AI 回复）自动生成的记忆；提示将显示实际删除的关联记忆条数，若无可删除记忆则提示「无关联记忆」 |
| 回滚 | 删除该消息之后的所有消息                                                            |
| 重发 | 重新发送该条消息                                                                |
| 转发 | 将消息转发到其他会话                                                              |
| 编辑 | 编辑已发送的消息内容                                                              |
| 复制 | 复制消息文本                                                                  |

> 💡 撤回联动记忆：自动记忆在生成时已绑定「用户消息 + AI 回复」这一组消息 ID，因此撤回其中任意一条都会命中并删除对应记忆；手动保存/快捷记忆不绑定消息 ID，不受撤回影响。

> 🧹 **清空当前聊天消息**：主界面工具栏「更多 ▾」菜单（以及快捷小窗工具条）中的 🧹 按钮，可一键清空当前会话的全部消息。弹窗提供「连同自动记忆一起删除（手动记忆不删）」勾选框：勾选后，本次清空还会删除在该聊天内产生的**自动记忆**（手动保存的记忆保留）；不勾选则仅清消息、保留全部记忆。

### 快捷记忆

选中聊天中的文本，点击弹出的「记住」按钮，可将内容一键存入角色的长期记忆。

### 图片发送

点击输入框的图片按钮可选择一张或多张本地图片。选中的图片不会立即发送，而是在**输入框正上方**展示一排缩略图预览（仿微信/豆包），每张图右上角有「✕」可单独移除；确认无误后点击发送，**所有选中的图片合并为同一条消息的图集一次性发出**（支持 1、2、3、4、≥5 张自适应网格布局），第一张图（或附带文字）触发 AI 回复。聊天中的图片消息采用**微信风格**：仅显示图片本身（圆角），不带绿色气泡背景；若消息同时含文字，文字保留气泡、图片在气泡下方以图集呈现。图片自动复制到数据目录，支持缩略图预览与全屏查看。主窗口与快捷小窗行为一致。

### 发图生图 / 生视频（F4）

在发送栏附上图片并输入一段文字提示词时，发送栏会额外出现两个按钮：

- **以此图生图**：AI 以你附带的图片为参考 + 你写的提示词，异步生成一张新图。
- **以此图生视频**：同样以图 + 提示词，异步生成一段视频（需在「设置 → 生视频」中配置独立视频生成 API）。

> 仅发送图片、未写任何提示词时，这两个按钮不出现，图片按普通图片消息发送（不触发生图 / 生视频）。生成结果经广播同步出现在主窗口与快捷小窗。

### 生视频（文本 → 视频，🎬 按钮）

发送栏的 **🎬 生视频**按钮可直接输入文字描述生成视频：

- 弹出输入框填写视频描述，点击「生视频」即开始；**生成期间可继续正常聊天**，无需等待。
- **完成检测与进度气泡**：后端自动识别「任务式」视频接口（提交后返回任务 ID 的，每 3 秒轮询直至完成）与「即时返回」接口；进度经 `video:progress` / `video:done` 广播到右上角**可拖动悬浮气泡**（显示百分比与状态），完成后气泡自动收起，视频消息自动插入对话。
- 生成的视频消息气泡内直接以内嵌 `<video>` 播放器展示（可点击播放）。

### AI 自动补全提示词（生图 / 生视频弹窗）

在「生图」与「生视频」弹窗（文本输入框）的左下角新增 **「AI 补全提示词」** 按钮：

- 先在输入框写一段粗略想法（如「一只猫」），点击该按钮，调用**默认模型**（`设置 → 模型管理` 中的默认模型；未设默认则取第一个启用模型）把想法扩展成一段完整、可直接使用的英文提示词，并**替换**回输入框，便于继续调整。
- 生图弹窗补「图像提示词」（主体 / 环境 / 光影 / 风格），生视频弹窗补「视频提示词」（画面 / 运镜 / 氛围）。
- **受 QPS 限速约束**：当默认模型命中每分钟请求上限（模型管理里设置的 QPS）时，按钮点击不会消耗额度，而是提示「AI 补全暂时用不了（请求过于频繁，请稍后再试）」，稍后重试即可。
- 主窗口的生图 / 生视频弹窗、快捷小窗的生图弹窗均已支持。

### QPS 限速覆盖范围（后台 AI 调用）

`设置 → 模型管理` 里为每个模型设置的 **QPS（每分钟最多请求数）** 约束所有走聊天模型（`queryAI`/`streamAI`）的调用，包含主回复与后台 AI：心情判定、关系/朋友圈判定、场景生图判定、翻译、群聊导演选人、群聊接话、空闲主动消息、随机事件、记忆提炼、模型对比（含评测）、角色简介 AI 补全、AI 自动补全提示词。

限速按**各模型自身 `qps`** 独立计数：每次调用使用它所用模型的 `qps`，超出时自动等待该模型的限速窗口解除后再发，避免触发服务端 429 限流；不同模型之间互不影响，默认模型不替其他模型背额度。

**支持小数 QPS（v2.3.10）**：QPS 框可填小数，如 `0.5` 表示每 120 秒约 1 次、`2` 表示每 30 秒约 1 次。内部采用「间隔制 + 60 秒滑动窗口」双重约束，既平滑了突发请求，也保证窗口内总量不超；填 `0` 或留空表示无限制。注意：之前填 `0~1` 小数会被吃掉小数点或误判为 1 次/分，现已修正。

生图、生视频、ASR、TTS 走各自**独立 API**（独立 `Base URL` 与 `API Key`，非聊天模型配置、无 `qps` 字段），不经过 `queryAI`/`streamAI`，因此**完全不受 QPS 约束**，高频调用不会被限速。

### 流式输出

AI 回复会逐字显示（打字机效果）。可以在设置中关闭流式输出。

- **切换聊天不影响进行中回复**：在 AI 尚未开始输出时切换到其他聊天，再切回原聊天，不会重复生成新的占位气泡；原回复仍在后台继续生成，完成后会正常出现在对话中。流式占位气泡统一在收到 `stream:start` 时创建，错过开始事件的中途 chunk 不会额外创建孤立气泡。
- **思考过程（reasoning）折叠行为**：若设置中开启「显示思维链」，AI 回复会附带可展开/收起的「思考过程」块；流式输出期间若模型正在输出思考过程且正文为空，该块会临时自动展开以便观察，正文开始输出后按设置恢复折叠/展开状态。手动点击箭头切换后，尊重你的选择不再自动回弹。

### Token 统计

每条消息下方显示该次请求的 Token 消耗量（prompt + completion）。v2.3.11 起**流式回复同样如实统计**：优先采用服务端返回的真实 usage（`stream_options.include_usage`），服务端不返回时按字符数估算（约 2 字符 ≈ 1 token）。统计页的全局 Token 消耗与人物板块 Token 均按此累计。

### 回到底部

长对话向上滚动、不在最底部时，输入框中轴线正上方会以线性动画弹出一个小气泡「⬇ 回到底部」（类似 WorkBuddy 的返回底部按钮）。无论消息区如何滚动，该按钮始终固定在输入框中轴线上方，不会随聊天内容移动；点击即可平滑滚动到最新消息。主窗口与快捷聊天小窗行为一致。

### 刷新界面

主界面工具栏与小窗工具条均提供「🔄 刷新界面」按钮（光标悬停为手指状，可点击）。当遇到输入框无法输入文字、界面偶发卡死、删除会话后工具栏无响应等异常时，点击该按钮会清空当前输入、重建并重新聚焦输入框、重新加载会话数据，并弹出「界面已刷新」提示，无需重启软件即可恢复。

### 删除正在生成的聊天（安全）

删除某个聊天时，若该聊天仍有 AI 回复正在后台生成，软件会**立即中止该聊天的全部进行中生成**，其回复不会写回已删除的聊天，也不会串到其他聊天。会话列表由消息反推生成，已删除群组或已删除角色的残留消息不再列为会话，因此不会凭空冒出「0 人、群名是一串数字、发消息无反应」的幽灵会话。正常删除（无进行中生成）行为与以往一致。

---

## 8. 群聊与智能体互聊

### 创建群组

1. 进入通讯录 → 点击「创建群组」
2. 填写群名
3. 从通讯录中选择成员（至少 2 人）
4. 创建完成后即可开始群聊

### 在群聊中 @ 提及

在输入框输入 `@角色名` 可以指定回复的目标角色。

### 调度策略

群聊中有两种 AI 发言调度方式：

| 策略       | 说明                   |
| -------- | -------------------- |
| 导演调度（推荐） | AI 根据对话上下文智能选择下一位发言人 |
| 轮询调度     | 按成员顺序轮流发言            |

在设置 → 群聊互聊中切换。

### 自动接话

AI 可以自动进行多轮对话：

- **AI 主动续聊（总开关）**: 开启后，群聊窗口才会显示「继续对话 / 自动接话」按钮，且用户发送消息后 AI 会自动按轮数继续接话；关闭时群聊只回复一轮，界面更干净。
- **自动接话轮数上限**: 设置每次触发后 AI 自动回复的轮数（默认 6 轮，0 表示无限）
- **同角色连续发言上限**: 设置同一位角色最多连续发言多少条（1–20，默认 1）。达到上限后会强制换另一位成员接话，避免「一个人一直说、其他人不动」。单聊不受影响。

在聊天窗口中点击「自动接话」按钮即可开始多轮对话，点击「停止」可随时中断。按钮区在任何窗口宽度下都保持横向排列，不会被压成竖排。

### 选人回复（精细控场，F1）

如果你希望精确控制群聊里「下一位由谁发言」，可开启选人回复（与「AI 主动续聊」互斥，二选一）：

- **开启方式**：设置 → 群聊互聊 → 勾选「群聊选人回复」。一旦开启，「AI 主动续聊」会自动关闭。
- **使用**：开启后，你每次主动发送一条消息、以及每条 AI 回复消息完成后，聊天界面会弹出**选人浮层**（列出群成员头像与名称），由你点击指定下一位接话者。
- **可见性与记忆**：群聊发送栏多了两个开关——
  - **全群可见**：取消后该消息仅作私密记录、不出现在群公开时间线；
  - **进入记忆**：仅当「全群可见」开启时可用，控制该消息是否参与自动总结记忆。**不可见消息不允许进入记忆**（取消可见会自动禁用并清零记忆开关）。
- 主窗口与快捷小窗均支持选人回复与可见性 / 记忆开关。

### 群聊转单聊

当群聊仅剩 1 人时会自动提示「转为单聊」，可以一键转换。

### 流式并发

群聊中多个 AI 可以同时流式输出。设置中可选择：

- 顺序：一次一个
- 适中：分批并行
- 全部：所有成员同时回复

### AI 互 @ 功能

在群聊编辑页面中开启「AI 互 @」开关后，AI 角色在群聊中可以使用 `@名字` 的方式点名其他成员。这增加了群聊的自然感和互动真实性。功能的开关按群聊独立配置。

---

## 9. 好感度与情绪系统

### 好感度

- 范围 0 ~ 100
- 默认初始值 50
- 通过对话中的关键词（中文正面/负面词库）动态调整
- 好感度会影响 AI 回复的语气和态度

### 关系值界面

- 在角色关系值弹窗中点击「让 AI 重新判定关系」，AI 会依据当前聊天内容重新判定关系类别与关系值。该操作**只更新关系，不会发朋友圈**。
- 朋友圈由朋友圈板块的「让 AI 生成」按钮或聊天过程中开启的自动发朋友圈负责，两者相互独立。

### 朋友圈（Moments）

- **AI 动态判定场景（与关系值判定无关）**：开启「AI 自动发朋友圈」后，每次 AI 回复结束会请模型独立判断「此刻是否真的想发朋友圈」。模型仅在对话中出现值得分享的内容（真实情绪起伏、重要事件、关系进展、有趣的梗等）时才发，日常寒暄不发。发朋友圈的频率由 AI 按场景决定，不硬编码为「每条消息都发」。注意：朋友圈的 AI 判定与「关系值 AI 判定」是两条相互独立的逻辑，互不触发——关系值界面判定关系不会发朋友圈，朋友圈板块生成也不会改动关系值。
- **单人物每日上限（全局默认 + 角色优先）**：朋友圈界面工具栏提供「单人物每日上限（全局默认）」下拉，可选 1/3/5/10/20 条或「无限」，即时保存到全局设置。选「无限」即不限制自动发送条数。角色编辑里的「朋友圈每日上限（单人物）」若填写了正数，则以该角色为准；留空或 0 则使用全局默认（全局设为无限时角色也无限）。达到上限后该角色自动发朋友圈暂停，次日恢复。该限制**只约束自动发送，与朋友圈板块「让 AI 生成」手动触发无关**（手动触发不受上限约束）。
- **朋友圈敏感程度（发圈克制度）**：设置中的「朋友圈敏感程度」滑块（0~~100%）控制自动发朋友圈的门槛。AI 判定时会额外给出「此刻想发的强烈程度 shareUrge（0~~100）」；只有当 AI 明确想发（postMoments=true）且 shareUrge 达到由敏感程度换算的阈值（阈值 = (1 − 敏感程度) × 100）时，才真正发出。敏感程度高（如 80%）→ 阈值低（20）→ 稍聊到尽兴就发；敏感程度低（如 20%）→ 阈值高（80）→ 只有极度想发才发，**普通唠嗑几乎不会发朋友圈**。默认 50%（需 shareUrge ≥ 50）。
- **朋友圈视频生成（独立开关）**：设置 → 「朋友圈视频生成」开关（默认关闭）。开启后，AI 判定动态时会同时给出 `needVideo` / `videoPrompt`（英文视频描述），调用**默认的视频生成模型**在后台生成视频，完成后挂到该条朋友圈下以内嵌播放器展示。未配置「生视频」API 时该开关不生效（界面有黄色提示）。生成进度同样显示在右上角悬浮气泡。

### 情绪系统

AI 角色有 8 种情绪状态：

| 情绪    | 说明     |
| ----- | ------ |
| 😊 开心 | 正面情绪   |
| 🥰 撒娇 | 亲昵情绪   |
| 💕 心动 | 浪漫情绪   |
| 😳 害羞 | 羞涩情绪   |
| 😌 平静 | 中性默认情绪 |
| 😔 低落 | 轻微负面情绪 |
| 😢 难过 | 负面情绪   |
| 😠 生气 | 强烈负面情绪 |

### 情绪影响

- 情绪由对话内容、好感度变化、随机事件共同决定
- 情绪会冷却（默认 20 秒后才可重新判定）
- **心情平滑过渡（防忽喜忽悲）**：角色内心维护一个连续的情绪值（−100~100），AI 每次判定的目标心情会按「心情过渡指数」平滑地移动过去，再反推出显示的离散心情词。因此心情是连续渐变的，不会一下子开心一下子伤心。
- 在设置「情绪与事件（高级）」中可调节「心情过渡指数」：0~100%，越小越平滑（变化幅度小、过渡慢），越大越灵敏（更快贴近 AI 判定的目标心情）；设为 100% 等同旧版每次直接跳变。默认 50%。
- 在设置中可调节「对话影响心情程度」和「事件影响心情程度」

---

## 10. 语音功能 (ASR/TTS)

### 语音输入 (ASR)

1. 在设置 → 语音中配置 ASR 模型
2. 使用支持 `/audio/transcriptions` 端点的模型配置
3. 在聊天输入框点击麦克风按钮进行语音输入
4. 语音会自动转写为文字

- 模型名可点击「刷新模型列表」按钮向对应 Base URL 拉取（按类型过滤后填入下拉），也可手动输入平台文档中的准确模型名（如 whisper-1）。

### 语音播报 (TTS)

1. 在设置 → 语音中配置 TTS 模型
2. 使用支持 `/audio/speech` 端点的模型配置
3. 选择语音角色（如 alloy, echo, fable 等）
4. 开启「自动播报」后，AI 回复会自动朗读

- 模型名可点击「刷新模型列表」按钮向对应 Base URL 拉取（按类型过滤后填入下拉），也可手动输入平台文档中的准确模型名（如 tts-1）。

### 未配置模型时的入口行为

- 若**未在设置 → 语音中配置 ASR 模型**，聊天框与小窗的麦克风按钮会**置灰禁用**（鼠标悬停提示「未配置语音输入模型，麦克风暂不可用」），不会隐藏入口，方便你发现并前往设置配置。
- 若**未在设置 → 语音中配置 TTS 模型**，朗读按钮同样会置灰禁用；未开启「自动播报」时不会朗读。
- 两条规则相互独立：ASR 与 TTS 可单独配置、单独启用。

### 注意事项

- 语音功能需要模型 API 支持对应的 `/audio/` 端点
- 并非所有 API 提供商都支持 ASR/TTS
- OpenAI 的 `whisper-1` 和 `tts-1` 模型是常用的选择

---

## 11. 快捷聊天小窗 (MiniChat)

小窗是一个独立的无边框迷你窗口，让你在使用其他应用时也能快速与 AI 对话。

### 启用小窗

在设置 → 快捷小窗中开启。

### 打开小窗

- 快捷键: 默认 `Ctrl+Shift+Z`
- 可以从系统托盘菜单中打开

### 小窗设置

| 设置项      | 说明            |
| -------- | ------------- |
| 启用小窗     | 开关            |
| 快捷键      | 自定义快捷键        |
| 最小化时自动弹出 | 主窗口最小化时自动打开小窗 |
| 始终置顶     | 小窗始终在其他窗口之上   |
| 默认绑定会话   | 小窗默认打开的对话     |

### 小窗功能

- 完整的聊天功能
- 边缘吸附
- 透明度调节（通过设置）
- 消息提示

---

## 12. 世界书

世界书是管理世界观和背景设定的工具，可以理解为角色扮演中的"设定库"。

### 使用场景

- 为角色设定一个完整的世界观
- 在聊天中自动注入世界设定作为 AI 的提示词
- 兼容 SillyTavern 和 NovelAI 的 Lorebook 格式

### 创建世界书

进入资源库 → 世界书 → 点击「新建」：

- 世界书名
- 内容描述（自由文本，世界观介绍）
- 条目列表（每个条目包含关键词和内容，按关键词匹配注入）

### 世界书优先级

系统按以下顺序确定当前对话使用的世界书：

1. 本对话指定的世界书（最优先）
2. 角色关联的世界书
3. 全局默认世界书
4. 使用 `'none'` 可显式禁用该层

### 在世界书中设置

- **按角色**: 在角色编辑页面的「世界书」下拉中选择
- **按对话**: 在聊天窗口顶部有「本对话世界书」下拉选择
- **全局默认**: 在设置中选择

### 导入世界书

支持导入格式：

- SillyTavern 世界书 JSON
- NovelAI Lorebook JSON
- 通用 JSON
- 纯文本（自动转换为单条目世界书）

---

## 13. 规则库

规则库是可复用的提示词规则集合。

### 规则类型

- **共享规则 (shared)**: 对所有角色生效
- **角色专属规则 (character)**: 仅对指定角色生效

### 创建规则

进入资源库 → 规则 → 点击「新建」：

- 规则名称
- 规则内容（提示词文本）
- 共享开关

### 应用规则

- 共享规则在设置中统一管理
- 角色专属规则在角色编辑页面中选取

---

## 14. 记忆系统

记忆系统让角色能够长期记住重要信息。

### 记忆隔离（按聊天隔离，默认开启）

- 角色编辑 → **记忆隔离**开关（默认开启）：开启后，该角色在不同聊天中的记忆**互相独立**——自动提炼、手动添加的记忆只在本聊天内读写，另一段聊天看不到也读不到；关闭后所有聊天共享同一份记忆。
- 记忆面板（资源库 → 记忆）中，每条记忆以徽标区分：`本聊天专属`（仅某段聊天可见）与 `共享`（所有聊天可见）。
- 复制角色 / 复制聊天时，隔离记忆随新聊天自动重映射，隔离关系保持不变。

### 手动记忆

1. 进入资源库 → 记忆
2. 选择角色
3. 点击「添加记忆」输入内容
4. 支持编辑和删除

### 快捷记忆

在聊天中选中文本，点击弹出的「记住」按钮一键添加。

### 自动记忆（AI 提炼 / 10 轮自动提取）

- 设置中开启「AI 自动提炼记忆」（**全局总闸**）后，仅对**同时开启「长记忆」的聊天**生效。
- 触发频率：**每累计 10 条用户消息**自动提炼一次（非每轮都跑）；前端在「长记忆」开关下方实时显示「已累计 N/10 轮」。
- 提炼出的记忆来源标记为 `auto`，会去重，避免重复记录相同事实。
- 与「长记忆」手动总结按钮共用同一提炼核心，二者可同时启用、互不替代。详见下方「长记忆」。

### 长记忆（per-chat 独立开关）+ 手动让 AI 总结记忆（v2.3.9）

长记忆是**每个聊天独立的总开关**，控制该聊天是否拥有「跨对话持久记忆」：

- **开启长记忆**：AI 可跨对话持久保存用户的偏好、习惯、个人信息等内容，并在后续对话中主动调用与呼应，实现连贯的长期交互体验。
- **关闭长记忆**：该聊天每次交互都从零开始——不向模型回灌任何已存记忆、不做任何自动提炼、也不显示「✨ AI 总结记忆」按钮（已存记忆原样保留在库，重新开启即恢复，非破坏性）。

**两种记忆提取机制（均仅在长记忆开启时生效）**：

1. **手动提取记忆**：在聊天「⋯」其他操作菜单中，打开「长记忆」后会出现「✨ AI 总结记忆」按钮。点击后调用**默认模型**把当前聊天关键信息提炼为长记忆并写入（与自动提炼共用同一提炼核心，产出一致）；受默认模型 QPS 约束。适合保存重要、核心的信息，精准可控。
2. **10 轮自动提取记忆**：需同时开启设置中的全局「AI 自动提炼记忆」开关。每累计 **10 条用户消息**（前端在长记忆开关下方显示「已累计 N/10 轮」），系统自动筛选对话中有价值的信息（如用户偏好、高频提及）完成记忆存储，全程无需用户操作。

**长记忆管理（用户主导）**：用户对 AI 记忆拥有控制权——可在「资源库 → 记忆」中自主增 / 改 / 删某条记忆，或在聊天中选中文本点「记住」一键添加，主导记忆的内容与生命周期，而非完全由系统自动决定。

> 手动提取与 10 轮自动提取核心区别：手动是用户主动精准挑选，自动是系统周期化批量筛选。二者可同时启用、互不替代。

### 记忆与消息关联

- 记忆与消息 ID 关联
- 撤回或回滚消息时，关联的记忆也会被联动删除

---

## 15. 插件导入

插件导入功能可以一次性解析并导入角色卡、世界书、规则，以及**念语原生插件清单 / OpenAI `ai-plugin.json`** 等外部插件格式，统一落为声明式「插件」记录，兼容 SillyTavern、NovelAI 等市面常见格式。

### 导入方式

1. 进入资源库 → 导入插件
2. 选择文件（支持 JSON / 纯文本）
3. 系统自动识别内容类型并导入到对应位置；若为插件清单，会额外在「设置 → 插件 → 已导入插件」中生成一条记录

### 支持导入的内容

- 角色卡 → 导入到通讯录
- 世界书 → 导入到资源库
- 规则 → 导入到资源库
- 念语原生插件清单 / OpenAI `ai-plugin.json` / SillyTavern / NovelAI → 导入为「插件」（含受控 HTTP 工具与提示词片段）

### 插件管理与安全边界

- 设置 → 插件 中可查看「已导入插件」列表，对每条插件启停或删除。启用的插件会将其提示词片段注入对话。
- **默认不执行任何 JS**。仅当全局「允许插件执行 JS」（`pluginAllowJs`，默认关闭）开启，且插件带有 `jsEntry` 时，才会在受限上下文加载该 JS——此开关等同于授予任意代码执行权限（RCE 风险），界面以红色高危警示标注，**仅在完全信任来源时开启**。
- 插件的「受控 HTTP 工具」（`plugin:callTool`）只会发送预设的请求，绝不执行任意代码，是安全的外扩方式。

---

## 16. 随机事件

随机事件系统让 AI 能在对话中主动触发剧情小事件，增加互动趣味性。

### 触发方式

点击聊天工具栏的 🎲 按钮，直接触发随机事件。会立即弹出一个模态弹窗，进入事件流程。

> 如果当前事件正在生成或已有事件未处理，重复点击不会触发新事件，也不会报错，等待当前事件完成即可。

### 事件流程

弹窗分为两个阶段：

**阶段一：AI 生成中（加载阶段）**

- 显示加载动画和「正在生成随机事件…」提示
- 底部有 60 秒倒计时
- 最后 10 秒倒计时标红闪烁提醒
- 如果 AI 超过 60 秒未完成生成，弹窗自动关闭
- 右上角有 ✕ 按钮可手动关闭弹窗

**阶段二：选择应对方式（选择阶段）**

- 显示事件描述文字
- 提供 2~4 个选项，**按好感度变化从高到低排序**
- 底部有 60 秒倒计时
- 最后 10 秒倒计时标红闪烁提醒
- 如果 60 秒内未选择，**自动选择第一个选项**（按好感度排序后最推荐的一项）
- 右上角有 ✕ 按钮可手动关闭弹窗

### 选择结果

选择后，会在聊天界面顶部弹出好感度变化提示（如「好感度 +5（角色名）」）。如果超时自动选择，提示会额外标注「已自动选择」。

角色心情也会同步更新，在角色名旁显示新的心情徽标。

### 事件主题

| 主题    | 说明   |
| ----- | ---- |
| 🎲 随机 | 完全随机 |
| 🎁 送礼 | 送礼事件 |
| 💕 约会 | 约会事件 |
| 🎮 日常 | 日常事件 |
| 🌟 惊喜 | 惊喜事件 |
| 😤 争执 | 争执事件 |

> 点击 🎲 按钮默认触发随机主题，不再提供主题选择菜单。

### 自动触发

AI 也会在每轮对话完成后按概率自动触发随机事件（约 45% 概率），前提是在设置中开启了随机事件功能。

> 设置中的「随机事件」开关对所有已打开的聊天窗口（主窗口与快捷小窗）即时生效：关闭后，正在进行的对话轮次结束后也不再自动弹出，无需重启或切换聊天；手动点击 🎲 按钮仍可触发。

### 多聊天隔离

事件状态按聊天隔离：

- 聊天 A 触发事件后切换到聊天 B → 弹窗自动隐藏
- 切回聊天 A → 弹窗恢复显示，倒计时继续
- 不同聊天可以各自拥有独立的事件状态
- 同一时间一个聊天只能有一个活跃事件

### 高级设置

| 设置项      | 说明                                                   |
| -------- | ---------------------------------------------------- |
| 影响心情程度   | 事件对心情的影响倍数 (0~1)                                     |
| 心情过渡指数   | 心情变化的平滑程度 (0~100%)：越小越平滑、防忽喜忽悲；越大越灵敏、越快贴近 AI 判定的目标心情 |
| 低好感冲突阈值  | 好感度低于此值时事件偏向冲突                                       |
| 高好感甜蜜阈值  | 好感度高于此值时事件偏向甜蜜                                       |
| 参考历史条数   | 事件生成参考的最近消息数量                                        |
| 最大 Token | 事件生成的 token 上限                                       |

---

## 17. 观察者模式（对局）

观察者模式让你可以以「上帝视角」旁观群聊对局。

### 启用观察者模式

在群聊中点击「观察者模式」开关。

### 功能特性

- **公屏频道**: 观察者可以看到所有对话
- **私密小窗**: 观察者专属私信频道，对话不进入公屏
- **信息隔离**: 私密对话不串流给其他 AI 参与者

### 观察者配置

| 配置项    | 说明            |
| ------ | ------------- |
| 记忆冻结   | 观察者模式下不更新角色记忆 |
| 纯旁观    | 不计算情绪演算       |
| 私密写记忆  | 私密对话写入角色记忆    |
| 私聊影响情绪 | 私密对话影响角色情绪    |

### 对局日志

每次对局结束后会自动保存完整的 JSON 日志到数据目录的 `matches/` 文件夹，包含：

- 公屏对话记录
- 私密对话记录
- 情绪变化轨迹
- 最终状态快照

---

## 18. 空闲主动回复

当用户一段时间没有向该角色发送消息时，AI 角色可以主动发送消息。

### 空闲标准

空闲 = **没有向该聊天发送消息**，与鼠标是否移动、键盘是否按下无关。你在其他聊天中聊天不影响此聊天的计时（具体行为由下方「切换聊天策略」决定）。

### 启用方式

1. 在设置 → 主动消息中开启全局开关
2. 可在每个对话中通过聊天界面的开关单独控制
3. 全局关闭时，所有聊天的单独开关变为灰色不可用

### 触发设置

| 设置项  | 说明                                           |
| ---- | -------------------------------------------- |
| 触发时机 | **固定间隔**：每次都按同一静默时长触发；**随机时间**：每次触发前在「最短 ~ 最长」秒内随机抽取本次静默时长（v2.3.11 新增） |
| 超时时间 | 固定间隔模式下可选 5 分钟 / 10 分钟 / 30 分钟 / 1 小时 / 2 小时 / 5 小时 |
| 随机范围 | 随机时间模式下设置最短/最长静默秒数，可调范围 **1 ~ 86400 秒（1 秒 ~ 24 小时）**，自动保证最短 ≤ 最长；默认 60 ~ 1800 秒 |
| 写入记忆 | 主动消息是否参与 AI 自动记忆提炼                           |

> 随机模式下每条主动消息触发后都会**重新抽取**下一次间隔，聊天界面的倒计时显示的就是本次实际抽中的时长。

### 切换聊天策略

当你切换到其他聊天时，离开的聊天计时行为可以在设置中选择（全局生效）：

| 策略       | 说明                       |
| -------- | ------------------------ |
| 暂停计时（默认） | 计时冻结。切回后从暂停处继续           |
| 重置计时     | 计时清零。切回后重新开始计时           |
| 继续计时     | 计时继续。不论你在其他聊天做什么，到时间就会触发 |

### 倒计时显示

在聊天界面工具栏的主动消息开关旁边，显示实时倒计时（如「10m23s」或「45s」）。关闭主动消息或已超时时隐藏。

> **后台运行仍触发（v2.3.7 起由主进程调度）**：主动消息的空闲计时与触发已由主进程统一负责，不再依赖渲染进程的定时器。将主界面最小化到托盘、或窗口处于隐藏/最小化状态时，到时间照常主动发言，无需重新打开窗口；关闭到托盘期间发出的消息会进入悬浮球未读清单并弹出后台消息卡片。倒计时显示仍由渲染端负责（数据来自主进程广播），主界面可见时行为不变。

## 19. 自我身份（用户角色卡）

你可以创建多个自我身份，在不同对话中使用不同的"自己"。

### 创建自我身份

进入设置 → 自我身份管理 → 点击「添加」：

- 姓名
- 性格
- 背景
- 说话风格
- 其他自定义属性

### 使用自我身份

- 在设置中选择全局默认身份
- 在聊天中可单独覆盖身份
- 不同身份可以在不同对话中使用

---

## 20. 主题与外观

### 主题

念语内置 14 套主题：

| 主题     | 风格        |
| ------ | --------- |
| 微信经典   | 浅色，类似微信风格 |
| 毛玻璃    | 毛玻璃效果     |
| 极���暗色 | 深色极简      |
| 活力多彩   | 彩色活泼      |
| 静谧青蓝   | 青蓝色       |
| 雾紫银河   | 紫灰色       |
| 松绿墨屿   | 墨绿色       |
| 熔橙炭灰   | 橙色+炭灰     |
| 冰川素白   | 冷白色       |
| 豆沙玫瑰   | 粉玫瑰色      |
| 赛博青柠   | 赛博朋克青柠色   |
| 石墨极简灰  | 石墨灰色      |
| 深海靛蓝   | 深蓝色       |
| 暖米浅棕   | 暖棕色       |

### 毛玻璃主题的浮层面板

在毛玻璃主题下，聊天界面的浮层菜单（其他操作、@提及、抽屉、模态框、错误气泡、思考过程、排队提示、轻提示）统一使用加深的半透明深色背景（约 88% 不透明），避免下方聊天内容穿透造成文字重叠、难以辨认。其余主题仍为原配色。

### 外观设置

| 设置项          | 说明                                                                                             |
| ------------ | ---------------------------------------------------------------------------------------------- |
| 字体大小         | 全局 UI 字体大小                                                                                     |
| 字体样式         | 11 种字体族可选                                                                                      |
| 整体圆角         | UI 组件的圆角大小                                                                                     |
| 气泡圆角         | 聊天气泡的圆角大小                                                                                      |
| 动效开关         | 全局 UI 动画效果                                                                                     |
| 聊天背景         | 按对话定制聊天背景图片                                                                                    |
| 输入框文字色 / 背景色 | 自定义聊天输入框的字体颜色与内部背景色，防止文字与背景相近看不清（默认浅灰底 + 深字，留空跟随主题）                                            |
| 全局文字对比度      | 全部文字、气泡、弹窗、提示、错误气泡、下拉菜单均使用主题 CSS 变量渲染；任意主题下均自动保证正文/辅助文字与背景有足够的对比度，不会出现黑底黑字或白底白字                |
| 毛玻璃背景        | 仅「毛玻璃 / 磨砂」主题可用。可选背景颜色或导入图片作为软件背景，毛玻璃磨砂效果仍保留；未开启毛玻璃类主题时该设置整块隐藏（颜色与图片互斥，选其一即清空另一；可一键清除恢复主题默认渐变） |
| 玻璃主题聊天界面颜色 | 仅「毛玻璃 / 磨砂」主题可用。自定义 **Token 栏字体色 / Token 栏边框色 / 用户气泡文字色 / AI 气泡文字色 / 气泡边框色** 五项，解决更换背景后字体、边框与背景颜色相近而看不清的问题；任一留空则跟随主题默认（可一键重置全部颜色） |
| 动态光标         | Canvas 自定义光标与特效（见下方）                                                                           |

### 模型报错气泡（v2.3.2）

当 AI 模型回复出错（如 401/403 无权限、429 限流、超时、服务端 5xx、网络异常等）时，聊天界面右上角会弹出**非模态错误气泡**：

- **15 秒自动消失**，或点击右上角 **×** 立即关闭；不遮挡、不阻断聊天输入框（空区域点击穿透）。
- **点击气泡展开详情**：显示「可能原因」与「解决办法」，并附上**软件可识别的错误码**（auth / rateLimit / timeout / serverError 等）。
- 原因与解决办法使用**软件当前设置的语言**（中文/英文）展示。
- 错误会**记录到错误日志**；错误信息**不会进入聊天框**，也**不会写入记忆**。此时输入框上会显示「发送失败」重发面板，可一键重发（AI 回复部分）。

### 动态 Canvas 光标

设置 → 主题与外观区域可开启 **动态 Canvas 光标**，使用全屏透明 Canvas 渲染自定义鼠标指针，替代系统原生光标。该设置对主界面与快捷聊天小窗同时生效，两处共用同一套渲染引擎与全部参数，规则完全一致。

**核心特性：**

- lerp 缓动跟随，灵敏度可调（0.1~0.5）
- 悬浮可交互控件时光标自动放大（比例可调）
- 拖拽操作自动切换抓取状态
- 液态渐变拖尾：移动时产生渐变透明消散轨迹（可独立开关）
- 流光粒子点缀：移动时散发柔光微粒（可独立开关，上限 8 个）
- 空闲保留时长：光标静止后保持可见的时长可调（1~300 秒，最高 5 分钟），超时自动隐藏并恢复系统原生光标

**性能保障（硬性规则）：**

| 规则     | 说明                                                             |
| ------ | -------------------------------------------------------------- |
| 空闲停帧   | 光标静止超过 300ms 自动停止渲染循环（仍保留静态光标，降 CPU 占用）；超过「空闲保留时长」后隐藏并恢复系统原生光标 |
| 拖尾点位   | 硬性上限 10 个，超出不创建                                                |
| 粒子数量   | 硬性上限 8 个，超出不创建                                                 |
| FPS 降级 | 实时帧率低于 52fps 时自动关闭粒子、减少拖尾                                      |
| 对象池复用  | 所有粒子和轨迹点预分配，运行时不新建/销毁对象                                        |
| 脏区域重绘  | 仅清除和重绘发生变化的画布区域，避免全屏 clearRect                                 |
| 零高开销滤镜 | 禁止模糊/径向发光/多层叠加等 GPU 重操作                                        |

**失焦保护与多窗口协调：**

- 窗口失去焦点时，光标以 150ms 平滑淡出后隐藏，同时恢复系统原生光标；重新聚焦（且鼠标确实在窗内）时以 200ms 丝滑淡入浮现。
- **单光标保证**：主界面与快捷聊天小窗共用同一套动态光标规则，但同一时刻只有**鼠标真正所在的窗口**渲染自定义光标，另一窗口自动淡出并恢复原生光标；移到桌面时两窗口均隐藏、由系统原生光标接管，绝不会出现双光标并存或「任何位置都找不到光标」。
- **切换逻辑**：用鼠标是否真正位于窗口内（而非窗口焦点）驱动显隐。鼠标从主界面移入小窗、或从小窗移回主界面、或从桌面拖入 UI，光标均先微微浮现再完全显示，不会突然弹出；切走时平滑消散而非瞬间消失。
- 唤起右键菜单时光标立即隐藏（不使用淡出动画），以便原生光标操作菜单；菜单关闭后自动恢复。
- Canvas 层级低于模态弹窗（z-index: 9998），不会遮挡任何 UI 交互。

---

## 21. 备份与还原

### 创建备份

1. 进入设置 → 备份与还原
2. 点击「创建备份」
3. 选择备份保存位置
4. 备份文件为 ZIP 格式，包含所有角色、聊天记录、设置、世界书、规则、记忆

### 自动备份目录

可以在设置中指定默认备份目录。

### 还原备份

1. 进入设置 → 备份与还原
2. 点击「还原备份」
3. 选择备份文件
4. 系统会先解压校验 → 将当前数据移至安全位置 → 替换为备份数据
5. 操作完成后自动重启应用

> **注意**: 还原操作不可逆，它会覆盖所有当前数据。建议在还原前先手动创建一份备份。

### 导出

设置中提供「导出」功能，将备份导出到指定位置。

---

## 27. 一键恢复初始设置

设置页面底部提供「🧹 恢复初始设置」按钮，可一键将所有偏好与行为设置恢复为出厂默认。

### 重置方式（每次可自行选择）

点击按钮后会弹出选择框，让你决定 **API Key 与模型是否保留**：

- **保留 API Key 与模型**：仅把主题、语言、语音（ASR/TTS）、空闲主动回复、随机事件、群聊互聊、外观圆角等偏好恢复默认，你的模型配置与密钥原样保留（推荐，适合「配置乱了想恢复默认」的场景）。
- **完全重置（清空密钥与模型）**：所有设置项回到首次安装的纯净状态，包含清空已配置的 API Key 与模型列表。

### 始终保留的数据

以下数据不受重置影响，因为它们不属于「设置」项：

- 角色卡、聊天记录、世界书、规则、记忆等独立数据存储
- 「我的角色卡」（自我身份）：`selfRoles` / 当前身份 / 按聊天身份覆盖
- 首启向导标记：重置后不会再次误弹初始向导

### 注意事项

- 重置后若界面语言与当前不同，会自动热切换。
- 重置仅改写 `settings.json`，不触及聊天数据；需要整体备份请参见 §21。
- 本软件关闭窗口只是最小化到托盘，修改后需从托盘右键退出再重新启动才能加载新构建。

---

## 22. 快捷键与窗口控制

### 全局快捷键

| 快捷键                 | 功能        |
| ------------------- | --------- |
| `Ctrl+Shift+Z` (默认) | 打开/切换快捷小窗 |

可在设置中自定义小窗快捷键。

### 窗口控制

| 操作     | 方式           |
| ------ | ------------ |
| 最小化到托盘 | 点击窗口关闭按钮 (X) |
| 真正退出   | 托盘右键 → 退出    |
| 最小化    | 标题栏最小化按钮     |
| 最大化/还原 | 标题栏最大化按钮     |
| 显示主窗口  | 左键点击托盘图标     |

---

## 23. 数据存储位置

所有数据（角色、聊天记录、设置、好感度、世界书、规则、记忆）都存储在本地 JSON 文件中：

- **Windows**: `%APPDATA%/念语/data/`
  - `store.json`: 角色、聊天消息、群组、好感度、世界书、规则、记忆
  - `settings.json`: 应用设置
  - `images/`: 聊天中的图片文件
  - `matches/`: 观察者模式对局日志

---

## 24. 错误自诊

念语内置了完整的错误自诊系统，覆盖渲染进程和主进程。当发生错误时，会自动分析原因并给出解决建议。

### 渲染进程错误（界面层）

当 React 组件崩溃或发生未捕获的 JavaScript 错误时：

- **React 错误边界**: 组件树中的错误会被 ErrorBoundary 捕获，显示恢复页面，可点击「Reload Application」刷新
- **全局错误弹窗**: `window.onerror` 和 `unhandledrejection` 会被全局监听器捕获，弹出 DOM 覆盖层诊断面板，显示：
  - 错误标题
  - 错误信息（红色背景）
  - 可能原因（黄色背景）
  - 解决方法（绿色背景）
  - 操作按钮：「复制详情」「重启应用」「关闭」
  - 复制详情包含完整错误信息、原因、解决方案和时间戳

### 主进程错误（后端层）

当 Electron 主进程出现异常时：

- **未捕获异常 (uncaughtException)**: 弹出原生系统对话框，显示错误信息、原因和解决方法。应用会自动退出。请根据解决建议排查后重启。
- **未处理的 Promise 拒绝 (unhandledRejection)**: 非致命错误会通过 IPC 推送到渲染进程，在界面层弹出诊断弹窗，不会强制退出。

### 多语言支持

诊断信息会根据当前界面语言（中文/英文）自动切换显示。

### 常见诊断场景

| 错误类型           | 可能原因             | 解决方法                                   |
| -------------- | ---------------- | -------------------------------------- |
| API 连接失败       | 网络不通、Base URL 错误 | 检查网络、核对 Base URL（含 https://）           |
| 超时 (timeout)   | 服务器负载高或网络延迟大     | 检查网络稳定性；免费 API 通常较慢，可更换模型              |
| 认证错误 (401/403) | API Key 无效或到期    | 检查 API Key 配置和余额                       |
| 模型不存在 (404)    | 模型 ID 填错或无权访问    | 核对模型 ID、使用「获取模型列表」查看可用模型               |
| 速率限制 (429)     | 请求过于频繁           | 降低并行度、稍后重试                             |
| 余额不足           | API 账户欠费或配额耗尽    | 登录 API 提供商控制台充值                        |
| 上下文超限          | 对话太长超出模型上下文窗口    | 降低上下文长度、开新对话、换用大窗口模型                   |
| 流式中断           | 网络不稳定或服务器端异常     | 检查网络稳定性；可临时关闭流式输出                      |
| 语音失败 (ASR/TTS) | 模型未配置或不支持语音端点    | 在设置→语音中确认模型配置，检查 API 是否支持 `/audio/` 端点 |
| 图片处理失败         | 磁盘空间不足           | 检查磁盘空间、尝试小尺寸图片                         |
| 文件读写失败         | 权限不足、文件被占用       | 检查文件权限、确保目录可写、重启应用                     |
| 备份还原失败         | 磁盘空间不足或数据损坏      | 检查磁盘空间；还原失败时旧数据会被保留                    |
| 模型配置失效         | 角色绑定的模型已被删除      | 编辑角色重新选择有效模型                           |
| 随机事件失败         | 未配置默认模型或正在生成中    | 确认默认模型已配置；等待当前事件完成                     |
| 角色不存在          | 角色已被删除但聊天仍在引用    | 刷新角色列表或删除对应的聊天记录                       |

---

## 25. 常见问题

### Q: 启动后什么也不显示？

A: 检查 Node.js 是否安装成功。在终端运行 `node --version` 确认版本号。

### Q: 启动后提示"无法连接到 API"？

A: 请先添加模型配置。首次使用必须至少配置一个可用的 AI 模型。

### Q: 模型连接测试失败？

A: 检查以下项目：

1. Base URL 是否正确（包括 https:// 前缀）
2. API Key 是否有效
3. 网络连接是否正常
4. 是否需要配置代理

### Q: 聊天没有回复？

A: 可能原因：

1. 角色未绑定有效模型
2. 没有设置默认模型
3. API Key 余额不足
4. 网络不稳定

### Q: 输入框无法输入文字？

A: 从 v1beta-a27 起，导致该问题的两类根因已修复：随机事件全屏遮罩不再拦截整窗输入；后台通知卡片窗口已设为不可聚焦，不会再偷走主窗口键盘焦点；窗口重新获得系统焦点时会自动把焦点还给输入框。若仍偶发异常，可尝试以下方法：

1. 点击工具栏 / 小窗工具条上的「🔄 刷新界面」按钮，可一键清空输入、重建输入框并重新聚焦。
2. 点击输入框区域任意位置，系统会自动聚焦到输入框。
3. 如果角色已被删除，输入框会处于只读状态，需要重新创建角色或删除该聊天。
4. 删除群聊 / 单聊后若新会话输入框无法输入，多为焦点未落到输入框，点一次「🔄 刷新界面」或切换一下其他聊天再切回即可恢复。
5. 从系统托盘右键退出后重新启动应用（兜底方案）。

### Q: 聊天记录突然消失了？

A: 检查设置中是否开启了自动记忆功能。如果使用了回滚功能，会删除之后的所有消息。定期备份是避免数据丢失的最佳实践。

### Q: 如何更改语言？

A: 点击左侧导航底部的「中/EN」按钮，或在设置 → 界面语言中切换。

### Q: 如何更新到新版本？

A: 目前没有自动更新机制。需要手动下载新版本，然后在设置中导出备份 → 关闭旧版本 → 解压新版本 → 还原备份。

### Q: 程序卡住了怎么办？

A: 从系统托盘右键退出，然后重新启动。如果问题持续，尝试导出备份后重新安装。

---

## 26. 后台消息提醒（主题自适应通知卡片）

当软件在后台运行、且**主界面与小窗均未打开或已最小化**时，收到 AI 新消息会自动弹出桌面右下角的线性动画卡片提醒。

### 触发条件

- **主窗口不可见或已最小化** 且 **小窗不可见或已最小化**（即软件完全在后台运行，用户未在操作任一窗口）。
- 收到一条 AI 新消息，包括：
  - 正常对话中 AI 流式输出完成的回复；
  - 空闲主动回复（见 §18）在后台自动生成并被触发的消息；
  - 群聊智能体互聊中轮到的 AI 成员发言。

> 若主界面或小窗任一处于可见且未最小化状态，则不会弹出卡片（你已在看界面，无需打扰）。仅将窗口最小化到任务栏也会触发卡片弹出。

### 卡片样式与行为

- 右下角滑入的线性动画卡片（毛玻璃质感），宽约 340px、高约 96px。
- **自动适配系统主题**：卡片配色跟随当前主题，浅色主题（经典微信、活力多彩、青蓝、松绿、霜雪、玫瑰）显示浅色卡片（白底 + 柔和灰影 + 深色文字），深色主题（极简暗色、毛玻璃、星河、赛博、烬燃）显示深色卡片（暗底 + 柔和暗影 + 浅色文字）。切换主题后下次弹出即时生效。
- 显示：角色名 + 消息预览文本。
- **10 秒后自动收回**（滑出动画）。
- **点击卡片**：立即打开主界面并跳转到对应会话。
- **点击右上角 ×**：关闭当前卡片（不影响后续新消息再次提醒）。
- 同一消息做去重防抖，不会因重复事件反复弹出。

### 设置与关闭

- 该功能为软件内置行为，无需额外开关；只要满足「主界面 + 小窗均关闭」的后台条件即生效。
- 卡片滑入时会播放「消息提示音」（可在 §27 音效中关闭）。
- 若想完全避免打扰，可在软件运行时保持主界面或小窗处于打开/可见状态、开启托盘右键「静默模式」，或彻底退出程序（托盘右键 → 退出）。

---

## 27. 音效

软件内置音效系统，在以下三类场景播放提示音（音频文件位于 `public/sounds/`）：

| 场景    | 触发时机                                           | 对应音频                                                           |
| ----- | ---------------------------------------------- | -------------------------------------------------------------- |
| UI 点击 | 点击任意可交互元素时（按钮、左侧栏导航、列表项、卡片、开关、链接等；空白界面无效点击不发声） | `dragon-studio-mouse-click-sfx-free-376869.mp3`                |
| 报错弹窗  | 出现全局错误弹窗（应用异常 / 未捕获错误）时                        | `audley_fergine-ui-button-click-5-327756.mp3`                  |
| 消息提示  | 后台收到 AI 新消息、弹出通知卡片时（随机播放其一）                    | `universfield-new-notification-012/021/022/026/036-*.mp3`（5 个） |

### 设置

- 进入「设置 → 音效」：
  - **启用音效**：总开关，关闭后以上三类音效均不播放。
  - **音量**：滑块调节播放音量（0–100%）。
- 设置会立即生效，下次播放即使用新音量；如需即时刷新缓存可重新打开设置页或重启软件。

### 自定义音效（全局）

在「设置 → 音效」中，可为以下三类音效分别指定**本地 MP3 / WAV 文件**进行替换：

- **报错弹窗音效**、**UI 点击音效**、**消息提示音效** 各自独立，互不影响。
- 每类音效提供三个按钮：
  - **选择文件**：从本机选取一个 `.mp3` 或 `.wav` 文件（仅支持这两种格式）。
  - **试听**：立即播放当前音效（忽略「启用音效」开关，便于试听）。
  - **恢复默认**：清除自定义，回到内置默认音效。
- 自定义文件保存在用户数据目录（`userData/custom-sounds/`，即软件配置目录内），不写入软件安装目录，重装软件或迁移软件目录后需要重新设置。

### 角色自定义消息音效（按人物）

- 在**角色编辑页**点击某个人物 → 「**消息音效**」字段，可为该角色单独指定一个 MP3 / WAV 文件作为它的**专属消息提示音**。
- 若该角色设置了专属消息音效，则**只对该角色生效**，全局「消息提示音效」的自定义/默认都不会作用于它；该角色留空时，沿用全局消息提示音。
- 「报错弹窗音效」与「UI 点击音效」属于全局行为，**不受任何角色自定义影响**。

### 静默模式（托盘右键）

- 在系统托盘图标上**右键** → 菜单中新增「**静默模式**」勾选项，勾选即开启。
- 开启「静默模式」后：
  - **暂停后台消息卡片通知**：软件在后台（主界面 + 小窗均关闭）收到 AI 新消息时，不再弹出 Steam 风格提示卡片。
  - **关闭消息提示音**：后台收到消息不再播放提示音（含角色专属消息音效）。
  - **UI 点击音、报错弹窗音等其余音效照常播放** —— 静默模式只屏蔽「消息提示音」一类。
- 与「设置 → 音效 → 启用音效」总开关的区别：
  - 总开关关闭 = 三类音效（点击 / 报错 / 消息）全部不响。
  - 静默模式开启 = 仅消息提示音与卡片被屏蔽，点击 / 报错声不受影响。
- 设置已持久化：勾选状态写入 `settings.json` 的 `silent` 字段，重启后保持；菜单中的勾选标记实时反映当前状态。

### UI 点击音覆盖范围

- 「UI 点击音」现在覆盖**所有可交互元素**：按钮、工具按钮、模态框按钮、左侧栏导航（通讯录 / 统计 / 资源库 / 设置等）、列表项、角色卡片、聊天项、标签（Tab）、开关（Switch/Toggle）、链接、复选框 / 单选框等。
- **点击纯空白区域（如面板留白、未命中任何可交互元素的背景）不发声**，即「空白界面的无效点击」不会触发点击音。

---

## 28. 异步场景生图

AI 依据对话场景在后台自动生成场景配图，让对话更有画面感。

### 特点

- **指令不进聊天界面**：生图指令（提示词）不会出现对话气泡里，仅生成一张 AI 图片消息；右键图片可「查看提示词」。
- **主窗 / 小窗同步且只生一次**：同一聊天同时只生成一张图，主界面与快捷小窗通过广播实时显示同一张图；两个窗口的开关共用同一份数据，经设置同步，**不会出现「主界面关、小窗还开」**。
- **每个对话独立控制**：在聊天界面工具栏点击 🖼️ 开关，可单独为当前对话开启 / 关闭（单聊按角色、群聊按群组）。

### 设置（设置 → 异步场景生图）

- **两次生图最小间隔（秒）**：距离上一次生图不足该秒数时跳过（10~600 秒，默认 120），避免频繁生图烧 token。
- **场景判定方式**：
  - **AI 判定**（默认）：由轻量模型判断当前对话是否值得配图并产出英文生图提示词，最准但消耗 token。
  - **关键词启发式**：扫描对话中的画面感关键词（风景、海、雨、笑、抱……），免费、零成本，但较粗。

> 前置条件：需在「设置 → 生图」中已开启生图并填写专用生图 API（Base URL / API Key）。未配置生图 API 时，场景生图会自动跳过。

### 自动读取人物头像作参考图（F3）

在「设置 → 异步场景生图」中新增开关 **「生图自动引用人物头像」**：

- 开启后，异步生图会**自动读取当前 AI 人物的头像**（角色头像，非用户头像）作为参考图一并提交给生图模型，显著提升生成图中人物的一致性。
- 与场景无关（无明确场景判定）时不影响生成流程，仅在生图请求里附带头像参考。

### 生视频（独立视频生成 API，F2）

念语支持用**独立的视频生成模型**异步产出视频，与生图调用方式一致：

- **配置**：设置 → 生图之上新增「生视频」配置段：`启用` + `Base URL` / `API Key` / `模型`（复用生图模型列表拉取）+ `尺寸` / `时长`，改动即时落盘。
- **触发**：除「发图生视频」（见上文「发图生图 / 生视频」）外，也可在聊天界面通过 🎬 按钮直接输入文字描述发起（见「生视频（文本 → 视频，🎬 按钮）」）；生成结果经广播同步到主窗口与快捷小窗。
- **完成检测（v2.3.1）**：自动识别两种视频接口——「任务式」（提交后返回任务 ID，每 3 秒轮询 `/videos/generations/tasks/{id}` 直至完成，最多等 9 分钟）与「即时返回」（直接返回视频地址）。生成期间**不阻塞聊天**，进度显示在右上角**可拖动悬浮气泡**（百分比 + 状态），完成后气泡自动收起，视频以 `<video>` 播放器插入对话/朋友圈。
- **前置条件**：需在「设置 → 生视频」中启用并填写专用视频生成 API，否则相关入口不生效。

---

## 29. 联网搜索

类 DeepSeek 联网搜索：在 AI 回答前先检索网络结果链接，再**并发抓取前 N 篇网页正文**（过滤广告/CSS/JS，仅留纯文本）作为参考上下文注入对话，让回答更时效、更准确、且有据可依。

### 特点

- **每个对话独立控制**：聊天界面工具栏点击 🌐 开关单独开启 / 关闭；搜索进行中图标变为 ⏳ 并显示「联网搜索中…」。
- **默认免费可用源（多引擎回退）**：默认搜索源为 **自动（auto）**，依次尝试 **Bing 直爬（cn.bing.com）→ 百度直爬 → DuckDuckGo（Lite 优先、HTML 兜底）**，任一返回结果即用，**全程无需任何 API Key**，且对国内网络友好（DuckDuckGo 在部分地区被屏蔽，自动回退可绕过）。**推荐保持「自动」**；单独选「百度直爬」需百度反爬较宽松，若长期无结果请改回「自动」。
- **声明式注入（强制引用）**：检索结果作为**系统提示（首条 system 消息）**&#x6CE8;入，且指令为**硬性约束**——「必须优先依据这些资料回答，不得凭空编造，也不得只依赖自身记忆；资料含答案时直接引用并融入角色口吻」（v2.2.5 强化，此前指令偏软导致模型被角色人设主导而裸答）。（v2.2.4 已修复「结果未被模型看到」的链路问题：Anthropic 路径与多数 OpenAI 兼容端点只读取首条 system 消息，故结果合并进首条而非追加末尾。）不依赖模型工具调用，兼容任意 OpenAI 格式模型。主进程终端会额外打印 `[websearch] injected search ctx len=… role=…`，可确认检索结果确实进了模型上下文。
- **DeepSeek 式「检索 + 抓取网页正文」（v2.2.6 新增，v2.2.7 修复百度源）**：不再只依赖搜索引擎返回的短摘要——检索拿到结果链接后，会**并发**（同时、非逐个）读取前 N 篇网页的纯文本正文，自动过滤广告 / CSS / JS / 导航，只保留文章主体（单页截断约 2600 字），再作为 grounding 注入模型。这样模型拿到的是「真材实料」而非一两句摘要，引用率与准确度显著提升。单页失败（超时 / 反爬 / JS 渲染页）不影响其余页面；全部抓取失败时自动回退到摘要格式。
  - **⚠️ 百度源特例（v2.2.7 修复）**：百度结果链接是加密跳转（`/link?url=`），目标页多为 JS 渲染 / 反爬站点（百科、知道、知乎），服务端直连几乎抓不到正文。故当搜索源选「百度」时，抓取阶段会**额外用 Bing 取得真实直连 URL**（可服务端渲染的百科 / 新闻等）来抓取正文，最大化命中可读页面的概率（终端日志 `[websearch] baidu→补充 N 条 Bing 直连 URL 用于抓取`）。想省一次检索、或所在网络 Bing 不可达，可把搜索源切到「Bing 直爬」或「自动」——它们本身返回的就是真实直连 URL，抓取成功率更高。**推荐用「自动」或「Bing」而非「百度」做网页正文抓取源。**

### 设置（设置 → 联网搜索）

- **搜索源**：自动（多引擎免费回退，推荐）/ Bing 直爬（免费）/ 百度直爬（免费）/ DuckDuckGo 直爬（免费）/ Tavily（需 Key）/ SerpAPI（需 Key）。
- **搜索 API Key**：仅使用 Tavily / SerpAPI 时需要填写对应 Key；其余免费源均可留空。
- **抓取网页正文（DeepSeek 式）**：开/关。开启后检索结果会并发抓取前 N 篇网页正文作为依据；关闭则只注入搜索摘要。默认开。
- **抓取条数（1~6）**：并发抓取的网页数量上限，默认 5。条数越多依据越全，但耗时与上下文越长。
- **单页超时（ms）**：每篇网页抓取的最长等待时间，默认 8000，范围 2000~20000。超时该页直接跳过。

> 网络环境说明：检索走公网 HTTPS。若所在网络无法访问对应搜索服务，搜索会返回失败（界面短暂提示「联网搜索失败」），但不影响正常对话。开发模式下主进程会在终端打印 `[websearch] provider=… query=… results=N` 日志，可用于确认检索是否真的执行、命中了几条。

### 搜索结果折叠展示

- 开启联网搜索后，每轮检索到的原始结果会以一枚低调的折叠气泡**紧贴在当轮 AI 回复之下**呈现（不再是全聊共享的一个气泡）：默认收起为一行「🌐 联网搜索 · N 条结果」药丸（不喧宾夺主，界面保持简洁）；点击药丸展开即可看到该轮结果的**编号 / 标题 / 摘要 / 链接**。**点击气泡内的编号徽标或任意结果条目，即用系统默认浏览器打开对应网页**（v2.2.10：改用 `shell.openExternal` 拉起外部浏览器，修复了 Electron 中 `<a target="_blank">` 不跳转的失效问题）。**每条 AI 回复各自拥有独立的搜索气泡**，回看历史时哪条回复触发了检索一目了然。
- **结果按数字标序（v2.2.9 新增）**：气泡里每条网页前都有一枚圆形编号徽标 `[1]、[2]…`，与注入模型的资料编号完全一致。AI 回复中会在引用处嵌入对应编号（如「……相关信息[1]」），你点开气泡即可按编号对照来源。编号顺序 = 检索/抓取顺序，去重后稳定。
- **正文内联引用可点击（v2.2.11 新增）**：AI 回复**正文里**出现的 `[1][2]…` 序号本身已渲染为可点击徽标（主题色、加粗、`cursor:pointer`），点击该序号即同上方一样用系统浏览器打开对应网页——无需先展开下方搜索结果气泡。仅 AI 消息、且本轮确有搜索结果时启用；若模型未显式标序号则保持普通文本。
- **引用一致性保证**：气泡展示的网页列表与喂给模型的带编号资料是**同一份、同顺序**（后端一次返回 `{context, pages}`），因此模型写的 `[2]` 必然对应气泡里第 2 条，不会出现错位。
- **点击编号打开网页（v2.2.10 新增）**：气泡内每条结果前的圆形编号徽标可点击——点击该编号（或整条结果）即调用主进程 `app:openExternal`：校验 `http(s)://` 后用系统默认浏览器打开对应网页。仅放行 http/https，避免任意协议被拉起；主界面与小窗均支持。
- 结果仅在**当前对话会话内**展示、不写入存档；同一对话多轮搜索各自归到对应回复之下。群聊一次检索被多个成员共享时，气泡仅挂在首个发言成员的消息下，避免重复。主界面与小窗各自独立显示，二者通过同一广播同步。
- 这是纯展示层：注入 AI 参考上下文的仍是原有机制，气泡只是把「查到了什么、对应几号」直观地回显给你看。

---

## 30. 模型对比（⚔️）

左侧导航栏的 **⚔️ 模型对比** 是一个独立的测试窗口，把**同一个问题**同时发给最多 3 个模型对照作答；**与角色记忆、聊天历史完全无关**（纯测试对话，模型只会拿到问题本身）。

### 使用方式

1. 在底部输入框填写问题，每列顶部下拉框各选一个模型（最多 3 列）。
2. 在「评测模型」下拉框中选择对输出质量打分的模型（默认「默认模型」；也可选择被测模型之一，即让模型之间**互评**）。
3. 点击「开始对比」：三个模型**同时开始**作答，每列独立显示加载动画。
4. 每个对话框**右上角显示该模型完成输出的耗时**（⏱ 秒数），下方显示输出 token 数与字数等客观指标。
5. 全部完成后，所选评测模型自动对每份输出做质量评判（**「质量评分」**&#x533A;域显示 0~100 分 + 简短评语，🟢≥85 / 🟡60-84 / 🔴<60）。
6. 点击「导出 JSON」可将本次对比结果（问题、各模型回复、耗时、token、评判）保存为 `model-compare.json`。
7. **「正在评分」横幅（v2.3.10 起）**：逐模型独立评分进行时，对比面板运行区下方会显示蓝色横幅「正在评分：{模型名}」（带打字指示器动画），明确表示当前正在对哪个模型做质量评判；全部评分结束（收到 `compare:judged` / `compare:done`）后横幅自动收起。若任何时刻看不到该横幅且评分迟迟不出，说明评测请求可能卡住或失败，可结合错误日志排查。

### 说明

- 每列模型可随时更换；更换后该列结果清空，需重新发起对比。
- 参与对比的模型需在「设置 → 模型管理」中处于**启用**状态。
- 评测模型默认使用「设置 → 默认模型」；评测模型与某个被测模型相同时（互评），该列不评判自身输出。
- **逐模型独立评分（v2.3.10 起）**：每个成功输出的模型各自独立发起一次评判请求。某个被测模型输出报错、或单次评判失败 / 解析失败，**只会影响该模型自身的评分**，绝不会连累其他模型的评分；至少有一条成功评分时不再弹全局「评分失败」横幅，局部失败仅记入错误日志。出错的被测模型本身本就不应有评分（符合预期）。
- 评分失败时底部会显示**红色提示**（如模型未返回合法 JSON、评测接口报错等），并记录到错误日志，不再静默吞掉。
- **评判已执行但无有效评分时显示「未评分」**（v2.3.10 起）：评测模型已被选定、某个被测模型却未拿到分数（评测返回非严格 JSON、代码块围栏、或字段写成 `score = 88` 等形式导致解析兜底仍失败）时，该模型输出框下方不再整块留白，而是渲染橙色「未评分（评分模型未返回有效结果）」提示，让你明确看到「该模型未拿到分」而非「什么都没发生」。主进程 `extractJudge` 已对常见非严格 JSON 做正则兜底，绝大多数合法打分都能正常显示。
- 客观指标 = 输出 token、耗时、字数；主观评分 = 评测模型从准确度 / 完整性 / 逻辑 / 可读性四方面打分。两者都看，建议优先参考评分并结合耗时与 token 成本综合判断。

---

## 29. 桌面悬浮球

桌面悬浮球是一个**独立的透明置顶窗口**，与主窗口进程分离，方便随时回到念语而不占用任务栏。

- **独立窗口**：无边框、透明、始终置顶、跳过任务栏、不可缩放；拖拽完全由主进程每帧轮询系统光标直接定位窗口（**未使用** `-webkit-app-region: drag`），窗口始终贴合光标，无跳动、无滞后、快速拖动也不粘滞，并适配高 DPI 与多屏。
- **左键点击**：呼出主界面（不清未读；未读只在该聊天被真正打开时清除）。
- **右键菜单**：在悬浮球上右键弹出菜单，含三项：
  - **退出念语**：安全退出整个软件。
  - **本次关闭悬浮球**：仅关闭当前会话的悬浮球（不改动设置）；下次启动仍按设置恢复显示。
  - **悬浮球置顶**（勾选）：勾选后悬浮球始终处于所有窗口最顶层，不被其它软件覆盖；取消勾选则悬浮球位于普通层级，会被其它窗口遮住。
- **拖拽移动**：按住悬浮球自由拖动，位置自动保存（下次启动恢复），不回弹。
- **未读角标**：主窗口不可见时累计未读；角标 ≤99 显示数字，>99 显示「99+」，无未读则隐藏。角标作为后台新消息提醒，点击悬浮球左键回主界面查看；未读数据仍由主进程 `electron/floatingBall.ts` 的 `unreadMap` 单一维护，打开对应聊天即清。
- **悬停快捷聊天面板（v2.3.10 改）**：鼠标悬停悬浮球即展开**快捷选择聊天面板**（移开自动收起）。面板通过 `api.getChatList()` 列出全部**单聊（💬）与群聊（👥）**，含最近一条消息预览；超过一屏可滚轮下拉查看其余聊天。点击面板内任一聊天，经主进程 `ball:open-chat` 通道**直接呼出对应小窗聊天界面**（`MiniChat`），免去先回主界面再找会话的步骤，同时清除该聊天的未读角标。
- **未读清单单一数据源不变**：未读数据仍全部保存在主进程 `unreadMap`，仅以角标数字提示；主界面侧栏与快捷小窗不维护独立未读清单，不存在「多处清单不同步」。快捷聊天面板（聊天列表）不再展示未读红点，未读仅以角标呈现。
- **两条一致性保证（v2.3.4 修正）**：
  - **已读即清除**：在主界面或快捷小窗中打开任一聊天，主进程 `setActiveChat` 会同步清除该会话未读角标，绝不会出现「消息已读却仍留角标」。
  - **新消息不漏显**：生图 / 生视频完成写入 AI 消息时，若主窗口不可见或未正盯着该聊天，会补计未读；即使结果由后台生成、不走普通流式回复路径，也会照常计入角标。
  - **进入主界面不视为已读**（v2.3.7 修正）：点击悬浮球或左键托盘图标呼出主界面、或窗口从最小化／托盘恢复，都**不**清空未读；只有你真正打开并查看某个聊天（`setActiveChat`）时，该聊天未读才被清除。其余未读继续保留在角标，直到你逐一打开对应聊天。
- **统一类 IM 未读判定（v2.3.9 起）**：未读仅在「你正盯着该聊天本身」时才不计——即主窗口可见**且**该聊天就是当前会话。其余情况（主窗隐藏、或你正在看其他聊天）下，手动回复、AI 主动消息、群聊自动接话产生的所有新消息都计入未读并亮角标。其判定依赖主进程记录的「当前聊天」，由主窗打开聊天时、以及快捷小窗切换会话时回传。
- **跟随主题**：悬浮球、角标与快捷聊天面板均使用当前主题 CSS 变量实时渲染，切换主题后同步变色。
- **无阴影**：悬浮球与快捷聊天面板不使用任何投影。
- **开关与设置**：在「设置 → 窗口与数据 → 桌面悬浮球」中可开启/关闭悬浮球；关闭时窗口被销毁，主进程不再维护未读。同区域还有两个开关：
  - **悬浮球置顶**（默认开）：控制是否始终置于所有窗口顶层，取消后悬浮球会被其它软件覆盖。
  - **全屏时自动关闭悬浮球**（默认开）：主窗口进入全屏时自动隐藏悬浮球，退出全屏后自动恢复；避免遮挡全屏内容。

## 30. 设置搜索框

设置页 **标题栏右侧** 提供搜索框，位于设置正文区域之外，不会遮挡或侵入正文：

- **百度建议式候选**：输入即给出候选，最多 5 条，按关联度排序（标题前缀匹配 > 标题包含 > 关键词匹配）。支持**多关键字拆词**（如「语音 输入」逐词命中），直接按回车跳转到关联度最高的设置项。
- **毛玻璃下拉**：候选框为毛玻璃样式，支持滚轮/滚动条浏览，适配多套主题（使用主题 CSS 变量）。
- **命中高亮闪动**：候选中命中的文字高亮（支持多词同时高亮）；点击候选平滑滚动到对应设置项并**闪动 5 秒**定位。
- **全量覆盖**：搜索范围包含**所有设置项**，不限于分区标题。扫描在设置页挂载后**运行时**进行，自动覆盖四类控件：① 全部勾选框（含 `label` 直接包裹与 `div` 包裹两种写法，如「随机事件」「全局主动消息」「主动消息记忆」「自动记忆」）；② 全部滑块（如「心情过渡指数」「心情判定冷却」）；③ 全部下拉、文本框、文本域（如「默认世界书」「群聊调度」）；④ 分段选项按钮组（如「主动消息触发时长」「切换聊天时的计时行为」）。各子分区标题同样被索引。输入即能定位并跳转，新增设置项自动纳入，无需手工维护。
- 搜索框右侧常驻「打开使用指南」按钮。

## 31. 内置使用指南

在「设置 → 窗口与数据」点击「打开使用指南」，或点击**设置页顶部搜索框右侧常驻的「打开使用指南」按钮**，即可唤起内置指南弹窗，无需联网。

- 覆盖快速开始、AI 配置、聊天/群聊、ASR、TTS、生图、生视频、朋友圈、悬浮球、设置搜索、备份、窗口与小窗等主题。
- 指南内搜索逻辑与设置页一致：百度建议式候选、最多 5 条、命中高亮、点击跳转后闪动 5 秒。

## 32. TTS 按角色音色

在「设置 → 语音（TTS）」填写独立的 TTS 专用 API 后，可为**每位数字人角色单独指定音色**：

- **按角色配置**：在「按角色配置音色」列表中，为单个角色或群成员选择音色；空 = 回退全局默认音色。
- **服务端拉取 + 手动输入**：音色下拉优先从服务端拉取（`/audio/voices`）；服务端不支持该接口时回退内置清单（alloy/echo/fable/onyx/nova/shimmer），也可手动输入自定义音色名。
- **自动播报**：开启「全局自动播报」后，每条 AI 回复自动朗读（使用对应角色的音色）。

## 33. ASR 语音输入修复

语音输入（录音发送）默认改为 **WAV（16kHz 单声道 16-bit PCM）** 上传：

- **根因**：此前 MediaRecorder 产出 webm/Opus，硬编码 `audio/webm` 导致多数 OpenAI 兼容 ASR 服务端返回 400。
- **修复**：前端把录音重封装/重采样为 WAV（兼容性最好），主进程按格式设置正确扩展名与 MIME，并追加 `response_format=json` 与可选 `language`。
- 可在语音设置调整上传格式（wav/mp3/m4a/flac/webm）与强制识别语言。

## 34. 单实例与开屏动画

- **单实例**：程序默认只允许一个实例；若已打开，再次启动会聚焦已有窗口而非新开（基于 `app.requestSingleInstanceLock`）。
- **开屏动画仅「进程冷启动」播放一次**：开屏动画只在软件进程真正冷启动（双击 exe / 开始菜单 / 命令行启动）时播放；从托盘双击唤出、或从悬浮球进入主界面（窗口此前只是隐藏，仍属同一进程）不重播开屏。判定由主进程在重建渲染进程时是否携带 `?nosplash=1` 决定，`App.tsx` 据此初始化 `showSplash`；`hasShownSplash` 仅作历史记录，不参与展示判定。
- **展示逻辑（判定机制）**：冷启动时 `App.tsx` 的 `showSplash` 为 `true`，进入开屏；播放时长默认 1.8 秒（系统开启「减少动效」时缩短为 0.6 秒），动画结束淡出后再过 0.5 秒调用 `onDone()` 进入主界面；开屏期间点击任意位置可立即跳过。

## 35. 错误日志

「设置 → 窗口与数据 → 错误日志」可打开念语运行期间记录的内部异常列表：

- 日志按时间倒序排列，区分「模型错误 / 功能错误 / 其他错误」三类标签。
- 支持一键清空全部日志。
- 弹窗使用固定深色背景 + 浅色文字，确保在任何主题下都能看清内容。

## 36. 主动消息与未读清单修复（v2.3.7）

修掉了「主动消息一定要重新打开窗口才姗姗来迟」以及「新消息不进悬浮球未读角标」这两个问题。

### 主动消息：改由主进程调度

- **根因**：过去由渲染进程用 `setInterval` 每 3 秒自行判断是否触发。主界面关闭到托盘时窗口只是 `hide()`，渲染进程的定时器会被 Chromium 节流乃至冻结，于是计时停摆；重新打开窗口、渲染进程恢复后定时器才继续跑，消息这才补发出来。
- **修复**：计时基准本来就在主进程（`idleState`），现在触发也一并下沉到主进程。主进程每 3 秒检查一次：全局开关 → 该聊天单独开关 → 是否已静默满设定时长 → 该聊天有没有进行中的生成 → 该聊天是否有历史消息，全部满足才发一条。窗口隐藏、最小化、托盘常驻都不受影响。
- **渲染端职责收敛**：主界面与快捷小窗只保留倒计时显示和「进入聊天时初始化计时基准」，不再自行触发，避免主进程与渲染端双触发。
- **单聊与群聊一致**：群聊主动消息走同一条主进程链路（原先同样受渲染端节流影响），已一并修复。

### 未读清单：群聊自动接话也计入

- **根因**：未读判定里区分「手动回复」与「AI 自发消息」。自动接话落库的消息没有打标记，被当成手动回复处理，于是「主窗可见就直接视为已读、不进未读」。
- **修复**：自动接话 / 自动续聊写入的消息带 `from_auto` 标记，与主动消息的 `from_proactive` 一样按「AI 自发」处理：你正盯着该聊天才不算未读，切走或窗口隐藏都进未读清单。手动回复行为完全不变。

### 后台消息卡片

主动消息与自动接话都走统一的 `sendStreamDone`，主进程与渲染端各有一路卡片触发并带 1.5 秒去重，窗口隐藏时照常弹出 Steam 风格卡片，不会出现「消息来了但卡片不弹」。

### 未读判定修正：进入主界面不再清空未读

- **问题**：此前呼出主界面（`showMainWindow`）或点击悬浮球（`ball:activate`）时会调用 `clearAllUnread()`，把全部未读一次性清空。用户只是打开了主窗口、却可能打开的是另一个聊天，托盘期间累积的主动消息未读就这样被悄悄抹掉，导致漏看。
- **修正**：移除上述两处的 `clearAllUnread()` 调用。未读只在「该聊天被真正打开查看」时（`setActiveChat → clearUnreadForChat`）清除；呼出主界面、从最小化／托盘恢复、点击悬浮球，都只显示窗口、不碰未读。其余会话的未读继续留在角标与悬浮球面板，直到你逐一打开对应聊天。

### 聊天内已读未读视觉标记 + 类 IM 已读回执（v2.3.9，v2.3.9 修订增强）

### 已读未读视觉标记（v2.3.11 起全部移除）

- 聊天气泡右上角的「● 未读」徽章、「以下 N 条新消息」未读分隔线、侧栏会话列表的红色未读角标均已按用户要求**移除**，聊天界面不再出现任何已读/未读视觉标记。
- **未读提醒只保留一处**：桌面悬浮球的红色角标与后台消息提醒卡片（详见「桌面悬浮球」章节）。主窗不可见或未正盯着该聊天时，新消息照常计入悬浮球未读；打开对应聊天即清除。
- 已读水位线（`readWatermark`）仍在主进程后台记录（打开聊天即推进），当前无界面消费，仅作为数据保留，便于后续扩展。
- 存储与迁移：水位线随设置（`AppSettings.readWatermark`）持久化到 JSON 文件，无需数据库迁移。

---

## 37. 统计界面

左侧导航栏的 **📊 统计** 汇总 Token 消耗与角色相关统计，含以下板块：

### 人物板块（关系值 + 关系类别）

- 列出与你聊过的数字人角色及其**关系值**（0~100 数值）。
- **关系类别标签（v2.3.10 新增）**：除关系值外，人物行额外展示该角色与你的**具体关系类别**（如「恋人 / 朋友 / 熟人 / 陌生」等），由 `src/types.ts` 的 `RELATION_LABELS[normalizeRelation(relation)]` 取中文标签，`normalizeRelation` 会把旧枚举或自由文本归一为受控类别，脏数据回退原始值，避免出现空白标签。关系类别与关系值一并呈现，直观区分「关系有多深」与「是什么关系」。

### 聊天模型调用量排名（v2.3.10 新增）

- 统计界面新增「聊天模型调用量排名」区块，按**各聊天模型累计 Token 使用量从高到低**排序，每项展示：
  - **Token 总量**：所有聊天类调用（单聊 / 群聊成员 / 群聊导演选人 / 空闲主动消息）的 `prompt + completion` 之和；
  - **调用次数**：该模型被调用的总次数（「调用 N 次」）。
- 以蓝紫渐变进度条按 token 占比可视化，一眼看出哪个模型最常被调用、最耗额度。
- **仅统计当前启用的模型**：模型被更换或删除（不再在「设置 → 模型管理」中启用）即自动退出排名，不会残留失效模型；无调用记录时显示空态「暂无模型调用记录」。
- 数据来自 `electron/db.ts` 的 `modelUsage` 累计（`recordModelUsage` / `getModelStats`），随聊天进行实时增长。

---

> 最后更新: 2026-09-05
